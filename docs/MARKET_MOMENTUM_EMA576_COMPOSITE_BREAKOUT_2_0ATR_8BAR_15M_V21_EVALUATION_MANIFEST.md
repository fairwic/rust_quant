# EMA576 资格周期 + 2.0 ATR + 8 根接受 V21 评估清单

## 研究身份

- 当前等级：L1；无标签覆盖门禁全部通过后，才允许执行一次冻结 L2。
- 候选版本：`market_momentum_ema576_cycle_fresh_breakout_200atr_acceptance8_ema144_retest_structural_stop_cost_cap050r_15m_v21`。
- 基线版本：V20 组合合同；V17 资格周期、V19 八根接受以及 V16 入场与风控合同全部冻结。
- 唯一变量：突破确认收盘距离从 `2.50 ATR14` 放宽为 `2.00 ATR14`。
- 周期与窗口：15 分钟，`1751328000000..=1784470500000`。
- 数据：本地 `quant_core` 已有 60 个返回成员中的 44 个预热合格成员；不补造、不同步 K 线。
- 冻结 V14 SHA-256：`099827aa7924f2e754fa5926e6b50299eaedfc03aa9c5ddc284ce2bf4fed51c1`。
- 冻结 V16 SHA-256：`0c199ad758becefab58a03da53837e85c022b6e4d0e510fa7b34c26332307cf2`。
- 行情指纹：`67516c927ce30323f38f34e6c87fd7bac7720bae8084209cc44b86cce6efe997`。

## 因果定义

所有候选必须同时满足：

1. 从长期资格完成到突破确认，EMA144/576 始终保持资格所属方向关系；关系周期结束立即消费旧资格。
2. 第二根 EMA576 突破确认 K 的方向性收盘距离至少为 `2.00 ATR14`；距离只使用该完成 K 的收盘、
   同棒 EMA576 与 Wilder ATR14，影线不能代替收盘确认。
3. 第一根越线收盘记为第 1 根，必须先连续完成 8 根 EMA576 同侧收盘；接受完成前仍沿用 V20，
   不得提前触及 EMA144±0.30ATR14 回踩区，第八根与回踩信号不得复用同一根 K。

V21 只改变第 2 项阈值，不改变第 1、3 项。L1 只能读取 setup、breakout、signal 以及信号时已经
完成的 EMA144、EMA576、ATR14 和 OHLC，禁止使用 exit、MFE、MAE、最终 R、胜负或 PnL。

## 保持不变

- V16 候选池、下一根连续开盘、每 setup 第一笔真实成交、每币单持仓与冲突顺序不变。
- 初始止损仍为信号时 EMA144±0.30ATR14；目标仍为扣除每边 8 bps 后净 2.00R。
- 入场止损双边成本必须 `<=0.50R`；同棒止损与目标同时触及时 stop-first，最大持有窗口不变。
- 距离或八根接受失败消费当前突破 episode，不恢复已经失效的历史资格。

## 事前覆盖预期与停止条件

- V20 在 2.50 ATR 下保留 325/8,732；V18 无标签距离分布的 P90 为 2.0827 ATR。事前预计 V21
  影响 V16 合格机会 `93.5%～96.3%`，不得根据 outcome 修改区间。
- 必须保留多头和空头各至少 100 个机会，覆盖至少 8 个币、6 个北京时间月份和 100 个一小时方向事件。
- ADA `2026-07-19 01:15` 空头与 ONT `2026-07-19 09:45` 多头仍必须因资格、距离或八根接受
  至少一项失败而拒绝。
- 零命中、目标样本未拒绝、影响比例超出冻结区间或任一分散覆盖门禁失败，立即停止在 L1。
- 若进入 L2，成本后 EV/PF、多空方向或集中度任一冻结门禁失败，固定为 `stop / Research-only`。

## 运行边界

本批次只生成本清单、一份 V21 JSON 机器报告和一份结论报告。不创建或修改 Pine，不注册 Paper、
ReadOnly、Live、scheduler、compose 或生产策略，不写数据库，不触发真实交易。
