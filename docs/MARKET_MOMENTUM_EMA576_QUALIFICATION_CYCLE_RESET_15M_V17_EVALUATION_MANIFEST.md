# EMA576 历史资格同周期失效 V17 预注册

## 假设卡片

- 研究等级：L1 无结果标签快速研究；通过后才允许一次冻结 L2。
- 唯一变量：长期资格从“跨 EMA144/576 关系周期永久锁存”改为“只在建立资格的同一连续关系周期内有效”。
- 因果定义：从 `setup_ts` 后到 `breakout_ts`，多头必须始终保持 `EMA144 < EMA576`，空头必须始终保持 `EMA144 > EMA576`；任意一根已完成 15m K 不再满足关系时立即消费旧资格，后续只能重新累计 144 根并形成新资格。
- 基线版本：V16 `market_momentum_ema576_first_entry_ema144_structural_stop_close_distance_net_target_2r_stop_cost_cap050r_15m_v16`。
- 目标样本：ADA-USDT-SWAP，旧资格 `2026-07-03 08:30 +08:00`，关系首次失效应为 `2026-07-08 18:15 +08:00`，不得支持 `2026-07-19 01:15 +08:00` 空头信号。
- 预计影响：拒绝 V16 成本合格机会的 10%～60%；实际比例只用于验证覆盖，不用于反向改变定义。
- 停止条件：ADA 目标样本仍通过；只影响 1～2 笔；任一方向保留少于 100 个机会；保留少于 8 个币、6 个北京时间月份或 100 个一小时事件。
- 保持不变：当前-live Top60 抽样、本地完整成员、评价窗口、两收盘 EMA576 突破、EMA144±0.30ATR 回踩、V14 1ATR 收盘距离、首笔真实成交、EMA144±0.30ATR 结构止损、V15 净 2R 目标、V16 0.50R 成本门禁、8bps/边成本、24 小时持仓上限、冲突与同币种持仓规则。

## 冻结身份

- V14 合并报告 SHA-256：`099827aa7924f2e754fa5926e6b50299eaedfc03aa9c5ddc284ce2bf4fed51c1`
- V16 合并报告 SHA-256：`0c199ad758becefab58a03da53837e85c022b6e4d0e510fa7b34c26332307cf2`
- 行情指纹：`67516c927ce30323f38f34e6c87fd7bac7720bae8084209cc44b86cce6efe997`
- 周期：15m。
- 评价窗口：`1751328000000..=1784470500000`。
- 数据边界：只读取本机 `quant_core` 已有完成 K，不同步、不补造数据。

## L1/L2 边界

- L1 只读取 setup、breakout 及其之间已完成 K 的 EMA144/576 关系，以及冻结 V16 成交前字段；禁止读取退出、MFE、MAE、R、胜负或 PnL 选择规则。
- 只有全部 L1 目标与覆盖门禁通过，才按完全相同的 V16 成交、止损、目标和成本合同执行一次 L2。
- 当前版本保持 Research-only；不创建 Pine，不接 Paper、ReadOnly、Live、数据库写入、调度或生产。
