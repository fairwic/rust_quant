# Vegas V63 冻结后 4H Forward Shadow 清单

## 1. 冻结身份与用途

- 冻结策略键：`vegas_volume_increase_compressed_sweep_4h_research`。
- 冻结来源版本：`xasset_4h_top100_v63_volume2_compressed_sweep_20260721`。
- 新观察版本：`xasset_4h_top100_v65_v63_frozen_forward_shadow_20260721`。
- V63 的 100 个配置创建于 `2026-07-20 19:55:54 UTC`；对应回测 `back_test_log.id=15620..15719` 创建于 `2026-07-20 19:57 UTC`。
- 新版本必须保持相同策略键、交易对、周期、配置、风险参数和研究语义，只允许修改 `version`、审计元数据与 `kline_end_time`。
- 全部配置保持 `enabled=false`，只运行本地 ResearchBar 回放，不进入 Paper、Live、部署或交易 mutation。

## 2. 预登记时间边界

V63 原始配置的 `kline_end_time=1784376000000`，即 `2026-07-18 12:00:00 UTC`；原回测实际最后使用的 K 线起点为 `2026-07-18 08:00:00 UTC`。本次在查看新增行情和结果之前固定以下两层：

1. **sealed local gap**：信号 K 线起点 `>=2026-07-18 12:00:00 UTC`。这些 K 线没有进入 V63 回测，但发生在策略冻结前，只能作为未被本地模型查看的补充留出，不能称为真正未来样本。
2. **strict forward**：信号所依赖的 4H K 线完成时间严格晚于 `2026-07-20 19:55:54 UTC`。当前首次符合条件的是 `2026-07-20 16:00:00 UTC` 起始、`20:00:00 UTC` 完成的 K 线。
3. 本轮固定回放上界为 `2026-07-20 20:00:00 UTC`，配置使用排他 `kline_end_time=1784577600000`，只纳入当时已经完成的 K 线。
4. 未平仓、样本尾部强制结算或缺少完整退出路径的交易不得作为实现盈亏、EV 或 PF 证据，只报告信号与持仓状态。

## 3. 不变规则

- 继续使用 V63 的压缩突破空头 `relative_volume>=2.0x` 门禁、V60 扫高确认空头与既有首回踩族。
- 不增加或删除 FVG、BOS/CHoCH、MACD、RSI、EMA、ATR、影线、币种或月份条件。
- 不改变止损、止盈、仓位、最大亏损、候选排序、最大并发或相关交易去重。
- 组合口径固定为初始权益 `100U`、风险缩放 `0.375`、最大并发 `12`、单边额外滑点 `5bps`、资金费压力 `1bps/8h`。
- 压力口径固定为单边额外滑点 `10bps`、资金费压力 `2bps/8h`；只有新增交易存在完整退出路径时才计算。
- V65 相对 V63 的共同历史交易必须保持入场时间、方向、入场价、退出时间、退出价与标签一致；任何旧交易变化先判为数据或实现合同问题。

## 4. 数据与停止规则

- 行情只通过 OKX 官方 `history-candles` 增量读取，并写入本地 `quant_core` 4H 分表；不修改历史已完成 K 线。
- 币种固定为 V63 的原 100 个交易对，不补币、不按结果换币。
- 必须报告：每币新增 K 线边界、共同/新增/移除交易、sealed-gap 与 strict-forward 分层、信号家族、多空、退出完整性、有效事件与组合容量。
- 本轮样本长度不足以晋级。即使新增交易全部盈利，也只能保留为 `forward_shadow_observation_insufficient`；不得据此修改阈值或宣称达到策略目标。
- 如果没有新增交易，结论只能是“观察窗口尚未产生机会”，不能把零亏损解释为策略通过。
- 如果出现亏损，只允许记录当时可见特征并等待更多冻结后样本复现；不在本轮历史上立即增加过滤器。

## 5. 预登记状态

- 记录时间：`2026-07-21 04:23 +08:00` 前后，早于增量行情读取与 V65 回放。
- 预登记决策：`frozen_forward_shadow_only`。
- 晋级资格：`false`。

## 6. 首轮观察结果

### 6.1 行情与数据完整性

- 创建 100 条 V65 配置，策略配置和风险配置除 `version`、审计元数据与回放上界外均与 V63 一致；100 条全部保持 `enabled=false`。
- 99 个交易对从 OKX 官方接口完成增量补齐；`LRC-USDT-SWAP` 返回 `51001 Instrument ID doesn't exist`，未伪造或替换交易对。首次 dry-run 暴露“只读模式仍把交易对标成 deleted”的副作用，已增加写模式门禁和回归测试；本地 LRC 行仍为 `deleted`，与本次官方不可用响应一致。
- ETH 历史窗口存在一根已经结束却仍为 `confirm=0` 的旧 K 线。连续性检查已把这类记录纳入修复点，并以 OKX 官方值将 `2026-07-18 12:00:00 UTC` K 线修复为已确认；未修改策略判断。
- V65 单币回测为 `back_test_log.id=15820..15919`，100 个交易对均完成，实际使用的最新 K 线起点为 `2026-07-20 16:00:00 UTC`。

### 6.2 与 V63 的交易事实差分

- V63 与 V65 的完整开平仓事实精确差分为：共同 `243`、新增 `1`、移除 `0`、共同交易变化 `0`。
- 唯一新增为 `NEAR-USDT-SWAP` 多头：入场 `2026-07-20 00:00:00`、价格 `1.917`，初始止损 `1.85949`；`2026-07-20 08:00:00` 以“做多触达指标动态止盈”完整平仓，交易级净收益 `+0.206329R`。该交易不是样本尾部强制结算。
- 这笔 NEAR 的信号属于 `sealed local gap`，发生在 V63 冻结之前；组合执行后的贡献为约 `+0.072888U`。它只能证明行情补齐后的历史交易可复现，不能计入严格 forward OOS。
- 冻结后首根符合 strict-forward 定义的已完成 4H K 线没有产生新增信号或交易，因此本轮严格前向样本数为 `0`。

### 6.3 组合结果与停止结论

标准成本下共 `244` 笔、63 个币种、179 个有效市场事件，约 `8.04` 笔/月；EV `0.671558R`、PF `2.272746`、Sharpe `1.756919`、Recovery `6.662530`、盘中保守回撤 `6.085569%`。样本内仍只有 EV/PF `0.525374R/1.837874`，walk-forward test-2 仍为 `-0.330475R/PF 0.732601`；新增 NEAR 仅使 test-7 变为 7 笔、`0.400191R/PF 1.594743`。

双倍成本下 EV `0.602545R`，但 PF 只有 `2.085475`；样本内进一步降至 `0.458591R/PF 1.690283`，walk-forward test-2 为 `-0.390395R/PF 0.671494`。这与 V63 的时间脆弱性和成本脆弱性结论一致。

本轮结论为 `forward_shadow_observation_insufficient`：聚合标准成本继续过线，但改善只来自冻结前历史缺口的一笔小额盈利，严格 forward 没有交易，不能宣称达到策略目标。V65 保持 disabled，不进入 Paper/Live，也不据此增加 MACD、FVG、BOS、影线或币种过滤器。
