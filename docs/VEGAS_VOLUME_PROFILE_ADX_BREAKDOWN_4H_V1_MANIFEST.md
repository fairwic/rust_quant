# Vegas 成交量价值区 ADX/DMI 跌破 4H V1 清单

## 1. 身份与选择偏差声明

- 策略键沿用 `vegas_volume_profile_value_area_breakout_4h_research`；V73 只给同一入场家族增加趋势强度门禁，不改变风险与退出合同。
- 版本：`xasset_4h_top100_v73_value_area_breakdown_adx25_20260721`。
- V72 已查看方向拆分后才提出 V73，所以“只做空”属于后验新假设；本轮历史只能检验可行性，任何通过都不能冒充全新样本外证明或直接晋级生产。
- V69 继续作为组合旧交易优先基线；V73 单币回放保持独立家族隔离。

## 2. 预注册规则

- 完整继承 V72 的 48 根、24 分箱、70% 价值区与 `2.0x` 放量即时突破语义，但只允许空头。
- 在当前已完成跌破棒上按 Wilder 标准计算 `DMI(14)` 与 `ADX(14)`；要求 `ADX >= 25` 且 `-DI > +DI`。
- `14/14/25` 使用教科书标准趋势强度口径，不根据 V72 盈亏扫描；当前棒已完成，因此指标只使用信号时点及以前数据。
- 入场仍为跌破棒收盘；结构止损仍为 `VAL×1.006` 并受既有最大损失约束；动态退出不变。
- 不增加 MACD、EMA、FVG、BOS/CHoCH、RSI、影线、BTC 状态、币种或成本过滤；不扫描 ADX 周期、阈值或 DI 差值。

## 3. 门禁

- 先与 V72 原始空头组比较漏斗和独立 V73 交易质量；IS/OOS 必须分别为正，PF 必须分别高于 V72 空头对应的 `1.302/1.184`，否则 ADX 没有提供稳定增益。
- 新增家族标准净 EV 仍须 `>=0.6R`、PF `>=2.2`；达不到即早停，不进入 V69 组合。
- 只有增量通过才执行成本上限、统一资金、有效事件、walk-forward 和双倍成本；最终频率仍须 `>=8笔/月`。
- 任一门禁失败即拒绝，不追加 `ADX20/30`、DI 比率、回踩、固定目标或止损扩张。

## 4. 预登记状态

- 记录时间：`2026-07-21`，早于计算 V72 交易的 ADX/DMI 分桶或运行 V73。
- 决策：`research_only_pre_registered_post_direction_split`。
- 配置/Paper/Live 晋级资格：`false`。

## 5. 回放与结论

- `back_test_log.id=16421..16520` 产生 200 笔空头，较 V72 原始 512 笔保留约 39%。100 条配置全部 disabled。
- 总样本 40 胜，EV/PF `-0.077189R / 0.910841`；IS 154 笔为 `-0.214075R / 0.754082`，OOS 46 笔为 `+0.381082R / 1.448443`。
- ADX/DMI 只在 OOS 改善，IS 却从 V72 空头的正期望转为显著负期望，既没有满足跨期增益，也没有达到 EV/PF 门槛。
- 机制解释：`ADX>=25` 描述趋势已经形成，在部分后期样本能排除弱跌破，但早期样本会过滤掉尚未积累高 ADX、随后才进入扩张的机会；它不是稳定的领先确认。
- 最终状态：`rejected_is_negative_time_drift`。不扫描 ADX 阈值、周期或 DI 比率，不进入组合、Paper/Live、部署或交易 mutation。
