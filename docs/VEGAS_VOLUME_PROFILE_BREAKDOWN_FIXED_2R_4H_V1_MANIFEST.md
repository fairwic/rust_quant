# Vegas 成交量价值区跌破固定 2R 4H V1 清单

## 1. 身份与冻结来源

- 策略键：`vegas_volume_profile_value_area_breakout_4h_research`。
- 版本：`xasset_4h_top100_v74_value_area_breakdown_fixed2r_20260721`。
- 入口回到 V72 原始空头，不继承 V73 的 ADX；只改变同一家族退出，因此保留策略键、使用独立版本。
- 方向选择来自已查看 V72 拆分，仍是后验研究；即使历史通过也只能进入 disabled forward shadow。

## 2. 预注册退出假设

V72 空头 512 笔只有约 26% 胜率，但 ATR 盈利单平均约 `+4.66R`。V74 检验：现有动态退出是否让少数长尾过度集中，固定较近目标能否提高兑现率并降低对极端赢家的依赖。

- 入口、价值区、`2.0x` 量能、方向和 `VAL×1.006` 结构止损与 V72 完全一致；只做空。
- 开仓层先按现有最大损失与结构止损选出最终有效初始止损，再以该风险距离冻结 `2.0R` 目标。
- 固定 2R 替换 ATR、指标和信号止盈；最大损失、结构止损及后续合法止损保护仍保留。
- `2R` 是项目既有标准收益风险几何，不扫描 `1.5R/2.5R/3R`，不同时增加 ADX、MACD、EMA、回踩或币种过滤。

## 3. 门禁

- 总样本、IS、OOS必须分别为正；净 EV `>=0.6R`、PF `>=2.2`。
- 固定目标后的收益不得比 V72 更集中于 Top3 交易或单一事件；所有有成交 walk-forward 窗口必须为正。
- 只有增量通过才合并 V69 并执行成本上限、统一资金、有效事件和双倍成本；组合频率仍须 `>=8笔/月`。
- 任一门禁失败即拒绝，不扫描目标或补充指标。

## 4. 预登记状态

- 记录时间：`2026-07-21`，早于计算 V72 空头的 2R 触达路径或运行 V74。
- 决策：`research_only_pre_registered_post_direction_split`。
- 配置/Paper/Live 晋级资格：`false`。

## 5. 回放结果与决策

- `back_test_log.id=16521..16620` 产生 527 笔空头，206 胜；100 条配置全部 disabled。
- 总 EV/PF `+0.091060R / 1.138719`；IS 400 笔为 `+0.153188R / 1.241519`，OOS 127 笔为 `-0.104617R / 0.855951`。
- 205 笔固定目标盈利平均约 `+1.920R`，190 笔信号止损与 131 笔最大损失合计约 `-345.94R`；固定 2R 提高兑现率，却没有形成足够命中率。
- Top3 盈利只占总净 R 约 `12.26%`，长尾集中度显著下降，但跨期质量和硬门槛仍失败。这证明收益集中可以被改善，却不能把入场优势从 `0.22R/PF1.27` 提升到职业级。
- 最终状态：`rejected_oos_negative_and_quality_below_gate`。不扫描其他 R 目标，不进入 V69 组合、Paper/Live、部署或交易 mutation。
