# TradingView Velocity V23 最近横盘首次突破锚点 L1 预注册

## 假设卡片

- 研究等级：L1 无标签可行性扫描。
- 基线版本：`volume_anchor_upthrust_failed_acceptance_short_15m_research_v20@a755168d`。
- 候选版本：`volume_recent_horizontal_first_break_upthrust_failed_acceptance_short_15m_research_v23`。
- 唯一变量：把 V20 的“突破前固定 20 根最高/最低价”替换为“最近已完成且尚未被收盘突破的有效横盘区”。
- 预计影响：预计剔除 V20 新家族候选的 `20%～80%`；该区间只用于预注册覆盖预期，不作为晋级门槛。
- 停止条件：候选零命中；只剩 1～2 笔；SHIB 目标反例仍形成候选；或实现必须同时改变确认棒、量能、止损、目标、冲突或成交路径。

## 因果定义

1. 只读取当前放量突破棒及其之前已经完成、生产可见的 15 分钟 K 线。
2. 在最近 48 根历史中，由近到远寻找固定 8 根的横盘区：宽度不超过中间价的 3%，上下沿各至少有 2 组、相隔至少 2 根的独立触碰。
3. 为拒绝阶梯式拉升或下跌，前后半段上沿漂移不超过箱体高度的 25%，下沿漂移不超过 35%。这些阈值沿用现有 20 根确认箱体口径，不根据本轮结果调整。
4. 从横盘结束到当前棒之前，不允许任何完成棒收盘高于横盘上沿；当前棒必须是首次收盘上破，并同时满足原 V20 放量、阳线和目标档位条件。
5. 在首次突破收盘时冻结横盘上沿、下沿、突破高点和突破量能；只有随后第 1 或第 2 根完成棒扫过突破高点、收阴并重新收回冻结上沿下方，才形成空单候选。
6. 原 V20 的拒绝量能、收盘位置、信号收盘预估风报比、止损、目标、下一根开盘成交、冲突与退出政策全部保持不变。

## 目标样本与无标签边界

- 必须拒绝：`SHIB-USDT-SWAP`，2026-07-06 15m；真实横盘约为 `02:15～04:00`，其上沿在 06:15 放量棒之前已经被多根完成棒收盘突破，因此 06:30 不得再形成该家族空单。
- 必须保留：存在有效横盘、当前棒首次放量收盘上破、随后第 1 或第 2 根扫高并收回冻结上沿的因果样本。
- L1 只读取候选数量、币种/月份/事件覆盖、信号时横盘与冻结风险字段；禁止读取退出时间、MFE、MAE、最终 R、胜负或成本后收益选择定义。
- L1 通过后才允许进入 L2 成本后诊断；L3 之前不创建 Pine、不接 Paper、ReadOnly、Live 或生产调度。
