# Rust Quant 量化交易系统架构设计文档

**最后更新**: 2025-11-10  
**项目版本**: v0.2.0  
**架构模式**: DDD Workspace (14个crate包)  
**状态**: ✅ 已实现并运行

---

## 📋 当前架构概览

项目采用 **Rust Workspace** 模式，包含 14 个独立的 crate 包，遵循 **领域驱动设计(DDD)** 原则。

### Workspace 结构

```
crates/
├── rust-quant-cli/  # 程序入口 (CLI)
├── core/            # 核心基础设施
├── domain/          # 领域模型层
├── infrastructure/  # 基础设施实现层
├── services/        # 应用服务层
├── market/          # 市场数据层
├── indicators/      # 技术指标层
├── strategies/      # 策略引擎层
├── risk/            # 风险管理层
├── execution/       # 订单执行层
├── orchestration/   # 任务编排层
├── analytics/       # 分析报告层
├── ai-analysis/     # AI分析层
└── common/          # 通用工具层
```

---

## 🎯 设计原则（基于三重角色）

### 1️⃣ **Rust 系统级开发视角**
- **异步优先**：tokio 全链路异步，避免阻塞
- **零拷贝**：K线数据处理使用 Arc/Cow，避免不必要的内存分配
- **并发模型**：策略执行并发 + 数据流并发分离
- **类型安全**：编译期保证策略配置正确性

### 2️⃣ **金融量化研究视角**
- **策略即插件**：新增策略无需修改核心代码
- **回测引擎**：独立的回测框架，支持多种回测模式
- **风控优先**：风控逻辑与策略逻辑分离
- **性能可观测**：策略执行时间、指标计算时间、信号生成时间可追踪

### 3️⃣ **加密货币交易视角**
- **交易所抽象**：统一的交易所接口，易于扩展
- **实时数据流**：WebSocket 数据流与业务逻辑解耦
- **订单执行**：订单执行与策略信号生成分离
- **多交易所支持**：OKX, Binance, Bybit 统一接口

---

## 📂 **实际架构（已实现）**

### Workspace 模式结构

```
crates/
├── rust-quant-cli/                 # 🚀 程序入口（CLI接口）
│   ├── src/
│   │   ├── main.rs                 # 程序入口
│   │   ├── lib.rs                  # CLI库 (app_init, run)
│   │   └── app/
│   │       └── bootstrap.rs        # 启动逻辑
│   └── Cargo.toml                  # [[bin]] 配置
│
├── core/                           # 🔷 核心基础设施
│   ├── src/
│   │   ├── config/                  # 配置管理
│   │   │   ├── db.rs               # 数据库配置
│   │   │   ├── env.rs              # 环境变量
│   │   │   ├── log.rs              # 日志配置
│   │   │   ├── redis_config.rs     # Redis配置
│   │   │   └── shutdown_manager.rs # 优雅关闭
│   │   ├── database/               # 数据库层
│   │   │   └── sqlx_pool.rs        # SQLx连接池
│   │   ├── cache/                  # 缓存层
│   │   │   └── redis_client.rs     # Redis客户端
│   │   ├── logger/                 # 日志系统
│   │   │   └── setup.rs             # 日志设置
│   │   └── time/                   # 时间工具
│   └── Cargo.toml
│
├── market/                         # 📊 市场数据层
│   ├── src/
│   │   ├── models/                 # 数据模型
│   │   │   ├── candle_entity.rs    # K线实体 (FromRow)
│   │   │   ├── candle_dto.rs       # K线DTO
│   │   │   ├── tickers.rs          # Ticker数据
│   │   │   └── tickers_volume.rs   # Ticker成交量
│   │   ├── repositories/           # 数据访问
│   │   │   ├── candle_service.rs   # K线服务
│   │   │   └── persist_worker.rs   # 持久化工作器
│   │   ├── streams/                # 数据流
│   │   │   └── websocket_service.rs # WebSocket服务
│   │   ├── exchanges/              # 交易所适配
│   │   │   └── mod.rs              # 交易所接口
│   │   └── cache/                  # 缓存
│   │       └── latest_candle_cache.rs # 最新K线缓存
│   └── Cargo.toml
│
├── indicators/                     # 📈 技术指标层
│   ├── src/
│   │   ├── trend/                  # 趋势指标
│   │   │   ├── ema.rs              # EMA
│   │   │   ├── ema_indicator.rs    # EMA指标
│   │   │   ├── sma.rs              # SMA
│   │   │   ├── nwe_indicator.rs    # NWE指标
│   │   │   └── vegas/              # Vegas指标系统
│   │   ├── momentum/               # 动量指标
│   │   │   ├── rsi.rs              # RSI
│   │   │   ├── macd.rs             # MACD
│   │   │   └── kdj.rs              # KDJ
│   │   ├── volatility/             # 波动性指标
│   │   │   ├── atr.rs              # ATR
│   │   │   ├── atr_stop_loss.rs   # ATR止损
│   │   │   └── bollinger.rs        # 布林带
│   │   ├── volume/                 # 成交量指标
│   │   │   └── volume_indicator.rs
│   │   ├── pattern/                # 形态识别
│   │   │   ├── engulfing.rs        # 吞没形态
│   │   │   ├── hammer.rs           # 锤子线
│   │   │   └── support_resistance.rs # 支撑阻力
│   │   └── cache/                  # 指标缓存
│   │       └── ema_indicator_values.rs
│   └── Cargo.toml
│
├── strategies/                     # 🎯 策略引擎层
│   ├── src/
│   │   ├── framework/              # 策略框架
│   │   │   ├── strategy_trait.rs   # 策略Trait
│   │   │   ├── strategy_common.rs  # 策略通用逻辑
│   │   │   └── mod.rs              # 框架模块
│   │   ├── implementations/        # 策略实现
│   │   │   ├── vegas_executor.rs   # Vegas策略
│   │   │   ├── nwe_executor.rs     # NWE策略
│   │   │   └── comprehensive_strategy.rs # 综合策略
│   │   ├── backtesting/            # 回测引擎
│   │   │   ├── backtest_engine.rs  # 回测引擎
│   │   │   └── mod.rs
│   │   ├── cache/                  # 策略缓存
│   │   │   ├── arc_nwe_indicator_values.rs
│   │   │   └── arc_vegas_indicator_values.rs
│   │   └── adapters/               # 适配器
│   └── Cargo.toml
│
├── risk/                           # ⚠️ 风控层（独立的风控系统）
│   ├── position_risk/              # 仓位风控
│   │   ├── position_limiter.rs     # 仓位限制器
│   │   └── leverage_checker.rs     # 杠杆检查
│   ├── order_risk/                 # 订单风控 [提取自 job/risk_*.rs]
│   │   ├── order_validator.rs      # 订单验证
│   │   └── price_checker.rs        # 价格合理性检查
│   ├── account_risk/               # 账户风控
│   │   ├── balance_monitor.rs      # 余额监控
│   │   └── drawdown_checker.rs     # 回撤检查
│   └── risk_policies/              # 风控策略
│       ├── stop_loss.rs            # 止损策略
│       └── take_profit.rs          # 止盈策略
│
├── execution/                      # 🚀 订单执行层
│   ├── src/
│   │   ├── order_manager/          # 订单管理
│   │   │   ├── order_service.rs    # 订单服务
│   │   │   └── swap_order_service.rs # Swap订单服务
│   │   ├── execution_engine/       # 执行引擎
│   │   │   ├── backtest_executor.rs # 回测执行器
│   │   │   └── risk_order_job.rs   # 风控订单任务
│   │   └── position_manager/       # 持仓管理
│   │       └── mod.rs
│   └── Cargo.toml
│
├── orchestration/                  # 🎼 任务编排层
│   ├── src/
│   │   ├── scheduler/              # 任务调度
│   │   │   ├── job_scheduler.rs    # 任务调度器
│   │   │   ├── scheduler_service.rs # 调度服务
│   │   │   └── task_scheduler.rs   # 任务调度器
│   │   ├── workflow/               # 工作流
│   │   │   ├── tickets_job.rs      # Ticker同步任务 ✅
│   │   │   ├── candles_job.rs      # K线同步任务 ✅
│   │   │   ├── account_job.rs      # 账户任务 ✅
│   │   │   ├── asset_job.rs        # 资产任务 ✅
│   │   │   ├── trades_job.rs       # 成交任务 ✅
│   │   │   ├── strategy_runner.rs  # 策略运行器 ✅
│   │   │   └── backtest_executor.rs # 回测执行器 ✅
│   │   ├── strategy_runner/        # 策略运行器
│   │   │   └── mod.rs
│   │   └── event_bus/              # 事件总线
│   │       └── mod.rs
│   └── Cargo.toml
│
├── analytics/                      # 📊 分析层（数据分析与可视化）
│   ├── performance/                # 性能分析
│   │   ├── strategy_metrics.rs     # [迁移自 trading/services/strategy_metrics.rs]
│   │   └── execution_metrics.rs    # 执行性能分析
│   ├── reporting/                  # 报告生成
│   │   ├── daily_report.rs         # 日报
│   │   └── strategy_report.rs      # 策略报告
│   └── visualization/              # 可视化（可选）
│       └── chart_generator.rs      # 图表生成
│
├── interfaces/                     # 🌐 接口层（对外暴露）
│   ├── cli/                        # 命令行接口 [迁移自 app/]
│   │   ├── commands/
│   │   │   ├── start_strategy.rs   # 启动策略命令
│   │   │   ├── run_backtest.rs     # 回测命令
│   │   │   └── show_metrics.rs     # 查看指标命令
│   │   └── main.rs                 # CLI入口
│   ├── api/                        # REST API（可选）
│   │   └── routes/
│   └── admin/                      # 管理后台（可选）
│
└── common/                         # 🔧 共享工具层
    ├── types/                      # 公共类型 [迁移自 trading/types.rs]
    │   ├── result.rs               # 统一Result类型
    │   ├── decimal.rs              # 高精度数值类型
    │   └── ids.rs                  # ID类型定义
    ├── utils/                      # 工具函数 [迁移自 trading/utils/]
    │   ├── math.rs                 # 数学工具
    │   ├── fibonacci.rs            # 斐波那契工具
    │   └── validation.rs           # 验证工具
    ├── constants/                  # 常量定义 [迁移自 trading/constants/]
    │   ├── timeframes.rs           # 时间周期常量
    │   └── exchanges.rs            # 交易所常量
    └── errors/                     # 错误处理 [增强自 error/]
        ├── app_error.rs            # 应用错误
        ├── market_error.rs         # 市场数据错误
        └── execution_error.rs      # 执行错误
```

---

## 🔄 **依赖关系（严格分层）**

```
┌─────────────────────────────────────────────────────────────┐
│ rust-quant-cli/ (CLI入口)                                   │
└────────────────┬────────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────────┐
│ orchestration/ (任务调度、工作流编排)                        │
└────────────┬───────────────────────┬───────────────────────┘
             │                       │
   ┌─────────▼────────┐   ┌─────────▼─────────┐
   │ strategies/      │   │ execution/        │
   │ (策略逻辑)       │   │ (订单执行)        │
   └────┬──────┬──────┘   └──────┬────────────┘
        │      │                  │
        │      │     ┌────────────▼────────┐
        │      │     │ risk/               │
        │      │     │ (风控检查)          │
        │      │     └─────────────────────┘
        │      │
   ┌────▼──────▼─────┐
   │ indicators/      │
   │ (技术指标计算)   │
   └────┬─────────────┘
        │
   ┌────▼─────────────┐
   │ market/          │
   │ (市场数据层)     │
   └────┬─────────────┘
        │
   ┌────▼─────────────┐   ┌──────────────┐
   │ services/        │   │ infrastructure/│
   │ (应用服务层)     │   │ (基础设施实现)│
   └────┬─────────────┘   └──────┬────────┘
        │                        │
        │   ┌────────────────────▼────────┐
        │   │ domain/                     │
        │   │ (领域模型层)                │
        │   └────────────────────────────┘
        │
   ┌────▼─────────────┐
   │ core/            │
   │ (核心基础设施)   │
   └────┬─────────────┘
        │
   ┌────▼─────────────┐
   │ common/          │
   │ (通用工具层)     │
   └──────────────────┘
```

**依赖规则**：
- **单向依赖**：上层依赖下层，下层不依赖上层
- **水平独立**：同层模块之间通过事件总线通信，避免直接依赖
- **核心稳定**：`core/` 和 `market/` 层最稳定，很少修改
- **策略隔离**：每个策略是独立插件，相互不依赖
