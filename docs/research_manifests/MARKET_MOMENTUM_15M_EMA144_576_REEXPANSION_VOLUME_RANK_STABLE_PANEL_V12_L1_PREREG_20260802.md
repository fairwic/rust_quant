# 15 分钟动量 EMA144/576 重扩张稳定成交额面板 V12：L1 预注册

## 当前等级与已知数据缺口

- 当前等级：`L1 快速研究`；本清单先于任何 V12 全样本候选扫描冻结。
- V11 保持 V6 形态不变，并要求重扩张完成 K 同时出现同方向、`delta_rank >= 0` 的 96 根成交额排名事件。V11 候选减少 24.2045%，但只命中 1/3 用户目标，因此已按定义停止且未读取 outcome。
- 对两个未命中的 BTC 目标做信号时数据质量核对：两根重扩张 K 都是上涨 K，BTC 当时 `new_rank=1`、`delta_rank=0`；但 CVX 的滚动 96 根正成交量窗口只有 43/44 成员有效，现有“全员完整”排名器把整个横截面快照作废。这是输入可用性问题，不是根据胜负、收益或未来价格得到的阈值。

## 单变量假设卡片

- 候选键：`market_momentum_15m_ema144_576_reexpansion_volume_rank_stable_panel_v12`。
- L1 规则版本：`l1_v11_exact_reexpansion_stable_available_panel95_v12`。
- 唯一变量：把 V11 的成交额排名面板政策从“44 个成员必须 100% 同时完整”改为“当时可见成员覆盖至少 95%，且当前与上一 15m 快照的实际成员集合必须完全相同”；V6 形态、精确同 K 对齐、方向、96 根成交额、`min_delta_rank=0` 和全部入场字段不变。
- 因果假设：稳定的 43/44 面板不会因成员进出制造伪排名跳升；允许最多 5% 的当时数据缺口，可避免一个无效成员把其他 43 个可计算标的全部抹掉。
- 预计影响：相对 V6 的 54,837 个候选减少 3%～60%，同时保留三张用户目标；候选不能只集中于单一方向、币种或月份。
- 停止条件：三张目标任一不匹配；候选减少不足 3% 或超过 60%；候选少于 30；多空任一少于 10；覆盖少于 8 个币种、6 个月或 15 个一小时事件；立即停止且不读取 outcome。

## 冻结因果与面板定义

1. V6 的连续 144 根 EMA 历史资格、永久独立资格、两收盘 EMA576 转换、0.75 ATR 重扩张、跨 EMA576 再穿越仍存活的武装订单、0.30 ATR 因果回踩区和首次触碰全部不变。
2. 排名事件仍精确满足 `event_ts = reexpanded_ts_ms + 15m`；不向前或向后扩窗口。
3. 多头仍要求该 K 为上涨 K，空头镜像要求下跌 K；`delta_rank >= 0`、96 根 `vol_ccy * close` 滚动成交额及其余 V11 条件全部不变。
4. 每个时点先按当时已有的 96 根连续、已确认、正价格和正成交额 K 线确定可用成员；实际成员数必须满足 `available * 100 >= expected * 95`。
5. 当前与上一根排名快照的实际成员列表必须逐项相同；若成员进入或离开，跳过该次排名变化。这样排名变化只来自共同成员的成交额变化，不来自分母切换。
6. 该政策只用于 V12 Research-only 加载器；现有生产排名器继续要求 100% 完整，不改变任何已有策略行为。
7. L1 只读取触碰时及此前可见字段；禁止读取成交、退出、MFE、MAE、R、胜负或 PnL。

## 数据身份与 L1 门禁

- V6 源报告 SHA-256：`a69b9cafb83ea55601bc35eaf13a821c0a5fb5080f4d256632457ab3e6f974da`。
- V11 停止报告 SHA-256：`02bbb99a7337c5213c25e5d503268d13c49bd4fe84d4aeeb61d69da6677104dd`，仅作为完整面板缺口证据，不作为收益标签。
- 行情指纹：`67516c927ce30323f38f34e6c87fd7bac7720bae8084209cc44b86cce6efe997`。
- 相同 Top60 抽样、44 个本地 OHLC 完整成员、2025-07 至 2026-07 窗口，不补数据。
- 三张目标必须 3/3；候选相对 V6 减少 3%～60%；多空各至少 10，至少 8 个币种、6 个 UTC 月份和 15 个一小时有效事件。
- 每个候选保存实际面板大小、排名事件 ID/时间、新排名、排名变化、重扩张时间、回踩时间和因果限价锚点；完整账本可哈希。
- `outcome_evaluation_performed=false` 必须成立。

全部通过也只能标记为 `coverage_pass_ready_for_l2_prereg`。任一失败即停止 V12，不创建 Pine，不注册 Paper/ReadOnly/Live，不修改生产 preset。
