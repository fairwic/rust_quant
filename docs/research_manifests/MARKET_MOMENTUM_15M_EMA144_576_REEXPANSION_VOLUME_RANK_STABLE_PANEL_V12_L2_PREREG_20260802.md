# 15 分钟动量 EMA144/576 重扩张稳定成交额面板 V12：L2 预注册

## 当前等级与授权身份

- 当前等级：`L2 多币种诊断`；本清单先于读取 V12 候选的成交后结果冻结。
- V12 L1 报告 SHA-256：`201114b4ae1e519793f2988f000e00b5751c05fffc710ad0146e28340eb14dd1`。
- V12 候选键：`market_momentum_15m_ema144_576_reexpansion_volume_rank_stable_panel_v12`。
- V12 L1 规则：`l1_v11_exact_reexpansion_stable_available_panel95_v12`。
- V12 L2 规则：`l2_v12_touch_limit_sl04_r052_hold24h_cost8bps_v1`。
- V6 原始 L1 SHA-256：`a69b9cafb83ea55601bc35eaf13a821c0a5fb5080f4d256632457ab3e6f974da`。
- 行情指纹：`67516c927ce30323f38f34e6c87fd7bac7720bae8084209cc44b86cce6efe997`。

## 单变量与保持不变项

- 相对已完成的 V6 L2，唯一变量是只允许 V12 L1 的 48,048 个信号时稳定面板候选进入成交回放。
- V6 的 0.30 ATR 因果限价、跳空仅取开盘价格改善、4% 初始止损、0.52R 固定目标、24 小时最长持仓、同棒止损优先、每币种单持仓锁全部不变。
- 压力成本保持单边 8 bps，开平双边按实际价格折算为 R。
- Top60 返回成员、44 个本地完整成员、2025-07 至 2026-07 窗口不变；不补 K 线、不新增币种、不改变 EMA seed。
- 不同时比较目标、止损、排名阈值、面板覆盖率或退出变体。

## 因果回放与冲突顺序

1. 必须同时校验 V6 源报告和 V12 无标签授权 SHA、候选键、规则、行情指纹、候选数与 `outcome_evaluation_performed=false`。
2. 仅按 `(symbol, signal_ts_ms, direction)` 从 V6 账本提取 V12 授权候选；缺失、重复或多余身份立即失败。
3. 信号 K 内触及预先可知的 `EMA144 ± 0.30 ATR14` 限价才成交；跳空穿越只按开盘价改善，不取盘中最优价。
4. 同一币种持仓期间的后续信号忽略；持仓结束后才允许下一笔。
5. 成交 K 即参与止损/目标判断；同棒二者都触及时按止损优先。
6. 24 小时未触发保护则按最后一根可见收盘退出；forward 不完整单独记录，不伪造成交结果。

## L2 停止与升级门禁

- 至少 30 笔完整交易，多空各至少 10 笔，覆盖至少 8 个币种、6 个月和 15 个一小时有效事件。
- 毛收益与 8 bps/side 成本后都必须满足 `expectancy_R > 0` 且 `Profit Factor > 1`。
- 多头和空头成本后必须分别为正，不能由一个方向补贴另一个方向。
- 移除净 R 最高两笔后仍为正；移除净 R 最高事件簇后仍为正。
- 单币和单事件簇占全部正净 R 的比例均不得超过 35%。
- 每笔限价、止损、目标、成本和候选授权合同必须全部一致。

任一门禁失败即停止 V12。全部通过也只能标记 `L2_pass_L3_required`，仍不得创建 Pine、注册 Paper/ReadOnly/Live、修改生产 preset 或触发实盘。
