# 15 分钟动量 EMA144/576 重扩张成交额排名确认 V11：L1 预注册

## 当前等级与问题定义

- 当前等级：`L1 快速研究`；本清单先于任何 V11 扫描结果读取冻结。
- V6 完整表达用户三张图，但脱离动量上下文后 L2 成本后为负；V10 又证明整套既有 breakout/FVG 上游会排除三张目标，不能把该形态降格为现有 FVG 的替代壳。
- V6 的 `reexpanded_ts_ms` 是回踩订单武装前最近一次价格相对 EMA144 扩张至少 0.75 ATR 的完成 K。与数万根前的永久历史资格或旧 transition 相比，它是每次回踩前最新、可因果观察的动量时点。
- V11 只检验这个重扩张 K 是否同时出现现有 `kline_15m` 96 根滚动成交额排名事件；不读取 V6/V9 的任何交易结果来选阈值。

## 单变量假设卡片

- 候选键：`market_momentum_15m_ema144_576_reexpansion_volume_rank_v11`。
- L1 规则版本：`l1_v6_reexpansion_same_candle_volume_rank_nonworse_v11`。
- 唯一变量：在 V6 全部形态规则不变的基础上，要求武装回踩单的 `0.75 ATR` 重扩张完成 K 同时具有同方向 15m 成交额排名事件；不增加 RSI、布林、FVG、价格形态或结果过滤。
- 因果假设：EMA 历史资格说明方向背景，0.75 ATR 重扩张说明价格重新加速，而同 K 的成交额排名不恶化说明这次加速具有横截面参与度；三者共同定义“15m 动量回踩”。
- 预计影响：相对 V6 的 54,837 个候选减少 20%～95%，但保留三张用户目标；候选不能只集中于单一方向、币种或月份。
- 停止条件：三张目标任一不匹配；候选减少不足 20% 或超过 95%；候选少于 30；多空任一少于 10；覆盖少于 8 个币种、6 个月或 15 个一小时事件；立即停止且不读取 outcome。

## 冻结因果定义

1. EMA144/EMA576 连续 144 根历史资格、两收盘突破、24 根内 0.75 ATR 离开、永久独立资格、已武装订单跨 EMA576 再穿越存活、0.30 ATR 动态回踩区及上一根已完成 EMA144/ATR14 锚点，全部严格复用 V6。
2. 对每个 V6 候选，取其已冻结的 `reexpanded_ts_ms`。成交额排名事件时间使用完成时刻，因此必须精确满足 `rank_event.ts = reexpanded_ts_ms + 15m`；不允许向前或向后扩窗口。
3. 多头要求排名事件来自阳线，空头镜像来自阴线；事件与候选方向必须一致。
4. 沿用现有 `kline_15m` 事件源的 96 根滚动 quote turnover 排名和 `min_delta_rank=0` 口径，即本 K 排名不得比上一根更差；不在查看结果后提高排名跃升阈值。
5. 一个重扩张事件可对应其后第一次回踩候选；V6 已保证每次武装只消费第一次触碰，不新增去重或冷却变量。
6. L1 只读取当前及此前已完成 K、排名事件和 V6 信号字段；禁止读取触碰后的价格、退出、MFE、MAE、R、胜负或 PnL。

## 数据身份与 L1 门禁

- V6 源报告 SHA-256 必须为 `a69b9cafb83ea55601bc35eaf13a821c0a5fb5080f4d256632457ab3e6f974da`，行情指纹必须为 `67516c927ce30323f38f34e6c87fd7bac7720bae8084209cc44b86cce6efe997`。
- 使用相同 Top60、44 个本地完整成员、2025-07 至 2026-07 窗口，不补数据。
- 三张目标图必须 3/3 命中。
- 候选相对 V6 减少 20%～95%；最终至少 30 个，多空各至少 10，至少 8 个币种、6 个 UTC 月份和 15 个一小时有效事件。
- 每个候选必须保存 rank event ID、时间、new rank、delta rank、重扩张时间、回踩时间和因果限价锚点；完整账本必须可哈希。
- `outcome_evaluation_performed=false` 必须成立。

全部通过也只能标记为 `coverage_pass_ready_for_l2_prereg`。任一失败即停止 V11，不创建 Pine，不注册 Paper/ReadOnly/Live，不修改生产 preset。
