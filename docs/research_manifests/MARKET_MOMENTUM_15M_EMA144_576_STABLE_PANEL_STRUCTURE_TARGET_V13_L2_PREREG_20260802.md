# 15 分钟动量 EMA144/576 稳定面板结构目标 V13：L2 预注册

## 当前等级与授权身份

- 当前等级：`L2 多币种诊断`；本清单先于读取 V13 候选的成交后结果冻结。
- V13 L1 报告 SHA-256：`d0cac4f5650d0f9df579081c4db62cedc8d6194288ab21aab5fa5663d7c38ec0`。
- V13 候选账本 SHA-256：`9f1395ea3d576d73a384863397fb69b9c6ea0c19cb51cf16b8a53ca0011e8b97`，候选数 48,019。
- V13 候选键：`market_momentum_15m_ema144_576_stable_panel_structure_target_v13`。
- V13 L1 规则：`l1_v12_latest_confirmed_fractal2_structure_target_lookback96_v13`。
- V13 L2 规则：`l2_v12_touch_limit_sl04_latest_confirmed_fractal2_target_hold24h_cost8bps_v13`。
- 行情指纹：`67516c927ce30323f38f34e6c87fd7bac7720bae8084209cc44b86cce6efe997`。

## 单变量与保持不变项

- 相对 V12 L2，唯一变量是把每笔固定 `0.52R` 目标替换为 V13 L1 已冻结的绝对结构目标价。29 个无法构造目标的 V12 候选不进入回放，不允许回退固定目标。
- V12 的 EMA144/576 入场形态、稳定 95% 成交额面板、48,048 个源候选身份和信号时间不变。
- 限价仍为前一根已完成 K 的 `EMA144 ± 0.30*ATR14`；跳空穿越只按开盘价获得价格改善。
- 初始止损仍为实际成交价的 4%；结构目标价不随跳空改善重新计算，因此每笔实际目标 R 可以不同。
- 从成交 K 开始检查退出；同 K 同时触发止损与目标时止损优先；最长持仓 24 小时，超时按当根收盘退出。
- 同币种同时只允许一笔持仓，信号落在既有持仓退出 K 及之前时忽略。
- 成本仍为开平每边 8 bps 的手续费加滑点压力，按实际进出价格折算 R。
- 相同 Top60 抽样、44 个本地完整成员、2025-07 至 2026-07 窗口；缺失或 forward 不完整只记录阻塞，不补 K 线。

## L2 授权与合同校验

1. 必须校验 V13 L1 文件 SHA、V12/V13 候选键、规则版本、行情指纹、候选数、账本哈希、3/3 用户目标和 `outcome_evaluation_performed=false`。
2. 每笔按 `(symbol, signal_ts_ms, direction)` 映射回冻结 V6/V12 入场候选；缺失、重复、多余身份或目标价不一致立即失败。
3. 每笔结构目标必须在信号开始前确认、距信号 3～96 根、位于计划限价盈利侧；L2 不新增目标 R 上下限。
4. 逐笔验证限价、实际成交、4% 止损、冻结结构目标、毛 R、双边成本、净 R、退出原因和事件簇身份。
5. 报告原始候选、解析候选、实际成交、完整/不完整交易、持仓冲突、币种、月份、方向和一小时事件覆盖。

## 查看结果前冻结的升级门禁

- 完整交易至少 30，多空各至少 10，至少 8 个币种、6 个 UTC 月份和 15 个有效市场事件。
- 毛期望 `> 0` 且毛 PF `> 1`。
- 成本后期望 `> 0` 且成本后 PF `> 1`。
- 多头与空头成本后分别都必须满足期望 `> 0`、PF `> 1`。
- 移除成本后最优两笔仍为正；移除最优一小时事件簇仍为正。
- 单币种和单事件簇占全部正净 R 的最大比例分别不超过 35%。
- 全部逐笔合同校验通过。

任一门禁失败即停止 V13。全部通过也只能标记 `L2_pass_L3_required`，仍不得创建 Pine、注册 Paper/ReadOnly/Live、修改生产 preset 或触发实盘。
