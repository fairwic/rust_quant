# 15 分钟动量布林长影回归 V1：EMA12/144/576 单根排列刚形成过滤 L1 预注册

## 当前等级与认知框架

- 当前等级：`L1 快速研究`。
- 已知的已知：用户最终指定只判断 EMA12、EMA144、EMA576；保持单根排列，不要求连续确认与斜率。
- 已知的未知：三均线排列首次出现对 673 个 Bollinger 长影基础 setup 的无标签覆盖率与多空分布尚未扫描。
- 未知的已知：通用 `ComputedCandle` 当前只有 EMA12/144/169/696，不能用 EMA696 冒充 EMA576；项目其他 Vegas/TradingView 规则已存在真实 EMA576 口径。
- 未知的未知：均线极小间距、current-live Top60 幸存者偏差和 15m OHLC 同根退出顺序可能影响后续结论，但均不属于本轮变量。

## 冻结身份

- 候选键：`market_momentum_bollinger_wick_reversion_15m_v1`
- L1 规则版本：`l1_bb20x2p5_wick_touch_reject_new_opposite_ema12_144_576_alignment_v1`
- 来源策略：既有 `market_momentum_exhaustion_reversal_15m_v2`，精确入场版本为 `kline15m_filtered_vol2p5_net96x8_wick_limit12_volume_atr_tp_v2`。
- 来源 cohort：先由上述 V2 产生多空方向，再保留其 `directional_wick_limit_12_candles` 分支中触碰对应 `Bollinger(20,2.5)` 外轨的 setup；EMA 只否决候选，不产生或改写多空方向。
- 运行边界：Research-only；不覆盖 Momentum V2/V3，不注册 Pine、Paper、ReadOnly 或 Live，不写回数据库。

## 保持不变的基础形态与退出语义

- 既有 V2 做空：过滤量比 `>=2.5`、当前 `vol_ccy` 达到此前 672 根的 nearest-rank P90、此前 96 根净移动 `>=+8%`，且信号 K 实体占比 `>10%`、上影占比 `>=60%`、上影严格长于下影；以信号 K 高点作为最多 12 根有效的限价。
- 新增做空条件：上述既有 V2 空头成立后，信号 K 的 `high >= Bollinger upper(20,2.5)`。
- 既有 V2 做多镜像：此前 96 根净移动 `<=-8%`，满足同一成交量门禁，且下影占比 `>=60%`、下影严格长于上影；以信号 K 低点作为最多 12 根有效的限价。
- 新增做多条件：上述既有 V2 多头成立后，信号 K 的 `low <= Bollinger lower(20,2.5)`。
- 来源过滤后量比 `>=2.5`、前 672 根 `vol_ccy` P90、前 96 根净移动 8%、实体与 60% 方向影线、影线极值限价 12 根以及 `1.5 ATR14[p]` 初始止损身份全部不变。
- 首次回归因果中轨时平掉原始仓位 50%，余仓止损移动到实际开仓价；余仓在对侧外轨附近或 `+5R` 先到者完全平仓。本轮只登记，不执行退出回放。

## 唯一变量：EMA12/144/576 对向单根排列刚形成

只使用信号 K 收盘时已经完成的当前 K 与上一根 K：

1. 当前多头排列：`EMA12[p] > EMA144[p] > EMA576[p]`；
2. 当前空头排列：`EMA12[p] < EMA144[p] < EMA576[p]`；
3. `newly_formed=true`：当前 K 首次满足该方向严格排列，而上一根 K 不满足同方向严格排列；
4. 做多反向信号只在空头排列 `false -> true` 时拒绝，做空反向信号只在多头排列 `false -> true` 时拒绝；
5. 不读取 EMA169、EMA696，不要求连续 3 根，不检查 EMA576 或其他 EMA 的斜率，也不增加均线距离与确认棒；
6. 当前或上一根任一所需 EMA 缺失、非有限或非正数时标记 `not_ready`，不得悄悄放行。

EMA12、EMA144 沿用当前候选链；EMA576 使用相同公式独立计算：首个完整 576 根收盘价的 SMA 为种子，之后按 `alpha=2/(576+1)` 递推。任何订单最早从信号 K 之后的下一根 K 开始生效。

## 预注册预期与停止条件

预计该精确过滤影响基础 setup 的 `2%~25%`。必须同时满足：

1. 命中 setup 至少 10 个，影响比例位于 `2%~25%`；
2. 按方向与 60 分钟单链归并后至少 5 个有效市场事件；
3. 至少覆盖 4 个币种和 3 个 UTC 月份；
4. 做多、做空各至少 2 个；
5. 全部基础 setup 的当前 K 与上一根 K 三条 EMA 状态均可判定。

零命中或只影响 1～2 个 setup 立即停止。任一门槛失败均不得读取成交、后续 K 线、MFE、MAE、退出、最终 R、胜负或 PnL。全部数值门槛通过时，因目标图表尚未审计，状态最多为 `coverage_pass_target_audit_pending`。

## 数据身份

- `sample_limit=60`
- `sample_seed=top60_v36_direct_kline_20260721`
- 评价窗口：`1751328000000..=1784470500000`
- 只使用本地已有且完整的 current-live Top60 成员；缺失成员跳过且不补造 K 线。
- EMA 输入指纹覆盖加载器冻结的 699 根预热窗口；该窗口只保障 EMA576 递推与上一根状态可判定，不构成多根确认。
- current-live Top60 存在幸存者偏差，仅用于 L1 同口径诊断。
