# 15 分钟动量布林长影回归 V1：EMA 趋势新形成过滤 L1 预注册

> 纠错状态：`rejected_definition_mismatch`。用户最终确认只看信号 K 单根 EMA 严格排列，
> 不要求连续 3 根，也不要求 EMA696 斜率。本文件保留为错误定义的审计记录，不得作为策略结论或运行入口。

## 当前等级与纠错边界

- 当前等级：`L1 快速研究`。
- 用户已明确：趋势由 EMA 均线排列确定。此前把 96 根净移动连续成立时间当作趋势年龄属于定义错误，旧批次标记为 `rejected_definition_mismatch`，不得沿用其阈值或结论。
- 本轮只研究一个变量：反向信号出现时，对向 EMA 趋势确认状态是否刚从 `false` 变为 `true`。
- 本轮只读取信号 K 及其之前已经完成且生产可见的 EMA；禁止读取成交、后续 K 线、MFE、MAE、退出、最终 R、胜负或 PnL。

## 冻结身份

- 候选键：`market_momentum_bollinger_wick_reversion_15m_v1`
- L1 规则版本：`l1_bb20x2p5_directional_wick_outer_touch_reject_new_opposite_ema_trend_v1`
- 来源 cohort：已经满足来源量比、96 根净移动、60% 方向长影线，并触碰对应 `Bollinger(20, 2.5)` 外轨的 673 个基础 setup。
- 运行边界：Research-only；不覆盖 Momentum V2/V3，不注册 Paper/Live，不写回数据库。

## 保持不变的基础形态

- 做空：长上影线，且信号 K 的 `high >= Bollinger upper(20, 2.5)`。
- 做多：长下影线，且信号 K 的 `low <= Bollinger lower(20, 2.5)`。
- 来源过滤后量比 `>=2.5`、前 672 根 `vol_ccy` P90、前 96 根净移动 8%、实体与 60% 方向影线、影线极值限价 12 根以及 `1.5 ATR14[p]` 初始止损身份全部不变。
- 已登记的退出语义保持不变：第一次回归中轨平掉原始仓位 50%，余仓止损移动到实际开仓价；本轮不执行退出。

## 唯一变量：对向 EMA 趋势刚确认

直接复用当前 Core 已存在的 15m 长期 EMA 趋势合同，不另造周期或阈值：

1. 多头排列：最近连续 3 根已完成 K 均满足 `EMA12 > EMA144 > EMA169 > EMA696`；
2. 空头排列：最近连续 3 根已完成 K 均满足 `EMA12 < EMA144 < EMA169 < EMA696`；
3. 多头还要求截至当前 K 的最近 4 个 EMA696 逐根严格上升；空头要求逐根严格下降；
4. `newly_formed=true`：当前信号 K 完成后上述确认成立，但在前一根 K 完成时同方向确认不成立；
5. 长下影做多只有遇到“刚确认的空头趋势”才拒绝；长上影做空只有遇到“刚确认的多头趋势”才拒绝；
6. 同向趋势、EMA 过渡/缺值状态，以及已经持续两根或更久的对向趋势，不由本轮变量拒绝。

所有 EMA 都由当时及之前的收盘价递推。信号 K 完成后才能确认排列，任何订单最早从下一根 K 开始生效。

## 预注册预期与停止条件

预计该精确过滤影响基础 setup 的 `2%~25%`。必须同时满足：

1. 刚形成的对向 EMA 趋势 setup 至少 10 个，且不得超过基础 cohort 的 25%；
2. 按方向与 60 分钟单链归并后至少 5 个有效市场事件；
3. 至少覆盖 4 个币种和 3 个 UTC 月份；
4. 做多、做空各至少 2 个。

若只命中 1～2 个、需要加入第二个 EMA 距离/斜率阈值才成立，或影响超过 25%，立即停止。数值门槛通过时，因用户尚未提供目标图表，状态最多为“无标签覆盖通过、目标图表审计待完成”，不得直接进入收益回放。

## 数据身份

- `sample_limit=60`
- `sample_seed=top60_v36_direct_kline_20260721`
- 评价窗口：`1751328000000..=1784470500000`
- 继续使用与 H1 相同的本地 current-live Top60；只纳入完整成员，缺失成员跳过且不补造 K 线。
- current-live Top60 存在幸存者偏差，仅用于 L1 同口径诊断。
