# 15 分钟动量布林长影回归 V1：近期 EMA12 领先双慢线 96 根 L1 预注册

## 当前等级与认知框架

- 当前等级：`L1 快速研究`。
- 已知的已知：192 根版本已完成无标签扫描，信号时年龄账本显示 673 个基础 setup 中有 423 个处于对向 EMA12 领先状态且年龄不超过 96 根。
- 已知的未知：独立 96 根版本能否精确复现该覆盖，机器身份、目标样本与门禁是否保持一致。
- 未知的已知：NMR 目标样本年龄为 158 根，因此只修改上限为 96 后必然不再拒绝 NMR；这不是运行后才能发现的结果。
- 未知的未知：缩短窗口后被保留与被拒绝交易的成本后差异。本轮在 L1 门禁通过前禁止读取该结果。

## 冻结假设卡片

- 研究等级：`L1`。
- 唯一变量：对向 EMA12 领先状态的拒绝年龄上限从 192 根改为 96 根 15m K，即从 48 小时缩短为 24 小时。
- 因果定义：只使用信号 K 及其之前已经完成的 EMA12、EMA144、EMA576；首次成立年龄为 1，连续成立逐根增加，中断归零。
- 基线版本：`l1_bb20x2p5_wick_touch_reject_opposite_ema12_leads_144_576_age_le_192_v1`。
- 新规则版本：`l1_bb20x2p5_wick_touch_reject_opposite_ema12_leads_144_576_age_le_96_v2`。
- 目标币种、周期与窗口：current-live Top60、15m、`1751328000000..=1784470500000`。
- 预计影响：根据已经存在且不含 outcome 的年龄分布，预计拒绝 423/673，约 `62.85%`。
- 停止条件：影响比例不在原冻结的 `5%~45%`；任一目标样本不再被拒绝；EMA 不完整；或需要第二个变量才能降低覆盖。
- 保持不变：币种池、数据窗口、既有 Momentum V2 方向、Bollinger(20,2.5)、量比、周 P90、96 根净移动、影线、限价、止损、目标、退出、冲突与资金路径。

## 方向与过滤顺序

1. 既有 `market_momentum_exhaustion_reversal_15m_v2` 先产生多空候选；
2. 只保留 `directional_wick_limit_12_candles` 分支；
3. 做空要求信号高点触碰 `Bollinger upper(20,2.5)`，做多要求信号低点触碰 `Bollinger lower(20,2.5)`；
4. 做空的对向状态仍为 `EMA12 > EMA144 && EMA12 > EMA576`，做多完全镜像；
5. EMA144 与 EMA576 的相对顺序无关；不要求连续三根确认，不读取 EMA169、EMA696 或任何 EMA 斜率；
6. 唯一变化是仅在 `1 <= age_bars <= 96` 时拒绝候选。

## 原停止条件保持不变

必须同时满足：

1. 全部基础触轨 setup 均 EMA-ready；
2. 至少拒绝 20 个，影响比例位于 `5%~45%`；
3. 做多、做空各至少 5 个；
4. 至少覆盖 10 个币种、6 个 UTC 月份和 15 个按方向与 60 分钟归并的有效事件；
5. AGLD、YFI、NMR、ORDI、SATS 五个原始目标样本全部存在、年龄精确匹配并被拒绝。

不因阈值缩短而放宽第 5 项。已知 NMR 年龄为 158，若机器结果按预期放行 NMR，则目标门禁失败并停止；不得事后把目标改成 4/5。任何门禁失败均不读取 fill、未来 K、MFE、MAE、退出、R、胜负或 PnL，不进入 L2。

## 数据与运行边界

- `sample_limit=60`
- `sample_seed=top60_v36_direct_kline_20260721`
- 只使用本地已有且满足预热的成员，不同步或补造 K 线。
- 复用相同 EMA 计算：EMA12/144 使用现有 SMA 种子序列，EMA576 使用 576 根收盘价 SMA 种子及 `alpha=2/(576+1)` 递推。
- Research-only；旧 192 根版本继续可复现，不修改 Pine、Paper、ReadOnly、Live、数据库、策略注册或生产执行。
