# 15 分钟动量布林长影回归 V1：单根 EMA 排列刚形成过滤 L1 预注册

> 纠错状态：`rejected_definition_mismatch`。用户最终确认只判断 `EMA12/EMA144/EMA576`；
> 本批错误使用 EMA169 与 EMA696，以下内容仅保留审计，不得作为策略结论或运行入口。

## 当前等级与四层认知框架

- 当前等级：`L1 快速研究`。
- 已知的已知：基础形态固定为来源量比与方向长影线成立后，做空信号 K 的高点触碰 `Bollinger(20,2.5)` 上轨，做多信号 K 的低点触碰下轨；EMA 周期固定为 12、144、169、696。
- 已知的未知：单根排列首次出现的无标签覆盖率、双向分布、币种与月份分散性尚未扫描；用户尚未提供多、空与边界目标图表。
- 未知的已知：上一批次额外加入连续 3 根排列和 EMA696 斜率，已经超出用户最终定义，状态为 `rejected_definition_mismatch`，其 5 个命中不能复用。
- 未知的未知：均线极小间距、current-live Top60 幸存者偏差与 15m OHLC 同根退出顺序可能影响后续结论；这些都不是本轮变量。

## 冻结身份

- 候选键：`market_momentum_bollinger_wick_reversion_15m_v1`
- L1 规则版本：`l1_bb20x2p5_directional_wick_outer_touch_reject_new_opposite_single_bar_ema_alignment_v1`
- 来源 cohort：已经满足来源量比、96 根净移动、60% 方向长影线，并触碰对应 `Bollinger(20,2.5)` 外轨的基础 setup。
- 运行边界：Research-only；不覆盖 Momentum V2/V3，不注册 Pine、Paper、ReadOnly 或 Live，不写回数据库。

## 保持不变的基础形态与退出语义

- 做空：长上影线，且信号 K 的 `high >= Bollinger upper(20,2.5)`。
- 做多：长下影线，且信号 K 的 `low <= Bollinger lower(20,2.5)`。
- 来源过滤后量比 `>=2.5`、前 672 根 `vol_ccy` P90、前 96 根净移动 8%、实体与 60% 方向影线、影线极值限价 12 根以及 `1.5 ATR14[p]` 初始止损身份全部不变。
- 第一次回归因果中轨时平掉原始仓位 50%，余仓止损移动到实际开仓价；余仓在对侧外轨附近或 `+5R` 先到者完全平仓。本轮只登记该语义，不执行退出回放。

## 唯一变量：对向单根 EMA 排列刚形成

只使用信号 K 收盘时已经完成的当前 K 与上一根 K：

1. 当前多头排列：`EMA12[p] > EMA144[p] > EMA169[p] > EMA696[p]`；
2. 当前空头排列：`EMA12[p] < EMA144[p] < EMA169[p] < EMA696[p]`；
3. `newly_formed=true`：当前 K 首次满足该方向严格排列，而上一根 K 不满足同方向严格排列；
4. 做多反向信号只在空头排列 `false -> true` 时拒绝，做空反向信号只在多头排列 `false -> true` 时拒绝；
5. 不要求连续 3 根排列，不读取或限制 EMA696 斜率，不增加均线距离、角度、确认棒或年龄窗口；
6. 当前或上一根任一 EMA 缺失、非有限或非正数时标记 `not_ready`，不得悄悄当成非趋势放行。

EMA 全部由当时及之前的收盘价递推。信号 K 完成后才能确认当前排列，任何订单最早从下一根 K 开始生效。本轮候选账本禁止包含成交、后续 K 线、MFE、MAE、退出、最终 R、胜负或 PnL。

## 预注册预期与停止条件

预计精确的首次单根排列过滤影响基础 setup 的 `2%~25%`。必须同时满足：

1. 命中 setup 至少 10 个，且影响比例位于 `2%~25%`；
2. 按方向与 60 分钟单链归并后至少 5 个有效市场事件；
3. 至少覆盖 4 个币种和 3 个 UTC 月份；
4. 做多、做空各至少 2 个；
5. 全部基础 setup 的当前 K 与上一根 K EMA 状态均可判定。

任一门槛失败即停止，不读取结果标签。全部数值门槛通过时，因目标图表尚未审计，状态最多为 `coverage_pass_target_audit_pending`，不得直接进入收益回放。

## 数据身份

- `sample_limit=60`
- `sample_seed=top60_v36_direct_kline_20260721`
- 评价窗口：`1751328000000..=1784470500000`
- 继续使用与基础 H1 相同的本地 current-live Top60；只纳入完整成员，缺失成员跳过且不补造 K 线。
- EMA 输入指纹覆盖加载器冻结的 699 根预热窗口；699 仅用于 EMA696 递推与前一根状态可判定，不构成连续 3 根确认。
- current-live Top60 存在幸存者偏差，仅用于 L1 同口径诊断。
