# 15 分钟动量 EMA144/576 永久资格 + 有限价格 Episode 回踩 V8：L1 预注册

## 当前等级与问题定义

- 当前等级：`L1 快速研究`；本清单先于 V8 扫描及任何 V8 结果读取冻结。
- V6 已证明永久资格语义可覆盖用户三张图，但其 `active transition` 也永久存活，三张目标距 active 建立分别达到 34,510、32,485、33,460 根 15m K。这是信号时状态证据，不是事后收益筛选。
- V7 收盘守线过滤因排除用户明确有效的第三张图而在 L1 停止；V8 不使用该过滤。

## 单变量假设卡片

- 候选键：`market_momentum_ema144_576_finite_price_episode_retest_15m_v8`。
- L1 规则版本：`l1_persistent_qualification_finite_price_episode_latest_arm_reexpand075atr_first_touch030atr_v8`。
- 唯一变量：把 V6 永久 `active transition` 改为由价格穿越 EMA576 定义的有限 episode；EMA144/EMA576 的 144 根历史 qualification 仍永久保存。
- 预计影响：相对 V6 减少 30%～85% 候选，并显著缩短 `bars_since_activation`；该预计只约束生命周期是否真正生效，不用于选择收益阈值。
- 停止条件：三张用户目标任一不匹配；候选少于 30；只剩单一方向、币种或月份；生命周期改动必须依赖第二个过滤器；或 L1 形态覆盖/分散门禁失败。
- 保持不变：144 根历史资格、两收盘突破、24 根内 0.75 ATR 离开、0.75 ATR EMA144 重扩张、0.30 ATR 动态触碰区、上一根已完成 EMA144/ATR 锚点、Top60、窗口和一小时事件归并。

## 因果状态机

多头如下，空头完全镜像：

1. EMA144 曾连续 144 根位于 EMA576 下方后，多头历史资格永久有效；数据或指标断档时失败关闭并重建。
2. 两根已完成 K 从 EMA576 下方向上突破，并在 24 根内达到 `close-EMA576 >= 0.75*ATR14`，开启多头价格 episode。
3. episode 开启期间，已完成 K 位于 EMA576 上方且相对 EMA144 重扩张 `0.75*ATR14` 时武装动态 EMA144 回踩单。
4. 若价格随后完成两根向下穿越 EMA576，多头价格 episode 结束，不能继续产生新的多头回踩单。
5. episode 结束前已经武装的多头订单不取消，仍按用户要求跨 EMA576 存活到第一次触碰；触碰后若 episode 已结束，必须等待下一次有效向上突破才能重新武装。
6. 新的同方向有效突破或另一方向完成重扩张并武装时，可以替换更旧的尚未触发订单，保证只保留最新因果上下文。

当前 K 的触碰始终先于该 K 收盘后的 episode 结束或新 transition 更新处理，避免用当前收盘撤销在 K 开始前已经存在的订单。

## L1 数据与门禁

- 数据身份、60/44 成员、评价窗口、预热和目标窗口与 V6 完全相同；不补数据。
- 三目标必须全部命中：NMR `1782835200000..=1782878400000` long；BTC `1782943200000..=1782964800000` long；BTC `1783828800000..=1783850400000` long。
- 候选至少 30，多空各至少 10，至少 8 个币种、6 个 UTC 月份和 15 个一小时有效事件。
- V8 候选数必须少于 V6 的 54,837，且减少比例落在预注册的 30%～85%。
- L1 不读取成交、退出、MFE、MAE、R、胜负或 PnL；通过也只能标记为 `coverage_pass_ready_for_l2_prereg`。

任一门禁失败立即停止。L1 通过后才允许冻结 V8 L2，且仍不得创建 Pine、修改 Paper/ReadOnly/Live 或覆盖生产 preset。
