# 15 分钟动量 EMA144/576 永续历史资格回踩挂单 V6：L2 预注册

## 晋级依据与边界

- 当前等级：`L2 多币种本地诊断`；本清单先于 V6 的任何成交后结果读取冻结。
- L1 报告：`docs/backtest_reports/market_momentum_ema144_576_persistent_qualification_order_retest_15m_v6_l1_20260802.json`。
- L1 报告 SHA-256：`a69b9cafb83ea55601bc35eaf13a821c0a5fb5080f4d256632457ab3e6f974da`。
- 行情指纹：`67516c927ce30323f38f34e6c87fd7bac7720bae8084209cc44b86cce6efe997`。
- 晋级证据：54,837 个候选、3,747 个一小时去簇事件、44 个本地完整币种、13 个 UTC 月份，多头 22,569、空头 32,268，用户三张目标图 3/3 命中，L1 明确未读取结果标签。
- 局限：当前 Top60 是现时成员快照，16 个成员因本地预热或数据缺口被跳过，存在幸存者偏差；本轮不是 OOS、walk-forward、统一资金曲线或生产晋级验证。

## 冻结假设卡片

- 候选键：`market_momentum_ema144_576_persistent_qualification_order_retest_15m_v6`。
- L2 规则版本：`l2_touch_limit_sl04_r052_hold24h_cost8bps_v1`。
- 唯一研究变量：在现有 15 分钟动量的风险和退出合同下，新增已经通过 L1 的 V6 EMA144/EMA576 回踩入场定义；不得再修改 144 根资格、两收盘突破、24 根转换窗口、0.75 ATR 离开/重扩张、0.30 ATR 回踩区或生命周期语义。
- 预计结果：该形态在单边 8 bps 压力成本后应保持正的交易级期望和 Profit Factor，且正收益不能集中于 1～2 笔、单一币种或单一市场事件。
- 停止条件：成本后总期望或 PF 不为正、任一方向不为正、移除头部交易/事件后不为正、集中度超限或覆盖门禁失败，立即停止 V6；不得根据结果调整止损、目标、成本、持仓时间或形态阈值。

## 成交、风险与退出合同

1. 程序必须校验 L1 文件 SHA、候选键、规则版本、L1 晋级状态、未读取结果标签、候选数和行情指纹；重载本地行情后必须再次得到相同行情指纹。
2. V6 在触碰 K 开始前已经用上一根已完成 K 的 EMA144/ATR14 形成限价区。多头限价为 `EMA144 + 0.30*ATR14`，空头镜像为 `EMA144 - 0.30*ATR14`。
3. 若触碰 K 跳空穿过限价，采用不劣于限价的开盘成交：多头 `min(open, limit)`，空头 `max(open, limit)`；否则按限价成交。不得使用触碰 K 收盘或未来更优价格。
4. 复用当前 15 分钟动量基线风险：初始止损距离为实际入场价的 `4%`；目标为 `0.52R`；最长持仓 `24h`。不启用保本、移动止损、分批退出、反手或第二目标。
5. 从成交 K 开始检查保护单；同一 K 同时触发止损与目标时按止损先发生。24h 到期按最后一根可见完整 K 收盘退出；forward 不完整单独记录，不进入绩效。
6. 零成本和压力成本使用完全相同的成交与退出路径。压力成本为每边 `5 bps` 手续费加 `3 bps` 等价滑点，共 `8 bps/side`，按实际开平名义价格折算为 R。
7. 每个币种同一时刻最多一笔持仓；持仓期间后续 V6 信号按现有 `ignore-entry-signal-updates-while-open` 口径阻塞。退出 K 内不接受新的同币种信号，下一根 K 才可重新入场。
8. L2 只报告逐币同资金风险单位的交易级结果，不把它冒充统一资金、容量、杠杆或组合权益曲线。

## 事件归并与门禁

- 有效市场事件按 `方向 + 距上一触发不超过 60 分钟` 跨币种归并；该规则在读取结果前冻结。
- 完整交易至少 30 笔；多空各至少 10 笔；至少覆盖 8 个币种、6 个 UTC 月份和 15 个有效市场事件。
- 零成本与 8 bps/side 成本后总体净每笔期望都必须大于 0，Profit Factor 都必须大于 1。
- 成本后多头、空头各自净每笔期望必须大于 0，Profit Factor 必须大于 1。
- 移除成本后净 R 最高两笔后，总净 R 仍须大于 0；移除正收益最高的单一事件簇后，总净 R 仍须大于 0。
- 任一币种占全部正成本后 R 的比例不得超过 35%；任一事件簇占全部正成本后 R 的比例不得超过 35%。
- 成交、止损、目标、成本、同棒顺序、同币种锁和 L1 身份合同必须全部通过一致性校验。

全部通过只能标记为 `L2_pass_L3_required`。任一失败即保留 Research-only 证据并停止，不创建 Pine，不注册 Paper/ReadOnly/Live，不修改生产 preset。
