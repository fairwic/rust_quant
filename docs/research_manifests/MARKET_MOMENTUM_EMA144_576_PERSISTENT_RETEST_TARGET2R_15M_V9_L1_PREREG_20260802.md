# 15 分钟动量 EMA144/576 永久资格回踩 V9：2.0R 固定止盈 L1 预注册

## 当前等级与证据边界

- 当前等级：`L1 快速研究`；本清单先于任何 V9 结果读取冻结。
- V9 完整复用 V6 已通过 L1 的入场候选账本：`docs/backtest_reports/market_momentum_ema144_576_persistent_qualification_order_retest_15m_v6_l1_20260802.json`。
- 冻结源文件 SHA-256：`a69b9cafb83ea55601bc35eaf13a821c0a5fb5080f4d256632457ab3e6f974da`；行情指纹：`67516c927ce30323f38f34e6c87fd7bac7720bae8084209cc44b86cce6efe997`。
- V6 L1 的 54,837 个候选、44 个完整本地成员、13 个 UTC 月份和三张用户目标图 3/3 命中均保持不变；V9 不重新筛选入场。
- L1 只读取源账本的策略身份、信号时间、方向、因果限价边界和目标图审计，不读取成交后的 K 线、退出、MFE、MAE、R、胜负或 PnL。

## 单变量假设卡片

- 候选键：`market_momentum_ema144_576_persistent_retest_target2r_15m_v9`。
- L1 规则版本：`l1_v6_entry_fixed_sl04_target2r_geometry_v9`。
- 唯一变量：将 V6 L2 的固定止盈从 `0.52R` 改为市场常用的 `2.0R`；这是一套独立研究身份，不回写或覆盖 V6。
- 因果假设：EMA144 回踩属于趋势延续入场，若形态确有延续优势，使用 `2.0R` 固定目标应比 `0.52R` 更能保留趋势收益；是否成立只能在本清单通过后由 L2 成本回放检验。
- 预计影响：入场候选、方向、币种、月份和目标图覆盖应与 V6 完全相同；所有正价格候选都应形成有限且为正的 4% 止损和 2.0R 目标。
- 停止条件：源账本身份不一致、任何候选保护价无效、候选数或覆盖发生变化、三张目标图不再 3/3，立即停止；不得查看结果后调整目标到其他 R 倍数。

## 保持不变的合同

1. 入场形态仍为 V6：EMA144/EMA576 连续 144 根历史资格永久保存，两根收盘突破 EMA576，24 根内达到 0.75 ATR 离开，随后相对 EMA144 重扩张 0.75 ATR，并在上一根已完成 EMA144/ATR14 形成的 0.30 ATR 动态区第一次触碰。
2. 已武装回踩单可跨越中途 EMA576 再穿越存活；数据或指标断档仍失败关闭。
3. L2 若获准，仍按触碰 K 开始前可准备的限价成交；跳空只接受开盘价改善，不读取盘中最优价。
4. 初始止损仍为实际入场价的 4%；最长持仓仍为 24 小时；同棒止损优先；不启用保本、移动止损、分批退出、runner 或反手。
5. 成本仍为每边 8 bps；同币种最多一笔持仓，退出 K 内不重入；币种池、窗口、数据指纹和事件归并保持不变。

## L1 几何门禁

- 源 L1 必须明确为 `coverage_pass_ready_for_l2_prereg` 且 `outcome_evaluation_performed=false`。
- 54,837 个源候选必须全部保留，且多空、币种、月份和三张用户目标图覆盖不变。
- 对因果限价边界 `entry`：多头止损/目标为 `entry*(1-0.04)` 与 `entry*(1+0.04*2.0)`；空头镜像为 `entry*(1+0.04)` 与 `entry*(1-0.04*2.0)`。全部价格必须有限且大于零。
- 保护价与入场价的距离必须严格对应 `1R` 止损和 `2R` 目标，且多空镜像一致。
- L1 不读取任何 outcome。全部通过也只能标记为 `coverage_pass_ready_for_l2_prereg`，随后必须另行冻结 L2 清单。

任一门禁失败即停止 V9，不创建 Pine，不注册 Paper/ReadOnly/Live，不修改生产 preset。
