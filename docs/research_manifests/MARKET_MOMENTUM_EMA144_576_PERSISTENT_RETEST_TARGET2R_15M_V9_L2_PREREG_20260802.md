# 15 分钟动量 EMA144/576 永久资格回踩 V9：2.0R 固定止盈 L2 预注册

## 晋级依据与边界

- 当前等级：`L2 多币种本地诊断`；本清单先于任何 V9 成交后结果读取冻结。
- V9 L1 授权报告：`docs/backtest_reports/market_momentum_ema144_576_persistent_retest_target2r_15m_v9_l1_20260802.json`，SHA-256 为 `31dbd99af9d9a0cc42b659eb99ef3038a22596dd15712adfa3671b57b2128769`。
- V6 入场源账本：`docs/backtest_reports/market_momentum_ema144_576_persistent_qualification_order_retest_15m_v6_l1_20260802.json`，SHA-256 为 `a69b9cafb83ea55601bc35eaf13a821c0a5fb5080f4d256632457ab3e6f974da`。
- 行情指纹：`67516c927ce30323f38f34e6c87fd7bac7720bae8084209cc44b86cce6efe997`。
- L1 证据：54,837 个候选全部具有有效 2R 保护价，三张用户图 3/3 保留，且 L1 未读取结果标签。
- 局限：当前 Top60 是现时成员快照，只有 44 个本地完整成员；本轮不是 point-in-time 币池、OOS、walk-forward、统一资金曲线或生产晋级验证。

## 单变量假设卡片

- 候选键：`market_momentum_ema144_576_persistent_retest_target2r_15m_v9`。
- L2 规则版本：`l2_v6_touch_limit_sl04_r20_hold24h_cost8bps_v1`。
- 唯一研究变量：相对 V6 L2，只把固定目标从 `0.52R` 改为 `2.0R`；V6 的完整入场、4% 止损、24h 持仓、成交、同棒冲突、成本、同币种锁、窗口和币种范围全部冻结。
- 预计结果：在每边 8 bps 压力成本后，总体和多空两个方向的交易级期望及 Profit Factor 均为正，且正收益不依赖 1～2 笔、单一币种或单一市场事件。
- 停止条件：成本后总体 EV 或 PF 不为正、任一方向不为正、移除头部交易或事件后不为正、集中度超限、保护价合同漂移或覆盖不足，立即停止 V9；不得根据结果继续尝试 1R、1.5R、2.5R 等未预注册目标。

## 成交、风险与退出合同

1. 程序必须同时校验 V6 源账本和 V9 L1 授权报告的完整 SHA、候选键、规则版本、候选数、无 outcome 状态和行情指纹；重载行情后必须重建同一 V6 L1 身份。
2. 入场仍使用触碰 K 开始前由上一根已完成 EMA144/ATR14 形成的限价：多头 `EMA144+0.30*ATR14`，空头镜像。跳空穿越时只接受开盘价改善，否则按限价成交。
3. 初始止损距离仍为实际入场价的 `4%`；唯一变化是目标固定为 `2.0R`，即多头目标 `entry*1.08`，空头目标 `entry*0.92`。
4. 最长持仓仍为 24h；从成交 K 开始检查保护单；同棒同时触发止损与目标时止损优先；超时按最后一根完整 K 收盘退出。
5. 不启用保本、移动止损、分批退出、runner、反手或第二目标。forward 不完整单独记录，不进入绩效。
6. 零成本与压力成本使用相同路径。压力成本仍为每边 5 bps 手续费加 3 bps 等价滑点，共 `8 bps/side`。
7. 每个币种同一时刻最多一笔持仓；持仓期间和退出 K 内的后续信号阻塞，下一根 K 才可重入。
8. L2 只报告逐币同风险单位的交易级结果，不冒充统一资金、容量、杠杆或组合权益曲线。

## 事件归并与门禁

- 有效市场事件继续按 `方向 + 距上一触发不超过 60 分钟` 跨币种归并。
- 完整交易至少 30 笔；多空各至少 10 笔；至少 8 个币种、6 个 UTC 月份和 15 个有效市场事件。
- 零成本和 8 bps/side 成本后总体 EV 必须大于 0，PF 必须大于 1。
- 成本后多头、空头各自 EV 必须大于 0，PF 必须大于 1。
- 移除净 R 最高两笔后和移除正收益最高单一事件簇后，总净 R 均须大于 0。
- 任一币种或任一事件簇占全部正净 R 的比例不得超过 35%。
- 成交、4% 止损、2R 目标、成本、同棒顺序、同币种锁、V6 源身份和 V9 L1 授权必须全部通过一致性校验。

全部通过只能标记为 `L2_pass_L3_required`。任一失败即保留 Research-only 证据并停止，不创建 Pine，不注册 Paper/ReadOnly/Live，不修改生产 preset。
