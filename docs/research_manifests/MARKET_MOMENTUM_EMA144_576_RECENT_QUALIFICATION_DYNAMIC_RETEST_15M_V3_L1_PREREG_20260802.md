# 15 分钟动量 EMA144/576 近期历史资格动态回踩 V3：L1 预注册

## 当前等级与前序停止证据

- 当前等级：`L1 快速研究`；本清单在 V3 扫描和任何结果回放之前冻结。
- V1 一次性阶段语义只匹配 1/3 用户目标；V2 历史 active 锁存语义匹配 2/3，二者均保持 `rejected_definition_mismatch`，不覆盖失败证据。
- V2 在本地同一数据指纹下产生 40,998 个无标签候选、4,077 个方向/60 分钟事件；覆盖不是停止原因。
- V2 未匹配 BTC 2026-07-12：目标窗口中的 long 资格曾在更早完成，但“最近资格”已被 short 覆盖；之后价格再次向上转换时无法复用仍然很近的历史 `EMA144<EMA576` 状态。
- 上述信息只来自 OHLC、EMA144、EMA576、ATR14、状态机方向与时间戳，不含成交和收益标签。

## 冻结身份与唯一变量

- 候选键：`market_momentum_ema144_576_recent_qualification_dynamic_retest_15m_v3`
- 规则版本：`l1_age144_recent576_dual_transition_reexpand075atr_first_touch030atr_v3`
- 唯一变量：将 V2 的“只保存最近一个均线资格方向”改为“多空资格独立记录；每次连续 144 根形成的资格在后续 576 根 15m K 内有效”。
- `576` 根等于慢均线自身周期，即 6 天；不是根据后续盈亏搜索出的阈值。本批次不比较 288/384/768。
- V2 的方向转换、0.75 ATR 离开、EMA144 重扩张、0.30 ATR 前一根锚点触碰与一次触碰一次武装语义全部保持不变。

## 因果规则

1. 连续 144 根 `EMA144<EMA576`，记录一个 long qualification；连续 144 根 `EMA144>EMA576`，镜像记录 short qualification。
2. 两个方向的 qualification 独立保存；从完成 K 起最多 576 根内可以被对应价格转换使用，之后因时间过期失效。
3. 仍在有效期内的 long qualification，遇到连续两根收盘站上 EMA576，且确认后 24 根内 `close-EMA576>=0.75ATR14`，active 切换为 long；short 完全镜像。
4. active long 后，已完成 K 的 `close-EMA144>=0.75ATR14` 武装下一次回踩；后续首次 `low<=上一根EMA144+0.30×上一根ATR14` 形成 long 候选并解除武装。short 完全镜像。
5. active 只由另一个方向完整价格转换切换；EMA144/EMA576 单独交叉不切换 active。
6. 触碰 K 是否守线、刺穿深度只入账本，不参与 L1 筛选。触碰使用上一根已完成 EMA144/ATR14，避免同根未来函数。

## 冻结目标、数据与门禁

- 三张目标窗口、Top60 seed、评价窗口、720 根预热、数据缺口策略与 V2 完全相同。
- NMR：`1782835200000..=1782878400000` long。
- BTC 2026-07-02：`1782943200000..=1782964800000` long。
- BTC 2026-07-12：`1783828800000..=1783850400000` long。
- 数据：OKX USDT 永续、15m、`confirm='1'`、`sample_limit=60`、`sample_seed=top60_v36_direct_kline_20260721`。
- 评价窗口：`2025-07-01 00:00:00 UTC` 至 `2026-07-19 14:15:00 UTC`。

门禁保持：三目标全部命中、候选至少 10、有效事件至少 5、至少 4 个币种和 3 个月、双向各至少 2、BTC/NMR 输入完整。全部通过后最多进入 `coverage_pass_ready_for_l2_prereg`；L1 不读取未来 K、MFE、MAE、成交、退出、R、胜负或 PnL，不接入任何运行时。
