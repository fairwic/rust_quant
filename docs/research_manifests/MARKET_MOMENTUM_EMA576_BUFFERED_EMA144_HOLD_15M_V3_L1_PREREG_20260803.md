# EMA576 活动方向下 EMA144 缓冲守稳 15m V3：L1 预注册

## 单变量假设卡片

- 当前等级：`L1 快速研究`；本预注册先于 V3 全样本扫描和任何成交后字段读取冻结。
- 候选键：`market_momentum_ema576_exclusive_active_ema144_buffered_hold_15m_v3`。
- 规则版本：`l1_v2_same_lifecycle_close_hold030_v3`。
- 基线版本：V2 `market_momentum_ema576_exclusive_active_ema144_retests_15m_v2`，机器报告 SHA-256 为 `61f8bec3b76670cf3336a8b8b2e842b8c88b65ce0a73f25f1b54b4b2647ca615`。
- 唯一变量：把 V2“多头触碰当根收盘必须不低于 EMA144”改成“多头触碰当根收盘不得低于 `EMA144 - 0.30 ATR14`”；空头从“收盘不高于 EMA144”镜像改成不得高于 `EMA144 + 0.30 ATR14`。
- 因果假设：用户所说“不有效跌破”包含收盘仍处在 EMA144 的 ATR 缓冲带内；轻微收在线下不是有效跌破，收盘越出 `0.30 ATR14` 才算失守。该判断只读取触碰当根已完成 K。
- 预计影响：相对 V2 的 44,781 个候选预计增加但仍不超过 60,000；覆盖不少于 8 个币种、6 个 UTC 月份和 100 个一小时事件，多空任一不得少于 10。
- 停止条件：三张正样本任一未命中；ALGO/MERL 任一错误空单时间触发；候选超过 60,000；或定义必须读取下一根及更晚 K、成交和收益字段。
- 保持不变：V2 的 144 根 EMA144/576 长期关系、80% 价格侧、两根收盘 EMA576 突破、唯一活动方向、重复回踩再武装、EMA144 上下 `0.30 ATR14` 触碰区、冻结币池、窗口、数据缺失政策和 Research-only 边界。

## 冻结守稳定义

1. V2 的方向资格、EMA576 突破接管和重复回踩生命周期完全不变。
2. 价格仍须先以完成 K 收盘离开 EMA144 的 `0.30 ATR14` 区域，之后第一次重新触碰该区域才进入守稳判断。
3. 多头触碰仍要求最低价到达 EMA144 附近且不低于 `EMA144 - 0.30 ATR14`；空头最高价镜像不得高于 `EMA144 + 0.30 ATR14`。
4. V3 仅把触碰当根的有效失守线从 EMA144 本身移到同一条区域外沿：多头 `close >= EMA144 - 0.30 ATR14`，空头 `close <= EMA144 + 0.30 ATR14`。
5. 守稳与失守均在触碰 K 收盘时确认，不允许等待后续 K 反包或回收；因此没有未来函数，也没有补偿入场。
6. 触碰成功形成候选；触碰失败仍沿用 V2 的重新离区再武装规则。
7. 每条候选只保存信号时可见字段；L1 不计算下一根成交、止损、目标、MFE、MAE、R、胜负或 PnL。

## 五张图与 L1 门禁

- 正样本：NMR 2026-07-01 00:00～12:00、BTC 2026-07-02 06:00～12:00、BTC 2026-07-12 12:00～18:00，均为北京时间多头，必须 3/3。
- 反样本：ALGO 2026-07-19 17:30、MERL 2026-07-19 18:45 的空头必须 0/2。
- 全部五张图通过，且候选、方向、币种、月份和事件覆盖通过，才可标记 `coverage_pass_ready_for_l2_prereg`。
- 任一失败立即停止 V3，不读取 outcome、不创建 Pine、不注册 Paper/ReadOnly/Live、不修改生产 preset。
