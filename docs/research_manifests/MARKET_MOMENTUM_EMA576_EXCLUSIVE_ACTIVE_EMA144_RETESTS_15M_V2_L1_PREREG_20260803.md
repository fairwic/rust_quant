# EMA576 互斥活动方向下 EMA144 重复回踩 15m V2：L1 预注册

## 单变量假设卡片

- 当前等级：`L1 快速研究`；本预注册先于 V2 全样本扫描和任何成交后字段读取冻结。
- 候选键：`market_momentum_ema576_exclusive_active_ema144_retests_15m_v2`。
- 规则版本：`l1_v1_exclusive_active_rearm_same030_hold_v2`。
- 基线版本：V1 `market_momentum_ema576_first_breakout_ema144_pullback_hold_15m_v1`，机器报告 SHA-256 为 `03b9162be3c09d16d9ab1509638a29cf7eb125928ddd25314574bea6add4a769`。
- 唯一变量：把 V1“首次回踩成功、失败或超时即消费整个突破周期”改成“最新合格 EMA576 突破方向互斥生效；该方向内允许价格重新离开 EMA144 后再次回踩”。
- 因果假设：BTC 7 月 2 日和 7 月 12 日可以属于同一个多头活动方向；ALGO/MERL 的旧空头资格在更新的多头突破生效后必须被清除，不能与多头方向并存。
- 预计影响：相对 V1 的 2,066 个候选预计增加到 2,000～60,000，覆盖不少于 8 个币种、6 个 UTC 月份和 100 个一小时事件；多空任一不得少于 10。
- 停止条件：三张正样本任一未命中；ALGO/MERL 任一错误空单时间仍触发；候选少于 2,000 或超过 60,000；或定义必须读取未来 K、成交和收益字段。
- 保持不变：144 根 EMA144/576 长期关系、80% 价格侧、两根收盘确认首次 EMA576 突破、EMA144 上下 `0.30 ATR14` 区域、触碰当根必须收回 EMA144、冻结币池、窗口、数据缺失政策和 Research-only 边界。

## 冻结生命周期

1. 多空资格仍分别由连续 144 根长期状态和至少 80% 价格侧形成；资格本身不产生信号。
2. 某方向完成两收盘 EMA576 首次突破时，它成为唯一活动方向；旧活动方向和对侧尚未消费的历史资格立即清空。
3. 活动方向不因一次 EMA144 回踩成功或失败而结束，也不使用 V1 的 576 根固定期限；只在输入缺口或更新的合格反向 EMA576 突破完成时失效。
4. 多头只有在一根完成 K 收盘高于 `EMA144 + 0.30 ATR14` 后才武装下一次回踩；空头镜像低于 `EMA144 - 0.30 ATR14`。该边界复用现有回踩区，不新增第二个距离阈值。
5. 武装后的第一次区域触碰消耗本次回踩机会：仍按 V1 要求多头最低价不低于 `EMA144 - 0.30 ATR14` 且收盘不低于 EMA144，空头完全镜像。
6. 触碰成功形成候选；触碰失败不形成候选，但活动方向保留，只有重新离开 EMA144 区域后才能再次武装，防止区间内每根 K 重复报信号。
7. 每条候选只保存 setup、活动突破、再次武装和回踩确认时已可见字段；L1 不计算下一根成交或任何 outcome。

## 五张图与 L1 门禁

- 正样本：NMR 2026-07-01 00:00～12:00、BTC 2026-07-02 06:00～12:00、BTC 2026-07-12 12:00～18:00，均为北京时间多头，必须 3/3。
- 反样本：ALGO 2026-07-19 17:30、MERL 2026-07-19 18:45 的空头必须 0/2。
- 全部五张图通过，且候选、方向、币种、月份和事件覆盖通过，才可标记 `coverage_pass_ready_for_l2_prereg`。
- 任一失败立即停止 V2，不读取 outcome、不创建 Pine、不注册 Paper/ReadOnly/Live、不修改生产 preset。
