# EMA576 首次突破后 EMA144 回踩守稳 15m V1：L1 预注册

## 当前等级与认知边界

- 当前等级：`L1 快速研究`。本预注册先于新候选扫描和任何成交后结果读取冻结。
- 已知的已知：用户确认多头顺序必须是“价格长期位于 EMA576 下方 → 首次有效上破 EMA576 → 回落至 EMA144 附近且未有效跌破 → 做多”，空头完全镜像；入场时不要求 EMA144 已经穿越 EMA576。
- 已知的未知：该定义在冻结 15m 本地成员中的无标签覆盖率、方向分布、月份分布和事件集中度尚未知。
- 未知的已知：旧 V6/V12 使用永久资格和可重复重扩张，能够在没有新鲜 EMA576 首次突破时产生候选；这些旧结果只能作为反例来源，不能作为新候选收益基线。
- 未知的未知：当前存活 Top60 存在幸存者偏差；L1 不补数据、不评估成本、成交、退出或收益，也不证明全市场适用性。

## 单变量假设卡片

- 候选键：`market_momentum_ema576_first_breakout_ema144_pullback_hold_15m_v1`。
- 规则版本：`l1_regime144_price80_break2_retest030_wait576_first_only_v1`。
- 唯一变量：把旧家族的“历史资格永久锁存并可重复重扩张回踩”替换为一个有限、一次性的“长期慢线压制—首次 EMA576 有效突破—首次 EMA144 回踩守稳”事件；本轮不搜索该事件内部阈值。
- 因果定义：只使用当前及此前已经完成、生产可见的 15m K 线和 EMA144、EMA576、ATR14；候选仅在回踩确认 K 收盘后成立，最早只能在下一根开盘执行。
- 基线身份：旧 `market_momentum_ema144_576_transition_first_retest_15m_v1` 仅提供数据窗口和无标签覆盖参照，不继承其 `0.75 ATR` 离开、96 根等待或回踩阈值。
- 目标币种/周期/窗口：OKX 永续 15m，本地冻结 Top60 抽样；评价窗口 `2025-07-01 00:00 UTC` 至 `2026-07-19 14:15 UTC`。
- 预计影响：预计形成 `200～3000` 条无标签候选，覆盖不少于 8 个币种、6 个 UTC 月份和 15 个一小时事件；不允许只集中于单一方向。
- 停止条件：三张正样本任一未命中；ALGO/MERL 两个错误空单时间任一仍触发；总候选少于 30 或超过 5000；多空任一少于 10；覆盖少于 8 个币种、6 个月或 15 个有效事件；或实现必须读取信号后的价格才能成立。
- 保持不变：冻结币种池、行情窗口、EMA/ATR 计算口径、数据缺失跳过政策和 Research-only 运行边界；L1 不引入成交、止损、目标、持仓冲突、成本或资金路径。

## 冻结形态定义

1. 多头长期状态使用连续 144 根已完成 K：每根均满足 `EMA144 < EMA576`，且其中至少 80% 的收盘价低于 EMA576；空头使用完全镜像条件。
2. 长期状态成立后，只认第一次有效 EMA576 转换：多头要求前一状态仍在慢线下方，随后连续两根完成 K 收盘在 EMA576 上方；空头镜像。
3. 突破确认后不再要求 EMA144 与 EMA576 已经交叉，也不要求额外 `0.75 ATR` 离开；不能用后续涨跌补充突破资格。
4. 从突破确认后的下一根 K 开始，最多等待 576 根完成 K。首次进入 EMA144 上下 `0.30 ATR14` 区域即消耗事件。
5. 多头有效回踩要求该 K 的最低价位于 `EMA144 ± 0.30 ATR14` 内，且收盘价不低于 EMA144；空头要求最高价位于相同镜像区域，且收盘价不高于 EMA144。
6. 首次触碰若超出允许刺穿范围或收盘失守，整次突破事件失效，不等待第二次回踩；576 根内没有触碰同样失效。
7. 无论成功、失败或超时，下一次同方向机会都必须从消费后的新 K 重新积累完整 144 根长期状态，禁止复用旧资格。
8. L1 账本只保存 setup、突破、信号时间及当时可见特征；`outcome_evaluation_performed=false` 必须成立。

## 预注册正反样本

| 类型 | 标的 | 方向 | 冻结窗口（北京时间） | 门禁 |
|---|---|---|---|---|
| 正样本 | NMR-USDT-SWAP | 多 | 2026-07-01 00:00～12:00 | 至少一条候选 |
| 正样本 | BTC-USDT-SWAP | 多 | 2026-07-02 06:00～12:00 | 至少一条候选 |
| 正样本 | BTC-USDT-SWAP | 多 | 2026-07-12 12:00～18:00 | 至少一条候选 |
| 反样本 | ALGO-USDT-SWAP | 空 | 2026-07-19 17:30 单根 K | 零候选 |
| 反样本 | MERL-USDT-SWAP | 空 | 2026-07-19 18:45 单根 K | 零候选 |

全部门禁通过也只能标记为 `coverage_pass_ready_for_l2_prereg`。任一门禁失败即停留 L1，禁止读取 outcome、创建 Pine、注册 Paper/ReadOnly/Live 或修改生产 preset。

> 审计更正：首次机器扫描前的表格把 BTC 7 月 2 日截图窗口误录为 12:00～18:00；截图箭头及既有目标审计均对应 06:00～12:00，实际候选时间为 08:30。这里只修正目标窗口转录，不改变策略规则、阈值或候选生成。
