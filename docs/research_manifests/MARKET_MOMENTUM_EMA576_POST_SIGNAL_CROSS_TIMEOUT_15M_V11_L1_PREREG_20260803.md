# EMA576 首信号后均线交叉确认超时 15m V11：L1 预注册

## 单变量假设卡片

- 当前等级：`L1 快速研究`；本预注册先于 V11 全样本扫描和任何成交后字段读取冻结。
- 候选键：`market_momentum_ema576_post_signal_cross_timeout_15m_v11`。
- 规则版本：`l1_v10_first_signal_requires_ema144_576_cross_within_96_bars_v11`。
- 基线版本：V10 `market_momentum_ema576_pre_cross_breakout_episode_15m_v10`，冻结 L1 报告 SHA-256 为 `1d780c35eef54b71490073323b722ce09ad1b38416e1062130125b1401cc05be`。
- 唯一变量：使用同一旧长期资格链产生第一笔交叉前交易信号后，启动一次固定 `96` 根完整 15m K（24 小时）的方向性交叉确认计时；多头期限内必须出现 `EMA144 > EMA576`，空头必须出现 `EMA144 < EMA576`。到期 K 仍未满足时，清空该方向当前 episode 与旧资格。
- 因果定义：首信号完成 K 记为 `t0`；`t0 < t <= t0+24h` 的每根完成 K 均可完成确认。到期 K 先判断交叉，再判断失效。计时属于旧资格链，同方向后续突破、episode 替换或信号不得重置。超时后必须重新积累 144 根长期资格，不能立即复用旧资格。
- 预计影响：删除 V10 中由未及时完成 EMA144/576 方向交叉的陈旧资格链产生的后续候选，预计影响 V10 候选的 `1%～30%`；至少应影响 3 个候选和 3 个资格链，同时仍覆盖不少于 8 个币种、6 个 UTC 月份和 100 个一小时事件。
- 停止条件：ALGO 目标日期链不符合；零影响或只影响 1～2 个候选/资格链；六张既有正反样本被破坏；候选少于 5,000 或超过 80,000；多空任一少于 10；或实现必须读取成交、退出、MFE、MAE、R、胜负、成本或 PnL。
- 保持不变：V10 的长期资格、80% 价格侧、价格两收盘 EMA576 突破且必须领先 EMA144/576 交叉、独立多空 episode、0.30 ATR14 回踩与守稳、影线消费、武装收盘失败、交叉后反向 EMA576 中断、候选成交边界、币种池、窗口和 Research-only 隔离。

## ALGO 目标日期链

1. 北京时间 `2026-07-15 06:00` 的多头首信号必须保留，并以该完成 K 启动唯一计时。
2. 北京时间 `2026-07-16 06:00` 为第 96 根完成 K；若此时仍未满足 `EMA144 > EMA576`，必须记录 `post_signal_24h_without_ema144_576_cross` 失效并清空旧多头资格。
3. 北京时间 `2026-07-16 07:15` 的价格再突破不得建立沿用旧资格的新 episode，后续也不得产生属于该陈旧突破的候选。
4. 后续候选或同方向价格突破不能延长上述截止时间。

## L1 门禁

- 只扫描完成 K、EMA144、EMA576、episode/资格状态和候选时间；不读取任何成交后结果标签。
- 输出首信号计时启动数、期限内确认数、超时数、相对 V10 删除候选数以及按方向/币种/月/事件覆盖。
- ALGO 日期链、六张既有目标图、覆盖和分散性门禁全部通过，才可标记 `coverage_pass_ready_for_l2_prereg`。
- 任一门禁失败立即停止，不创建 L2 结果、不创建 Pine、不注册 Paper、ReadOnly、Live 或生产策略。

## L2 晋级预注册（L1 通过后、读取 outcome 前冻结）

- L1 机器文件：`target/research/market_momentum_ema576_post_signal_cross_timeout_15m_v11_l1_20260803.json`。
- L1 SHA-256：`ebc2b886d1a64e6900e81900bc59a5b0a93c245437d8537642dbfe4f9b64e1e6`。
- 行情指纹：`67516c927ce30323f38f34e6c87fd7bac7720bae8084209cc44b86cce6efe997`；60 个返回成员中 44 个本地完整，16 个跳过，不补造 K 线。
- L1 晋级证据：17,041 个候选、3,108 个一小时事件、44 个币种、13 个 UTC 月份；多头 9,042、空头 7,999；相对 V10 删除 9,615 个且没有新增候选；2,533 个首信号计时中 982 个期限内确认、1,543 个超时；ALGO 与六张既有目标图全部通过。
- L2 规则版本：`l2_v11_timeout96_next_open_sl04_r052_hold24h_cost8bps_symbol_lock_v1`。
- 唯一变量仍是 V11 的 96 根首信号后 EMA144/576 方向性交叉期限；成交和退出不作为本轮优化变量。
- 冻结成交：信号完成后的下一根连续 15m K 开盘成交；缺 K、时间不连续或价格无效即阻塞，不补偿入场。
- 冻结风险：实际入场价 `4%` 初始止损、固定 `0.52R` 目标、最长持仓 `24h`；不启用保本、移动止损、分批、反手或第二目标；同棒止损优先。
- 冻结成本：每边 `8 bps`（手续费 5 bps + 等价滑点 3 bps），不建模资金费；同币种最多一笔持仓，持仓及退出 K 内信号阻塞。
- 事件归并：按 `方向 + 与同方向上一触发相距不超过 60 分钟` 跨币种归并，与 V10 相同。
- L2 通过门禁：成本前与成本后总 EV、PF 均为正；成本后多空分别为正；完整交易不少于 30、多空各不少于 10、覆盖不少于 8 个币种、6 个月和 15 个事件；移除头两笔与头部事件后仍为正；单币和单事件正收益占比均不超过 35%；L1 SHA、行情、账本、成交与风控合同全部一致。
- 任一门禁失败即停止在 Research-only；即使相对 V10 改善，也不能用“少亏”代替绝对成本后正期望，不进入 L3、不创建 Pine、不接入运行态。
