# 任务计划：PA Quant Tree v1 基础设施

## 2026-07-28：架构迁移文档防腐收敛

- [x] 固定业务边界：不存在 Core 自营账户或系统执行请求；所有 `ExecutionRequest` 只能由 Web 的用户商业授权创建。
- [x] 将平台固定 API Key 定义为 `MarketDataAccessCredential`，只用于 K 线和公共市场数据读取；与用户 `credential_reference`、私有流和 mutation capability 完全隔离。
- [x] 修订目标 Contract、Execution Claim/Outcome 交接、Credential capability、Control/Strategy owner、Research subject、公共/私有配额、运行状态与 ResearchBar 风控闭环。
- [x] 将迁移治理改为父计划加单 Owner 子 Manifest，补齐依赖、状态/晋级/Cutover 分离、证据 hash 与 Research 历史记录边界。
- [x] 二次未来迁移文档审计后新增机器可读 Migration Program registry，固定跨仓库 Claim/Renew/Release 命令与回执方向、B0 不可变测试 Evidence、Manifest 真值表和历史记录不可作为后续依赖。
- [x] 将 Research/Paper 的纯 `ExecutionPlanningValue` 与 live-only 持久 `ExecutionPlan` 分离；补齐 ActivationEligibility、MarketDecisionReadiness、PortfolioEvaluationBatch、Account recovery fence、SafetyObligation 与外部仓位禁止自动 mutation 的目标合同。
- [x] 为 `rust-quant-architecture` 增加“未来目标/迁移文档审计模式”与 Gate matrix 规则；本轮验证仅覆盖文档一致性、TOML 解析、链接/术语与差异格式，不构成代码或生产验证。
- [ ] 后续代码迁移必须分别创建新的 current-migration Owner 子 Manifest；本轮只修订目标文档与架构技能，不移动目录、不修改运行链路、不触发交易动作。

## 2026-07-21：Core 六角色运行拓扑 Phase 1

- [x] 将默认长期容器收敛为 `control-api / market-worker / signal-worker / account-worker / execution-worker / reconciliation-worker`，保持同仓库、同镜像、同数据库，不拆微服务
- [x] 为六个角色建立固定 binary；关键 lane 意外退出时结束进程，避免容器存活但核心循环静默死亡
- [x] Market 合并 symbol sync、radar、scanner 与最多 2 天 bounded repair；移除 Web execution secret 并关闭 signal dispatch；paper、观察、schema 和历史 backfill 改为显式 Job/profile
- [x] Signal 合并 Vegas 4H 运行入口，按策略类型隔离 symbol scope、按启动 config ID 精确执行，并以 `strategy_key@preset` 冻结多份 handoff 配置
- [x] Execution、Account、Reconciliation 使用代码固定互斥 lane，不再由 legacy mode 环境变量决定进程职责
- [x] 同步 runtime image、Compose、发布、回滚、生产只读验证和 deploy contract；首次切换增加显式确认与 legacy topology snapshot，清退单次/scheduler live-handoff，并在 CI 部署后强制执行只读验收
- [x] 六个 binary 编译、CLI app 层 714/714、WebSocket config scope 5/5、Execution 环境/lane 10/10、既有生产安全合同 7/7 和新增六角色合同 5/5 通过
- [x] 将 475/474 行 promote/rollback 重构为 5 行薄入口，新增版本化六角色清单与单一共享部署实现，移除 CI `DEPLOY_SERVICES` 伪配置，同时保留全部安全检查、cutover 快照和 rollback 行为
- [ ] 生产切换前核对两份 handoff `strategy_configs` 的 key/preset/slug、目标环境、网络和数据库 readiness，并先做无 mutation shadow/canary
- [ ] 将 account confirmation 内的保护单 mutation 迁回 Execution owner，补真正 AccountProjection worker；将 reconciliation 从 report replay 扩展为完整差异检测和恢复闭环
- [ ] 为 Market/Signal/Account/Execution/Reconciliation 增加依赖、checkpoint、lease 和数据新鲜度 readiness，收回 radar 内部 detached task
- [ ] 六角色生产验收并结束约定回滚窗口后，删除共享部署实现中的 cutover token、legacy topology restore 和 retired/obsolete 兼容清单

下一步：当前只完成本地 Phase 1，不提交、不部署、不触发实盘。生产切换必须显式设置一次性 cutover token，并在发布后核对六个容器 revision、重启数、关键 lane 日志、checkpoint 与 Web/Core read-only 链路；任何一项不满足就使用保存的 legacy topology rollback。

## 2026-07-21：持久化顺序与外部 Mutation 协议修订

- [x] 将 ADR-0006 固定为订单、撤单和保护单持久化/提交顺序的唯一权威
- [x] 固定 RiskDecision owner、risk evaluation generation、单 parent Intent/plan 绑定、跨 owner 失败窗口和不可跨 owner 回滚边界
- [x] 固定 Execution 原子事务内容：AccountOpeningSlot、审批引用、OrderIntent、完整 ExecutionPlan/ProtectionPlan、SubmitPending、幂等、Outbox 与审计
- [x] 明确 commit、Outbox、aggregate version/send claim、attempt ledger、MutationPermit、Fenced Gateway、结果事务和 delivery ack 的先后关系
- [x] 删除 Submitted/Acknowledged 重复状态，补齐 cancel/recovery revoke 与 Gateway permit consume 互斥、Submit/Cancel/Protect mutation generation rollover、Unknown 禁止同 kind 直接重投、DefinitivelyAbsent 按原 kind 恢复新 generation Outbox、旧 delivery no-op、主动终态和 Outbox 重放规则
- [x] 明确 transient blocker 的 durable retry，以及 User Stream 先订阅缓冲再合并 signed snapshot/query watermark 的启动闭合协议
- [x] 将 Golden Slice 拆为 Dry-run、PaperEvent、RecoveryHarness，禁止模拟链路写生产交易事实或复用生产提交 Outbox
- [x] 同步目标架构、运行规范、CRUD/Port 规则、依赖规则、AI 护栏、迁移计划和 ADR 索引
- [x] 完成全局旧顺序扫描、协议交叉复核和 Markdown 差异检查
- [ ] 实现 Execution schema/Port/RecoveryHarness 前，先形成可验证的最小切片设计
- [ ] 如需同账户并发独立开仓，另立 Risk Reservation ADR；当前由持久 opening slot 保持单账户单未安全收敛开仓意图

下一步：只实现 disposable RecoveryHarness 和 owner-scoped 原子写测试，不接生产数据库、真实凭证或 live Exchange Adapter；通过双 staging slot 竞争、permit consume/revoke 竞态、stale Dispatcher DefinitelyNotSent、崩溃窗口、Unknown 缺席证明与新 Outbox、durable blocker retry 和 startup stream-gap 测试后，再讨论生产迁移。

## 2026-07-20：交易系统目标架构文档收敛

- [x] 将目标物理目录收敛为 `domains / quant / contracts / adapters / platform` 五类，并将 Operations 改为边界更窄的 Reconciliation
- [x] 明确 Model、Policy、Use Case、Port、Adapter、App 与 Wire Contract 的代码位置和依赖方向
- [x] 明确数据库 Create/Read/Update/Delete、Query、SQLx transaction、Outbox 和 migration 的唯一放置规则
- [x] 明确 Web ExecutionRequest 与 Core Order/Fill/Protection 事实的 owner 迁移边界
- [x] 补齐部分成交、保护数量、撤单竞态、Unknown、最大未保护窗口与恢复协议
- [x] 将迁移改为 Golden Vertical Slice + shadow/parity + CI ratchet，而不是横向大搬迁
- [x] 补充 AI 编码护栏、Owner-scoped 持久化 ADR 和开源参考取舍
- [x] 用现有 Vegas/回测主链反向验证目标目录，修正 signal-worker 与账户级 Portfolio/Risk 的运行边界
- [x] 新增 Research Domain，明确 Experiment、BacktestRun、DatasetManifest、SimulationLedger 与 ResearchEvidence 的唯一 owner
- [x] 以 ADR-0009 取代 ADR-0008：`quant/backtest` 收敛为纯确定性内核，业务编排回到 Research Use Case
- [x] 将回测验证拆为 ResearchBar、PaperEvent、RecoveryHarness，避免普通策略回测虚假复刻整套生产 OMS
- [x] 明确 Vegas Evaluation State 的 run/deployment scope、多币种 decision-time barrier、SimulationLedger 与生产 AccountProjection 的隔离
- [x] 明确 StrategyArtifact 与 ResearchEvidence 分权，以及对象存储 + PostgreSQL 的 ResearchEvidence 原子可见发布协议
- [x] 重写 Vegas 迁移实战，补齐逐文件代码分配、Research 数据访问、迁移切片与分层 parity 验收矩阵
- [x] 将目标架构封装为项目级 `rust-quant-architecture` Skill，并完成官方结构校验和 umbrella 可发现安装

下一步：文档不等于实现；先冻结 Vegas Slice 0 的事件级基线、随机 symbol 输入顺序基线和证据指纹，再建立最小 Research owner 骨架与只拦新增违规的只读 `arch-check`；通过后迁移无副作用 Vegas Evaluator 和 ResearchBar 单资产切片，不能一开始就搬整套 OMS。

## 目标

在不改变 Vegas 现有交易行为的前提下，交付无 AI 运行时的 PA 量化树纵向切片，并建立可复现的离线 Challenger 评估与 Paper 生命周期基础。

## 阶段

- [x] 阶段 1：确认仓库边界、已有能力和脏工作树
- [x] 阶段 2：实现确定性特征、候选、DSL、Manifest 与独立策略
- [x] 阶段 3：实现 Vegas Shadow Meta-filter、研究评估和生命周期
- [x] 阶段 4：运行测试、回归、行数检查和注释审查
- [x] 阶段 5：同步设计文档和交付结论
- [x] 阶段 6：收紧策略标识，并接入研究样本与共享组合回放
- [x] 阶段 7：接入 BTC/ETH/BCH 15m 只读历史研究与成本结算
- [x] 阶段 8：使用现有 OKX backfill 补齐四市场 365 天 15m/1h 历史
- [x] 阶段 9：用四市场全年数据重新执行 15m 训练期研究
- [x] 阶段 10：独立执行四市场全年 1h 训练期研究
- [x] 阶段 11A：修正逻辑回归成本后期望阈值并独立重跑 15m/1h
- [x] 阶段 11B：补齐单一来源全年资金费率代理并按持仓小时保守计入成本
- [x] 阶段 12：预注册并验证趋势入场质量 Feature Registry v2
- [x] 阶段 13：预注册并实现独立的趋势跟随确认候选与新策略版本合同
- [x] 阶段 14A：记录模型竞赛 OOF 决策并生成入选模型 OOF 路径统计
- [x] 阶段 14B：完成 v1/v5 的 A/B/C 同 setup 配对反事实
- [x] 阶段 14C：建立统一实验账本与 Holm 多重检验校正
- [x] 阶段 14D：按预注册门禁归档 PA 独立策略，取消继续开发 PA Meta-filter
- [x] 阶段 15：修复工程构建基线，冻结新独立策略默认注册，并把后续资源切回 Market Velocity/Vegas
- [x] 阶段 16：只读核对 Market Velocity/Vegas 生产 revision、角色、调度、数据、readiness、task/lease、保护单和结果回写
- [x] 阶段 17：仅执行一次 Vegas 历史 active 仓位与保护单 signed read-only 对账；在交易所探针前按失败门禁停止，mutation count=0
- [x] 阶段 18：从旧 `quant_core_postgres` 卷迁移 Market Velocity 回放数据到独立本地数据库，修复权益回放时间对齐基线并精确复跑 Long / Breakdown Short
- [ ] 阶段 19：在补齐显式滑点合同后，只执行 Breakdown Short v6 的固定参数前向稳健性验证；不得同时开启其他策略实验
- [x] 阶段 20：修复实盘执行 worker 任务类型/确认/回放角色、Vegas Web 状态所有权、Market Velocity 新鲜度与调度不变量、空轮询审计写放大、rank snapshot 批量写入和长期连接复用（代码与本地测试完成，尚未提交或部署）
- [x] 阶段 21：按新版回测规则完成 Market Momentum Opposite Move v4 成交量优势门禁、参数邻域、多窗口/有效事件审计与本地明细持久化；研究门禁失败，不晋级 Paper/Live
- [x] 阶段 22：修正 Vegas 跨币种共享账户报告的实际配置风险、初始 1R、日频 Sharpe、Recovery 与保守盯市回撤，并冻结 v26 历史币池/事件聚类/前向 OOS 评估清单
- [ ] 阶段 23：按冻结清单实现历史币池成员表和事件聚类报告；不得在当前已查看区间继续调 v26 参数
- [x] 阶段 24：建立 v27 统一 0.75% 风险 / 容量 12 的 disabled 研究候选，完成真实 100 币回测、事件/月份/状态/集中度/成本邻域审计
- [x] 阶段 25：按用户确认的 live-only 边界跳过退市币，补齐当前 live 加密币且上市满 150 天的 OKX USDT 永续 4H 数据，并完成月度 Top100 重建与扩展回归
- [x] 阶段 26：冻结初始止损/R 合同，补齐滑点、资金费、严格 OOS 与固定参数 walk-forward；v27 以 70% 风险缩放把保守回撤压到 9.48%，聚合门槛通过但 OOS/滚动稳定性仍阻断 Live
- [ ] 阶段 27：仅以版本化 Vegas v2 Paper/ReadOnly 累积前向证据；未显式创建匹配的 production_default 前，禁止生成默认实盘任务
- [ ] 阶段 28：冻结 v27，不再用已查看区间调参；从冻结点累计 6 个完整月、至少 50 笔组合交易和 30 个有效事件的前向 OOS
- [x] 阶段 29：完成 ETH Vegas 4H v1/v2 同数据、同引擎、同仓位和同成本报告；确认 v2 同口径优于 v1，但仍只允许 Paper/ReadOnly
- [x] 阶段 30：按主项目联合门槛完成 Market Momentum Opposite Move v11/v12 同身份迭代、有效事件聚类和停止评估；失败版本不入库、不打开新 OOS、不进入 Paper/Live
- [x] 阶段 31：完成 Vegas Universal 4H 亏损逆向分析，并依次验证长下影线拦截、1.5R 保本、2R 锁 1R 三个 disabled 候选；三者均因跨期不稳定或 EV/PF 恶化被拒绝
- [x] 阶段 32：冻结并验证低 ATR 扫高确认、动量回测、压缩突破与结构失效止损独立版本；全部按标准成本和时间稳定性门禁决策
- [x] 阶段 33：完成 v41/v42 组合与弱扩张亏损门禁；确认当前频率—质量前沿仍无法同时满足 PF、EV、回撤、Sharpe、Recovery 和 8 笔/月目标
- [x] 阶段 34：以相同 245 个入场验证 v43 的 1ATR 最小失效止损；PF/EV 与弱 walk-forward 均恶化，否定“止损普遍过紧”假设
- [x] 阶段 35：预注册并验证 v44 弱量压缩突破固定一根确认；确认组 EV/PF 为负且配对收益恶化，按标准门禁拒绝
- [x] 阶段 36：预注册并验证 v45 以 1.5ATR 价格位移替代缺失量能；新增 2 笔均亏损且 PF 失败，拒绝
- [x] 阶段 37：预注册并验证 v46 放量突破压力后下一根收回区间的失败突破空头；validation 转负，按门禁拒绝
- [x] 阶段 38：依次验证 v47 动态止盈消融、v48 BOS+FVG+MACD 追空、v49 FVG 收复+MACD 多头、v50 fresh bullish CHoCH 门禁；全部未通过联合目标
- [x] 阶段 39：按用户指定方向分别验证 MACD 背离 + fresh CHoCH 和 MACD 趋势复位 + fresh BOS，不扫描参数
- [x] 阶段 40：修复研究结构快照旁路污染与 payload 超长未落库，用独立运行时快照复跑 v51b/v52c 全 100 币池
- [ ] 阶段 41：冻结 v51b 仅做 forward shadow，归档 v52c；下一新 setup 只允许使用“扫流动性 -> 转向 -> 首次回测接受/拒绝”完整序列并等待未见数据，不从当前亏损桶扫阈值
- [x] 阶段 42：修正 15m BOS/CHoCH/FVG 因果语义，完成 12 个月 current-live-only Top60 扩窗，并分别证伪独立 CHoCH+FVG 回补与 funding 拥挤空头扫低收复家族
- [x] 阶段 43：补齐多币种历史 OI 与主动成交，预注册并一次性验证去杠杆 V1/V2；两者均因费前后期望、PF、时间稳定性和集中度失败而淘汰
- [x] 阶段 44：完成 v53b—v57 首回踩、固定 R 退出和两个独立多头反转分支；确认 v54 最接近门槛但仍未通过 PF 与滚动稳定性，其他版本按预注册门禁拒绝
- [ ] 阶段 45：冻结 v54 仅累计新的 forward shadow；没有未见数据或新独立市场机制前，不再从当前历史挑 MACD、RSI、EMA、影线、币种或月份条件
- [x] 阶段 46：在 2025-07～2026-05 未见窗验证回踩接受 V3，并在 2022-07～2023-06 未见窗验证反弹失败空头 V4；两者均未达到职业联合目标
- [ ] 阶段 47：停止 15m 极端下跌结构家族的历史调参；只允许等待新增 forward OOS，或先预注册与 OHLC 结构、funding、OI/taker 翻转都正交的新市场机制
- [x] 阶段 48：完成 v58 零机会、v59 扫高跌破与 v60 MACD 零轴位置门禁；v60 聚合数值过线但样本内、走步、双倍成本和增量集中度仍阻断晋级
- [x] 阶段 49：预登记并验证 V68 入场预计双倍成本/R 门禁；确认压力成本质量改善但频率、IS/OOS 与走步强度失败，不扫描成本阈值
- [x] 阶段 50：预登记并验证 V69 既有首回踩家族的 `2.0x` 普通放量边界；仅新增 3 笔正收益交易，仍不足以解决组合频率与时间稳健性
- [x] 阶段 51：实现并验证已预登记的 V70 EMA144/169 隧道顺势回踩确认；新增 83 笔但多空增量均为负，按增量门禁早停并保持 disabled
- [x] 阶段 52：预登记并验证 V71 固定历史成交量分布价值区突破—回踩确认；97 笔在总样本、IS/OOS 和多空方向均负，按增量门禁早停
- [x] 阶段 53：预登记并验证 V72 固定价值区即时放量突破；多头显著负、空头跨期为正但质量远低于目标，整体与 OOS 门禁失败
- [x] 阶段 54：预登记并验证 V73 空头价值区跌破 + 标准 ADX14/DMI；IS 转负而 OOS 改善，确认时间漂移并拒绝
- [x] 阶段 55：预登记并验证 V74 原始 V72 空头固定 2R；集中度下降但 OOS 转负、EV/PF 远低于目标
- [x] 阶段 56：预登记并验证 V75 上方价值区失败拍卖均值回归空头；IS 转负、总 PF 仅 1.229 且 Top3 高度集中，按门禁拒绝
- [x] 阶段 57：验证 BTC 残差动量、单腿残差回归、杠杆资金流双向/空头；全部因标准成本、时间稳定性、频率或集中度失败而淘汰
- [x] 阶段 58：用冻结候选集完成静态订单簿深度与对侧深度衰减两组正交因子面板；动态空头存在弱毛优势，但 Discovery、命中率和方向稳定性不通过
- [x] 阶段 59：冻结并一次性验证 BTC Beta 对冲双腿残差回归；530 组交易费前即为负，标准成本 EV/PF `-0.2327R/0.6272`，永久淘汰
- [x] 阶段 60：阶段 59 交易级早停失败，按预登记规则跳过统一资金组合权益，不把负交易优势包装成仓位优化
- [x] 阶段 61：OKX—Binance 7 日基差 z-score 面板方向稳定且 4h 命中 80.39%，但毛收益仅 7.24bps、远低于 32bps 成本，禁止策略化
- [x] 阶段 62：50bps 首次越界仅 501 个原始信号、107 个有效事件，4h 毛收益 18.09bps 且前后半年 21/480 严重漂移，跨交易所基差分支停止
- [x] 阶段 63：premium 折价修复仅 237 个确认、199 个有效事件，4h 毛收益 9.38bps/命中 49.37%，Validation 转负，永久淘汰
- [x] 阶段 64A：冻结等名义横截面动量价差面板；1,092 组频率充足但前后半年方向翻转、总体 8h/24h 均为负，永久淘汰
- [x] 阶段 65A：验证官方 Binance 横截面 funding carry V1/V2；V1 覆盖合同不可实现，V2 仅 11 组且价格逆风吞掉 carry，永久淘汰
- [x] 阶段 66A：验证头部交易者金额/账户确信度与头部持仓/全市场人群分歧；两者样本充足但总体为负或跨期不稳，按预注册规则结束 positioning 家族
- [ ] 阶段 67A：停止用同一已查看历史反转方向、排列字段或降低成本门槛；下一候选必须依赖新的完整 point-in-time 外生数据或冻结后的 forward 样本
- [x] 阶段 61：预登记并验证 V76 Donchian20 + 2x量能趋势启动家族；1,405 笔机会充足但 OOS 多空均负，按门禁拒绝
- [ ] 阶段 62：停止已查看历史上的 OHLC 指标拼接；保持 V69/V65 frozen shadow，只用冻结后新完成的 4H 交易更新证据
- [x] 阶段 63：预登记并验证 V77 Donchian 突破后一棒接受；确认减少交易但 OOS EV/PF 仅 0.004R/1.006，多头恶化且空头质量不足，拒绝
- [x] 阶段 64：对 V77 亏损单做因果 MACD 分层；顺势柱体增强无法修复多头，空头表面强组缺少 OOS 覆盖，不生成后验 V78 补丁
- [x] 阶段 65：预登记 BTC 日级 OI 扩张空头风险门禁并刷新 7 月映射；关系发生时间翻转且误拦 XRP 大盈利空单，拒绝、不生成 V78

## 关键问题

1. 如何保证候选判断只读取信号时点及以前的已确认 K 线？
2. 如何让 Vegas Meta-filter 保留原始方向、入场和风险计划？
3. 如何冻结 Manifest，并让相同输入得到完全相同结果？
4. 如何让自动迭代只生成 Challenger，绝不在线修改 Champion？

## 已确定决策

- 运行时不依赖 AI、网络或自然语言结构判断。
- v1 使用下一根 K 线开盘执行 PA 独立候选。
- Vegas Meta-filter 仅输出 keep/reject 与审计轨迹，不修改原始信号。
- 训练与统计组件使用纯 Rust；不在策略迭代中使用 Python。
- 本轮不触发真实下单、部署、生产配置切换或 Live promote。
- 暂停浅回踩 v6；先修复入选模型 OOF bootstrap、配对反事实和多重检验闭环。
- `pa-v7-abc-counterfactual-once` 是 PA 独立策略最后一次训练窗口诊断；不重复运行、不调门槛、不追加新 PA 候选。
- v7 任一硬门禁失败即归档 PA 独立策略；PA 不再接入 Vegas Meta-filter，后续研究资源回到已有生产证据的 Market Velocity/Vegas。
- Market Velocity/Vegas 的下一步不是新参数或新机制；`prod-vegas-open-leg-readonly-reconciliation-20260715` 已因现有入口无法同时满足精确凭证解析和零写回而在交换所探针前失败，禁止重跑或旁路绕过。
- 在当前有效 combo、signed preflight、运行 slug/release、K 线 universe 和仓位证据同时闭环前，不把容器存活或历史成交解释为当前 live-ready。
- Market Velocity Long 当前生产 preset 在迁移样本上 784 个信号候选全部被 FVG 执行门禁阻塞，保持非 Live 晋级状态，不做阈值搜索。
- Breakdown Short v6 虽然费后权益为正且最大单 symbol 回撤低于 15%，但仅 43 笔、完整胜率 58.14%、去 Top3 后胜率 57.14%，严格门禁失败；唯一后续实验是固定参数的新增样本前向验证。
- 用户于 2026-07-19 显式要求继续当前 Market Momentum Opposite Move 优化；本轮按同一策略键新增 v4，不新增独立策略身份。v4 修复目标缩量误入场，但联合指标、频率和集中度门禁失败，停止晋级。
- Vegas v26 成本压力共享账户结果虽为 100U → 401.69U，但净 EV 0.557R、净 PF 2.049、保守盯市回撤 28.77%、组合月频约 4.89，仍不晋级；Recovery 4.018 与日频 Sharpe 1.545 仅刚过线，不能抵消其他硬门槛失败。
- v26 的 2024-01-19 至 2026-07-16 区间已经被查看并锁定为研究样本；后续先实现历史币池和有效事件聚类，再从 2026-07-16 之后累计真正前向 OOS。候选保持 disabled，不部署、不切换生产。
- v27 完整继承 v26 信号，只把所有币种风险统一为 0.75%、共享容量改为 12；真实回测 `10280..10379` 在基础压力成本下为 100U → 210.33U、PF 2.254、EV 0.693R、Sharpe 1.647、Recovery 4.214、保守回撤 13.12%。当前按 live-only 口径补齐上市满 150 天币种并重建月度 Top100；该口径显式排除退市币并接受幸存者偏差，滚动窗口也仍有 2025 年中弱段，因此保持 disabled。
- live-only 数据审计已完成：262 个当前 live 加密币 USDT 永续中，246 个上市满 150 天且 246/246 有 4H 表；退市币与非币类合约直接跳过。v28 全量重新分档和 v29 保留 v27 档位后扩展新币均未超过 v27；不做 Top100 虽增至 249 笔和 +119.02%，但 PF 1.700、EV 0.445R、Sharpe 1.379、回撤 16.75% 均不满足联合目标。v27 仍是冻结研究基准，所有扩展版本保持 disabled。
- Market Momentum Opposite Move v11/v12 都是 `market_momentum_opposite_move_reversal` 的版本迭代，不是独立策略。v11 与 v12 的有效事件 EV/PF 分别为 `-0.116R/0.847`、`-0.089R/0.880`；冻结当前窗口和未见 Universe v4，不按亏盈选币或继续扫描阈值。
- Vegas v41 在 245 笔、约 8.20 笔/月下取得 EV `0.612R`、PF `2.120`、Sharpe `1.648`、Recovery `5.028`、盘中回撤 `14.79%`；仅 PF 失败仍按硬门禁拒绝。v42 精确移除 7 笔跨时段负 EV 的弱扩张空头后，EV 提到 `0.641R`、PF 提到 `2.163`，但频率降到约 `7.97/月` 且 walk-forward 仍有负窗，同样拒绝。v43 保持 v41 入场身份，仅把突破止损扩到至少 1ATR，EV/PF 反降至 `0.584R/2.032`，确认问题在入场质量而非普遍止损过紧。不得继续从这两类已查看特征扫描阈值。
- v48—v50 已依次验证顺势 FVG 回补失败、反向 FVG 完整收复和 fresh bullish CHoCH。v48 新增 79 笔 EV/PF `-0.298R/0.636`；v49 新增 2368 笔 EV/PF `-0.092R/0.698`；v50 新增 9 笔 EV/PF `-0.085R/0.818`。FVG/BOS/MACD 入场族停止，不以参数扫描救活。
- v51 初版与 v52 初版暴露公共市场结构快照会旁路改变旧规则；已改为专用运行时结构快照并不持久化。可审计 v51b `13987..14086` 为 247 笔、EV/PF `0.632R/2.096`，新增 9 笔的验证段 3 笔全亏；v52c `13887..13986` 为 286 笔、EV/PF `0.493R/1.781`，新增 48 笔全为空头且合计 `-8.208R`。两者都不晋级，不在已查看样本上加 RSI/影线/MACD 幅度补丁。
- v53b 严格三棒首次回测相对 v42 新增 11 笔、移除 0 笔；v54 用有效初始止损生成 2R 上限后达到约 `8.24/月、EV 0.635R、PF 2.188`，仍因 PF、样本内和 walk-forward 弱窗失败。v55 固定 R 替换退出、v56 普通扫低多头和 v57 失败跌破收回多头均使 PF 恶化；v57 新增 43 笔在 discovery/validation 均负期望。MACD 改善与金叉均未提供稳定确认，不再继续拼指标。
- v58 严格四棒更高低点突破没有新增交易；v59 扫高确认后跌破低点新增 4 笔并使频率达标，但 PF 只有 `2.180`。事后冻结的 v60 仅允许新增分支在 MACD 主线仍位于零轴上方时入场，标准组合达到约 `8.31/月、EV 0.652R、PF 2.225`，但样本内 PF/EV `1.847/0.539R`、walk-forward test-2 `-0.397R/PF 0.675`、双倍成本 `0.582R/PF 2.041`，且两笔增量主要依赖 TRB。v60 只保留 disabled forward shadow，不晋级。

## 错误记录

- 本机 K 线覆盖统计的首次 SQL 因含连字符表名未加双引号失败；修正引用后只读查询成功，未产生数据变更。
- 策略 crate 曾受既有 `range_breakout_drop` 测试初始化缺字段与过期真实数据 smoke API 影响；已按当前公开合同修复，`cargo test -p rust-quant-strategies --no-run` 恢复通过。
- 真实 15m 候选的固定比例 purge 未能覆盖持仓结算期限，时间泄漏 Gate 拒绝训练；改为仅按 signal/settled 时间扩大 purge，不读取收益标签。
- 1h 训练发现逻辑回归使用胜负标签和固定 0.5 阈值，未直接优化成本后期望 R；该问题进入下一训练协议，不回写本批次结果。
- 本机 `quant_core.funding_rates` 表当前四市场均为 0 行；仓库 TSV 只有约 91 天，不能对 365 天回测宣称已包含资金费率。
- Binance 公共接口因地理限制返回451；改用单一 Hyperliquid 来源，并写入分源分片表，避免与OKX事实混写。
- Hyperliquid funding 时间带毫秒偏移，首次按整点终点查询少1小时；修正为同小时桶容差后，15m每市场8,760点、1h每市场8,759点，缺口为0。
- v4首次运行因逻辑回归权重扩为10维但梯度仍为2维而越界；修正为按注册特征数量分配梯度，并新增维度回归测试，首次失败未产生研究结果。
- 研究 CLI 的 `--help` 在数据库门禁之后执行，无 Core 连接变量时无法显示帮助；未为本阶段修改该非关键顺序。
- 首次可复现性重跑使用项目脚本示例默认密码，被当前本机 Postgres 拒绝；随后只在进程内读取 Podman 容器现有配置完成只读重跑，未输出凭证。
- v5首次研究运行调用了旧构建产物，参数解析失败且未产生有效报告；显式重建后重跑。
- v5首次有效输出发现确认候选数反而超过原趋势 setup；定位为确认函数绕过 v1 顶层 Trend regime 门禁。该结果作废，修复为完整复用 `generate_pa_candidate` 后再重跑，未据此修改任何预注册阈值。
- v5 1h 首次运行被既有最少100笔研究门禁拒绝；为保留失败证据，只增强错误上下文输出实际已结算数量，不降低样本门槛。
- 错误上下文增强后，`collect()` 在先调用 `len()` 时无法从后续构造函数推断集合类型；显式标注为 `Vec<_>` 后恢复编译，不改变研究逻辑。
- 新增三时点回归测试首次使用的陡坡夹具无法触碰EMA20，因而没有形成原趋势setup；将测试专用斜率降为0.1，使夹具同时满足趋势和EMA回踩门禁，未修改生产阈值。
- OOF 聚合逻辑拆分后，Rust 默认从 `src/bin/oof.rs` 查找模块；补充显式 `#[path]` 指向同名二进制目录，未改变研究行为。
- 生产 K 线新鲜度首次查询使用了错误的动态表命名和时间列；按实际 `*-usdt-swap_candles_*` 与毫秒 `ts` 重新执行后得到只读结果，未产生数据变更。
- 部分生产容器 JSON 日志包含 NUL，标准 `docker logs` 解析失败；审计只读清理 NUL 后读取原始日志，未改动生产日志文件。

## 状态

**PA 独立策略已归档；Vegas 当前安全证据仍未闭环；Market Velocity 两个生产 preset 均未通过 Live 晋级门禁**：独立回放库已恢复旧动量数据，Long 为 0 可执行入场；Breakdown Short v6 为正收益但只有 43 笔、完整胜率和去集中度结果不达标。当前不启动 Live；先补显式滑点合同，随后只允许固定 v6 的新增样本前向稳健性验证。Vegas 在发布可显式关闭全部写回的 signed read-only 入口前，仍不得把历史成交与保护单确认解释为当前安全。

## 2026-07-19：ETH Vegas 4H v3 受控实盘发布

- [x] 新增独立版本 `eth_4h_id102_live_v3`，保留 v1/v2；固定 `entry_rule_version`、初始止损/R 与 `position_scale=0.58` 合同，禁止旧信号重放。
- [x] Core 提交 `9813f116` 已由后续 CI 修复提交 `312af50e` 包含并发布；生产 Core 全部目标容器运行该 revision。
- [x] 生产迁移已将 v1/v2 设为 disabled、仅启用 v3，并重启 ETH 4H 专用 worker；worker 仅加载 OKX `ETH-USDT-SWAP/4H/Vegas` 单一配置并等待未来已确认 4H K 线。
- [x] signed read-only 对账确认当前无非零仓位、无活动挂单；本次禁止下单、撤单、平仓和成交写回。
- [x] 用户已明确接受 20.79% 历史压力回撤的人工风险例外；这不等于自动职业门禁通过，严格前向 OOS 仍待累计。
- [ ] 仅观察未来新鲜 v3 信号与保护单证据；不得强制制造信号或补发历史信号。

## 2026-07-20：Vegas Universal 4H 方向与频率迭代

- [x] 精确复原 XRP `1957503` 信号时点、K 线、指标和初始风险证据。
- [x] 分解 v27 多空原始候选、过滤漏斗、成交质量和组合频率。
- [x] 以独立 disabled 版本实现两 K 线流动性扫单确认、窄口径方向冲突拦截和对称高波动延迟多头。
- [x] 完成 XRP 单例与 100 币共享资金回测，对比 v27/v28/v29/v30。
- [x] 按联合门槛拒绝未达标候选，保留 v27 冻结基线；未部署、未切换生产、未执行实盘 mutation。
- [ ] 后续仅在独立多头策略假设、历史 point-in-time 币种池和新的 forward OOS 上继续，不再用已查看区间放宽 v27。

## 2026-07-20：Vegas Bull Rank 4H v1 独立候选

- [x] 核对 v27 冻结身份、当前 live-only 币池覆盖、历史 point-in-time 缺口和本地正交因子覆盖。
- [x] 在查看结果前冻结独立多头策略身份、横截面排名规则、统一资金成本和失败即停止清单。
- [x] 新建 100 条 disabled v31 配置并完成 `11481..11580` 单币回测；未覆盖 v27、未部署、未触发实盘。
- [x] 修复 Universal 覆盖统计、完整基线 passthrough 合并和同币种重叠持仓约束，并以单元测试固定执行合同。
- [x] 完成宽口径池和严格 Bull Rank 组合评估；候选因增量交易为负、频率无实质改善和回撤超限被拒绝。
- [x] 按预注册早停规则跳过参数邻域、多窗口与 OOS，不在已查看样本上继续搜索阈值。
- [ ] 下一研究问题须先具备历史 point-in-time 币种池和全币池正交因子覆盖，再冻结新的独立候选。

## 2026-07-21：Vegas Universal 4H v61—v64 亏损逆向迭代

- [x] 按入场家族连接交易、动态调整与信号时点指标，确认压缩突破空头是最弱新增家族，并复核下影线、ATR、EMA、RSI、MACD、BOS/CHoCH 的跨期稳定性。
- [x] 预登记并回放 V61 `2.5x` 高量门禁；质量提升但频率约 `7.42/月`，拒绝。
- [x] 预登记并回放 V62 第二根严格首次回测；仅新增 1 笔，频率仍失败，拒绝。
- [x] 预登记并回放 V63 既有 `2.0x` 放量门禁；标准聚合达标，但样本内、walk-forward 与双倍成本 PF 失败，只保留 disabled 诊断前沿。
- [x] 预登记并回放 V64 对称下方扫单 + 局部 bullish BOS + MACD 零轴位置多头；新增组 PF `1.331` 且事件集中，拒绝。
- [x] 完成共同/新增/移除交易精确差分、统一资金组合、历史留出、12/3 月走步、双倍成本与有效事件审计；400 个研究配置全部保持 disabled。
- [x] 完成聚焦单元测试、配置转换测试与回放二进制构建；未部署、未进入 Paper/Live、未触发交易 mutation。
- [ ] 当前已查看历史停止继续拼接 FVG/BOS/MACD/RSI/影线阈值；下一步只累计冻结后的 forward shadow，或取得完整历史覆盖的新卖压衰竭/去杠杆证据后另立候选。

## 2026-07-21：V65 冻结前沿首轮 Forward Shadow

- [x] 在读取新增行情前冻结 V63 的策略、风险、100 币与组合合同，创建 100 条 disabled V65 配置。
- [x] 增量补齐 99 币 OKX 4H 行情；确认 LRC 官方不可用，不换币、不补造行情。
- [x] 修复 dry-run 修改交易对元数据和已结束未确认 K 线未进入缺口修复的两个数据合同问题，并完成聚焦回归。
- [x] 完成 V65 `15820..15919` 单币回测、统一资金组合、有效事件、时间切片、walk-forward 和双倍成本审计。
- [x] 精确确认 V63/V65 共同 `243`、新增 `1`、移除 `0`、共同变化 `0`；NEAR 新增盈利属于冻结前 sealed gap，strict-forward 交易为 `0`。
- [x] 按预登记停止规则保留 `forward_shadow_observation_insufficient`，V65 全部保持 disabled，未进入 Paper/Live 或交易 mutation。
- [ ] 继续累计冻结后完成 K 线；出现完整 strict-forward 开平仓后，再按相同成本和组合合同更新证据。

## 2026-07-21：V66 熊市失败压缩跌破回收多头

- [x] 拆分最差 walk-forward 亏损事件、相关簇、入场家族与 BTC 因果长周期状态，否定简单相关簇限仓。
- [x] 在查看反向路径前预登记独立策略身份、12 根观察、止损位收回确认、确认棒止损、2R 与 12 根持有上限。
- [x] 实现只读研究入口，固定读取 V63 信号时点审计与确认 K 线，不读取原空头结局生成候选。
- [x] 完成 18 笔标准/双倍成本、IS/OOS、年度与固定 walk-forward 回放；各分段均未形成可接受优势。
- [x] 按四项失败门禁淘汰 V66，不进行窗口、目标、MACD、FVG、BOS、影线或币种参数补丁。
- [ ] 下一候选若使用 MACD，只测试此前未使用的 BTC 市场级动量状态，并在结果读取前固定规则。

## 2026-07-21：V67 BTC 市场级 MACD 冲突与回收多头

- [x] 严格按信号前 BTC 4H MACD `12/26/9` 拆分压缩跌破，确认正柱组原空头在 IS/OOS 均弱。
- [x] 在读取反向路径前冻结两根阳线实体中点收回、共同低点止损、2R 和 12 根持有规则。
- [x] 实现独立 V67 只读入口与 MACD 同棒排除回归测试。
- [x] 回放 28 个正柱种子；仅 5 笔确认且全部成本后亏损，IS/OOS 均不通过。
- [x] 按门禁淘汰 V67，不放宽确认、不修改止损/目标、不进入组合替换。
- [ ] 保留 V65 frozen forward；不再从当前历史为 MACD/FVG/BOS 反向规则追加参数。

## 2026-07-21：V69 组件去耦

- [x] 将 V69 已成交信号按互斥机会来源归因，不改变任何 Strategy 阈值或历史交易事实。
- [x] 将预计双倍成本上限提取为独立纯质量政策，验证新旧接受交易 ID 与顺序一致。
- [x] 分别报告机会候选、质量通过、组合接纳三个阶段，并补充固定 IS/OOS 组件表现。
- [x] 验证币种输入反转后组合结果不变；聚焦测试 51 项通过。
- [x] 重跑 V69 标准/双倍成本及多空诊断，确认容量无淘汰、成本门禁主要作用于压缩空头。
- [x] 保持 V69 rejected/disabled 研究边界，不创建新版本，不进入 Paper/Live。
- [ ] 后续 Strategy evaluator 的完全物理解耦等待目标 Research/Strategy/Portfolio 切片迁移；迁移前继续使用本次零漂移研究归因，禁止一次性改写 live evaluator。

## 2026-07-21：V79 压缩突破空头独立回放

- [x] 预登记单变量假设：只解除压缩突破与旧机会家族的决策优先级耦合，不修改市场阈值、止损、退出、成本或容量。
- [x] 新增默认关闭的 `standalone` 隔离开关与独立策略身份；100 条 V79 配置保持 disabled，参数和风险配置相对 V69 无额外漂移。
- [x] 完成 100 币单币回放 `16922..17021`：86 笔全部为压缩突破空头，其他机会标签为 0。
- [x] 完成统一资金标准/双倍成本、IS/OOS、固定 walk-forward、有效事件、集中度和币种顺序无关验证。
- [x] 确认去耦只比 V69 压缩组件增加 3 个标签，其中相对 V69 总机会集真正新增仅 1 笔且亏损；有效频率未改善。
- [x] 按双倍成本 PF、Sharpe、Recovery、最后 walk-forward 和频率门禁拒绝 V79，不并入 V69，不进入 Paper/Live。
- [ ] 保留 V69 为不可 promote 的冻结历史前沿；当前已见历史不再扫描压缩突破、MACD、FVG/BOS、影线或成本阈值。

## 2026-07-21：Market Momentum v38 MACD 增量验证

- [x] 冻结 v36 唯一增量：收盘价 MACD(12,26,9) 前一根负柱、当前柱缩短；不扫描参数。
- [x] 新增独立 research-only 参数、审计字段、因果 MACD 计算和未来 K 线隔离测试。
- [x] 复核动态开发 Top60 已随当前 live 币池漂移，拒绝用 0 候选冒充旧 v36 对照。
- [x] 在冻结 2024-07 至 2025-06 manifest 上精确复现 v36，并完成 v38 同口径扩窗。
- [x] 按样本、EV、PF、Sharpe、月份覆盖、频率和去 Top3 集中度门禁淘汰 v38。
- [ ] 保持策略总目标未完成；后续只接受新的 point-in-time 外生因子数据或真正 forward 样本，不在已见 v36 窗口扫描 MACD/BOS/FVG 参数。

## 2026-07-21：Market Momentum v36 开发池结果恢复落库

- [x] 从原始运行产物校验并恢复 57 笔交易、41 个币、30 胜 27 负及完整净 R/利润汇总。
- [x] 落库主记录 `back_test_log.id=17022` 和 114 条开平仓明细，确认初始风险与平仓净 R 无缺失。
- [x] 标记为 ResearchBar 历史产物恢复，保留动态 Top60 未冻结与不可 promote 边界。
- [ ] 后续查询必须区分开发池快照 `17022` 与历史扩窗基线 `16921`；两者均不得进入 Paper/Live。

## 2026-07-21：Vegas ETH 15m 随机参数回测性能与首轮迭代

- [x] 审计 legacy 随机参数链路，修复笛卡尔网格冒充随机采样、RSI 索引错位、进度分母错误和未使用锤子线维度。
- [x] 增加固定种子、有限无重复采样、小批次耗时熔断、并发上限、精确单目标入口和 CPU 计算线程隔离。
- [x] 随机模式关闭逐 K 线审计、shadow 诊断、成交明细、过滤信号和绩效明细写入；专用 Vegas/NWE 随机开关统一使用同一底层口径。
- [x] 完成 ETH 15m 40,000 根 K 线两轮各 64 组采样；第一轮 `17028..17091`、成本感知第二轮 `17093..17156` 均为 0 个盈利候选。
- [x] 完成最佳候选 `17107` 明细诊断：净 PF `0.7651`、Sharpe `-2.1294`、最大回撤 `71.50%`，不满足晋级门槛。
- [x] 完成随机模式合同复测 `17157..17164`：8 条汇总、0 条成交明细，峰值 RSS 约 43 MiB，未触发 5 秒/组与 1.5 GiB 熔断。
- [ ] 不扩大当前参数空间；下一轮必须先形成减少换手/费用侵蚀的独立入场或退出假设，再冻结新版本、窗口和成本口径。

## 2026-07-21：15m 趋势耗竭一次性状态 V1

- [x] 冻结独立 research-only 策略身份，只使用已完成 15m K 线，不读取 `market_rank_events`、episode 或 1m。
- [x] 实现 `neutral -> armed -> consumed -> neutral reset` 状态机；趋势直接翻转不能绕过中性重置。
- [x] 当前 live OKX USDT 线性永续币池完成冻结复算：66,902 个有效 setup 去重为 20,644 个，最终 18,360 笔交易。
- [x] 完成前后半段、四分位、集中度和成本后组合审计；胜率 35.64%，但 EV/PF 为 `-0.072563R/0.888247`，四段均不为正。
- [x] 按预登记门禁淘汰 V1，保持 research-only；未写回策略配置、未进入 Paper/Live、未触发实盘 mutation。
- [ ] 下一候选必须另立预登记版本，验证稳定中性持续与趋势真正失效后的重置；不得用 V1 已见结果原地调阈值。

## 2026-07-21：15m 趋势耗竭稳定重置 V2

- [x] 在读取结果前冻结唯一变化：趋势状态必须连续 8 根中性 15m K 线后才允许重置，不扫描确认长度。
- [x] 实现可审计的中性连续计数；同方向趋势恢复会清零部分计数，趋势直接翻转不能绕过中性确认。
- [x] 完成默认值 V1 精确复现，确认新增参数默认关闭时不污染既有研究结论。
- [x] 完成 216 个当前 live OKX USDT 线性永续的冻结回放；armed、重置、状态后 setup 和实际交易均低于 V1。
- [x] 完成前后半段、四分位与盈利币集中度审计；V2 净 EV/PF 为 `-0.070694R/0.889728`，四段均为负。
- [x] 按预登记门禁淘汰 V2，保持 research-only；未落库、未进入 Paper/Live、未触发交易 mutation。
- [ ] 下一候选不得继续扫描重置长度；先预登记多空方向诊断与生产短窗口内的价格拒绝/回收确认。

## 2026-07-21：15m 外部证据纠偏与独立方向验证

- [x] 先查阅 TradingView 官方 Volume、Relative Volume、RVAT、Volume Delta、图形确认说明，及短周期加密货币动量/反转研究，再冻结本地假设。
- [x] 修复 15m 研究入口只读取 `confirm=1` 已完成 K 线，保持当前 live OKX USDT 线性永续币池，不读取事件归档、episode 或 1m 信号。
- [x] 诊断确认旧反向信号中 `94.22%` 的 setup 实体仍沿历史趋势，且量比/振幅越极端，反向 EV 越差。
- [x] 预登记并回放 setup-open reclaim V3；新旧窗口 EV/PF 分别为 `-0.160582R/0.764387` 与 `-0.143475R/0.801138`，拒绝等待回收确认。
- [x] 新建极端量延续 V1 策略家族；新窗口 `-0.090580R/0.864218`，旧窗口 `+0.055022R/1.084146`，拒绝时序不稳定优势。
- [x] 按策略平台定义实现 UTC 同时点过去 10 日 RVAT10 V2；新窗口 `-0.093252R/0.861429`，旧窗口 `+0.090063R/1.137263`，确认季节性修正不能恢复最新优势。
- [x] 所有版本保持 research-only，不落库、不进入 Paper/Live、不触发交易 mutation；停止对现有 15m OHLCV 家族扫参数。
- [ ] 若继续，需用户确认是否允许把逐笔主动买卖量聚合为已完成 15m volume delta/taker imbalance；策略仍按 15m 记录判断，不使用 1m K 线触发。

## 2026-07-21：Core WebSocket 与 AWS CPU 热路径优化

- [x] 策略进程默认关闭重复 ticker 订阅，并在生产 compose 对 ETH/Universal 4H worker 固定 candle-only 模式。
- [x] 未确认 K 线只更新进程内缓存；确认收盘后才同步 Redis、触发策略并进入数据库持久化队列。
- [x] 健康状态改为紧凑 debug，异常只预览最多 20 个过期目标，停止每 10 秒序列化全部订阅快照。
- [x] Market Velocity 雷达默认扫描周期由 10 秒调整为 60 秒，并新增生产部署合同。
- [x] Market 22 项测试、CLI 检查、生产部署合同 7 项和差异检查通过；所有触碰文件低于 1000 行。
- [x] 提交并通过 CI/CD 发布 `c92d33f`；生产 4H worker 固定 candle-only，雷达扫描周期固定 60 秒。
- [x] 提交并通过 CI/CD 发布 `70f3281`；K 线分表只在进程首次访问时 ensure，历史快照改为批量 UPSERT 并跳过无变化行。
- [x] CloudWatch 连续三个 5 分钟窗口 CPU 约 `11.4% / 12.1% / 12.0%`，`CPUSurplusCreditsCharged=0`；第二阶段上线后 4 个雷达周期仅冷启动首轮出现 25 次 DDL，后续无重复。
- [ ] 继续观察 24 小时 CPU credits、确认 K 线延迟与 Data Transfer；当前证据不足以支持再引入全市场 1m 聚合服务。

## 2026-07-21：15m Taker Delta 背离反转 V1

- [x] 核对本地 OKX 15m schema 不含主动买卖方向，拒绝用 K 线实体或普通成交量伪造 Volume Delta。
- [x] 冻结独立 research-only 规则：OKX 已完成 15m 价格/总量 + Binance 原生 15m taker buy quote share，不读取 1m、事件归档或 episode。
- [x] 只使用 current-live crypto-only OKX top60，并要求 Binance 当前 live 映射；排除当前退市币并保留幸存者偏差声明。
- [x] 实现 SHA-256 月包校验、精确 15m 对齐、60%/40% 背离、一次性趋势状态、下一根开盘、3% 止损、Volume-ATR 目标和双边成本。
- [x] 完成开发窗口：189 笔、177 个有效事件、EV/PF `+0.077393R/1.136688`，但 Q2/Q3 负向且去 Top3 后为负。
- [x] 完成独立旧窗口：129 笔、124 个有效事件、零成本 EV `-0.058993R`，成本后 EV/PF `-0.112376R/0.827757`。
- [x] 按时序、集中度、职业门槛和频率联合门禁淘汰 V1；不落库、不生成回测 ID、不进入 Paper/Live。
- [ ] 若继续，先建立多棒累计 delta 的独立因子面板并验证固定 1h/4h forward return；不得在已见结果上扫描 55%/45%、65%/35% 或只做单方向。

## 2026-07-21：15m 多棒累计 Taker Delta 增量因子 V1

- [x] 在读取结果前冻结四根原生 15m 累计 Delta、20 个过去 1h 总量基线、UTC 4h 决策屏障与固定 1h/4h outcome。
- [x] 按每个决策点的价格方向、价格幅度中位数和相对总量中位数建立 2×2 对照，主要统计背离组减同向组的 time-level 配对增量。
- [x] 开发窗口形成 126,103 个完整 observation；多头/空头 4h 配对增量为 `+0.035890%/-0.054704%`，均未达到 0.16%经济门槛。
- [x] 独立旧窗口形成 126,034 个完整 observation；多头/空头 4h 配对增量为 `+0.004369%/+0.055970%`，方向与开发窗不稳定且仍低于经济门槛。
- [x] 两个年度的四个背离象限自身 4h 平均方向收益均低于 0.16%，其中旧窗两方向均为负；因子门禁失败。
- [x] 5 项因果、连续性和分层单测及研究二进制构建通过；未落库、无回测 ID、未进入 Paper/Live 或交易 mutation。
- [ ] 停止在当前已见历史扫描累计窗口、Delta 阈值、单方向、MACD 或 BOS/FVG；该因子不进入 15m 动量反转开仓逻辑。

## 2026-07-21：全市场收盘 K 线亚秒监听

- [x] 否决“收盘后等待 2～3 秒再轮询”的实现，改为 OKX 全市场 `candle1m` 确认流；只让 `confirm=1` 进入业务队列。
- [x] 按最多 150 个交易对分片订阅，并使用 4096 容量有界队列；单任务无锁聚合 1m、5m、15m、4H。
- [x] 固定使用当前已确认成交量相对前 20 根已确认 K 线平均成交量的倍数，默认 2.0；输出只读观测，不生成交易任务。
- [x] 启动预热按交易对流式完成，数据库缺失时用受限 REST 补内存；运行中缺口只暂停对应币种并异步修复。
- [x] 增加每分钟本机 P50/P95/P99 延迟、队列深度和亚秒 SLO 证据；正常热路径不查库、不调用 REST、不等待持久化。
- [x] 补齐独立生产二进制、Compose 服务、发布/回滚与只读验收契约。
- [x] Market 定向测试 10 项、CLI 定向测试 4 项、专用二进制检查和生产部署合同 7 项通过。
- [ ] 尚未提交、推送或部署；上线后必须用真实 `all_market_candle_local_latency` 日志确认 P99，并区分交易所 `confirm=1` 到达延迟与 Core 本机处理延迟。

## 2026-07-22：15m RSI 放量横盘突破与背离 V2

- [x] 保留 v1 和冻结 v36，新增独立 research-only v2 规则版本；不注册 Paper/Live。
- [x] 横盘分类移除“前两根 RSI 在 40～60”，改为信号前 96 根布林带宽 20% 低分位且 MACD 双线距离零轴不超过价格的 0.15%。
- [x] 仅当当前 RSI 严格高于 70 或低于 30 时进入横盘突破判断，并要求收盘突破前一根布林带边界。
- [x] 新增同一历史价格枢轴配对的常规 RSI 背离；背离不要求当前 RSI 极值，但继续要求当前成交量至少为前 5 根均量的 2 倍。
- [x] 背离、横盘突破和原 96 根反转分支均保留反向长影线拦截、结构止损与 3% 最大止损门禁。
- [x] 15 项策略定向测试、427 项回测模块回归、格式、构建、diff 和文件行数检查通过。
- [x] 同样本成本回放得到 8,585 笔：净 EV `-0.493619R`、PF `0.578133`、胜率 `28.7711%`、Sharpe `-7.54864`、最大单币隔离回撤 `77.5016%`。
- [x] 多空背离与两种压缩突破分支均为负 EV；v2 保持研究失败状态，不落库、不生成回测 ID、不进入 Paper/Live。
- [ ] 若继续，不直接扫描 RSI、布林、MACD 或背离参数；先基于分支亏损路径提出独立、可证伪的新入场确认假设。

## 2026-07-22：15m RSI 量价反转与 ATR 止损 V3

- [x] 保留 v1、v2 与冻结 v36，新增独立 research-only v3；不注册 Paper/Live，也不覆盖既有版本。
- [x] 全部分支统一使用当前成交量除以前 4 根均量，量比严格要求至少 1.5。
- [x] RSI 背离保留 48 根、左右各 3 根因果枢轴，改为 RSI 严格低于 30/高于 70 且同点至少改善 1 点。
- [x] 横盘突破仅用前一根低分位布林压缩与 MACD 双线零轴附近判断背景；突破方向不读取当前 RSI。
- [x] 96 根背景仅保留首开到末收净跌/涨至少 8%，移除线性趋势替代条件；不要求当前 K 线收阳/收阴。
- [x] 全部分支限制做多上影线或做空下影线不超过实体 45%，零实体阻塞；同向分支合并原因，反向冲突不交易。
- [x] 初始止损固定为入场价反方向 `1.5 * ATR14`，并验证不会回退到旧 3%；Volume-ATR `1.8R～3R` 止盈保持不变。
- [x] 25 项策略测试、438 项回测模块回归、二进制构建、格式与差异检查通过；文件 1398 行低于 2000 硬上限，但存在 1000 行目标提醒。
- [x] 同样本成本回放实际 8,992 笔：净 EV `-0.133195R`、PF `0.845078`、胜率 `25.9453%`、Sharpe `-4.25877`、最大单币隔离回撤 `89.6022%`。
- [x] v3 相对 v2 有改善但仍远低于晋级线，且约 `713.5 笔/月`；保持研究失败，不进入 Paper/Live。
- [x] 应用户要求将同一冻结样本完整落库为 `back_test_log.id=17169`：8,992 笔交易、17,984 条明细，初始止损/风险全覆盖、8,992 条平仓净 R 全覆盖；旧的不完整记录 `17166` 已标记由 `17169` 替代。
- [ ] 唯一正向组合“底背离 + 96 根净跌”仅 151 笔，EV/PF `0.121435R/1.158036`，不足以拆出生产候选；若继续必须先冻结新假设和未见验证窗口。

## 2026-07-22：15m RSI 放量反转 V4 移除压缩突破

- [x] 保留 v3 回测与规则版本不变，新增独立 research-only v4，避免覆盖 `back_test_log.id=17169` 的历史证据。
- [x] 从 v4 多空两侧完全移除窄布林、MACD 零轴及收盘突破上下轨分支。
- [x] v4 只保留 RSI 极值背离与前 96 根净涨跌至少 8% 两个候选分支，并继续执行四根量比、反向影线、方向冲突和 ATR 止损门禁。
- [x] 增加 v3/v4 对照测试，证明 v3 压缩突破仍可重放，而相同多空 setup 在 v4 均不生成信号。
- [x] 格式检查、7 项 v4 定向测试和 research-only 身份测试通过；未运行回测、未落库、未进入 Paper/Live。
- [ ] 后续若需评估效果，以 v4 独立规则版本运行冻结样本与未见窗口回测，不能把结果写回 v3。

## 2026-07-22：15m RSI 放量反转 V5 因果异常量过滤

- [x] 保留 v4 四根原始均量 1.5 倍口径不变，新增独立 research-only v5。
- [x] 对最近 10 根历史 K 线逐根判断：该根成交量大于或等于它自己此前 10 根原始均量的 2 倍时标记为历史放量。
- [x] 当前量比的分母只使用最近 10 根中未被标记的历史 K 线；当前 K 线不进入分母，始终保留为分子。
- [x] 当前成交量大于或等于过滤后均量的 2 倍才通过；若 10 根历史全部被剔除则阻塞入场，不回退旧均量。
- [x] 测试覆盖历史量等于 2 倍、超过 2 倍、低于 2 倍、20 根历史预热、v4 隔离和 v5 ATR 止损；RSI 策略族 31 项回归通过。
- [x] 格式、差异和代码文件硬上限检查通过；策略文件与参数文件仍有 1000 行目标提醒，但均低于 2000 行硬上限。
- [x] 按 v3 同一 current-live Top60、2025-07-01 至 2026-07-19、双向、单边手续费 5bps、单边滑点 3bps、48 小时上限完成全量回放并落库为 `back_test_log.id=17170`。
- [x] `back_test_detail` 共 11,512 行：5,756 个开仓与 5,756 个平仓一一配对，覆盖 42 个币；初始止损、初始风险和净 R 均完整。
- [x] v5 净 EV `-0.081238R`、净 PF `0.901686`、胜率 `24.5309%`、Sharpe `-2.209516`、最大单币隔离回撤 `90.9215%`、总利润 `-631.5504U`，约 `457.29` 笔/月。
- [x] 相比 v3，交易数减少 `35.99%`，EV/PF/Sharpe 只改善到较小负值；胜率下降 `1.4144` 个百分点，最大单币隔离回撤反而增加 `1.3193` 个百分点，仍无正优势。
- [x] 前后半段 EV 分别为 `-0.079472R/-0.083110R`；42 个币仅 11 个盈利。回测后按同方向相邻不超过 30 分钟聚类得到 3,049 个事件，只作未预注册诊断，不作为晋级证据。
- [ ] 当前结果存在审计阻塞：1,159 笔（20.14%）初始止损仍被通用风控收紧到 3%，与“纯 1.5 ATR、不受固定 3% 上限”不一致；5,756 笔 `stop_loss_source` 全为空；逐笔明细没有过滤量比的分子、分母与剔除数量；另有 6 条月末结束平仓缺 `close_price`。因此 `17170` 仅为当前实现的诊断回测，不是最终可晋级 V5 基线，也未进入 Paper/Live。

## 2026-07-22：过滤量比 + RSI/EMA/MACD 15m V1

- [x] 新增独立 Research-only 策略 `market_filtered_volume_rsi_ema_macd_15m_v1`，规则版本为 `kline15m_filtered_volume3_rsi_ema_macd_atr15_v1`；未覆盖旧策略，也未注册 Paper/Live。
- [x] 最近 10 根历史 K 线逐根按各自此前 10 根原始均量标记 `>= 3` 倍异常量；当前量比分母排除已标记历史量，至少保留 5 根，当前量比必须 `>= 3`。
- [x] RSI、EMA、MACD-DIF 三个分支并行生成候选；同向合并、反向冲突不交易。未继承 96 根涨跌、布林带、BOS、FVG、CHoCH 或其他旧入场逻辑。
- [x] 入场使用已完成 15m K 线收盘价；初始止损固定为反方向 `1.5 * ATR14`，不受 3% 上限；过滤量比 `[3,4)/[4,6)/[6,+∞)` 分别冻结为 `1.8R/2.4R/3.0R`。
- [x] 13 项新策略测试、459 项回测模块回归、专用二进制构建、格式、差异和 2000 行硬上限校验通过。
- [x] 按 current-live Top60、2025-07-01 至 2026-07-19、双向、单边手续费 5bps、单边滑点 3bps、最长持仓 48 小时完成回放并落库为 `back_test_log.id=17171`。
- [x] `back_test_detail` 共 14,464 行：7,232 开仓与 7,232 平仓一一配对，覆盖 42 个币；策略身份、过滤量比、保留根数、目标档、分支门槛、止损来源和净 R 审计均无错配，旧逻辑触发为 0。
- [x] 总体净 EV `-0.141419R`、PF `0.822623`、胜率 `32.2179%`、Sharpe `-4.724372`、最大单币隔离回撤 `64.0420%`、净和 `-1022.7452R`；前后半段 EV 均为负，仅 11/42 个币净 R 为正。
- [x] 独占分支均无正优势：RSI-only `-0.100281R/PF 0.873671`，EMA-only `-0.099267R/PF 0.865661`，MACD-only `-0.159776R/PF 0.803399`；255 笔同向重叠信号更差，为 `-0.228968R/PF 0.718430`。
- [x] 185 笔初始 ATR 止损距离超过 3%，证明未被旧上限截断；6 条窗口末结束平仓缺 `close_price`，但 `net_profit_r` 均完整，不影响总指标。
- [ ] 该版本结论为 `research_rejected_no_edge`，不进入 Paper/Live；若继续研究，先重新定义可证伪的新假设和未见窗口，不在已见样本上继续扫描阈值。

## 2026-07-22：过滤量比策略 MACD 枢轴背离 V2 修复

- [x] 保留 v1 与 `back_test_log.id=17171` 不变，以同一策略家族的新规则版本 `kline15m_filtered_volume3_rsi_ema_macd_pivot_atr15_v2` 承载修复，继续限制为 Research-only。
- [x] MACD 只在当前完成柱 `t` 收盘时检查刚确认的 `p=t-3`，并与 `p` 前 48 根内最近同类型严格价格枢轴 `q` 比较；相等高低点不再认作枢轴。
- [x] 背离只读取 `p/q` 的 DIF、收盘价、ATR14 与 `p` 的 RSI14；顶背离要求两点严格高于各自 `Z*ATR14`，底背离要求严格低于负向边界，并使用 `DIF/close` 检查 `D_min`。
- [x] `Z` 与 `D_min` 必须成对显式提供且都大于 0；任一未配置时只关闭 MACD 分支，不猜默认值，也不影响 RSI、EMA 与成交量门禁。
- [x] 逐笔证据新增 `p/q` 时间、价格、DIF、归一化 DIF、RSI、实际改善值、`Z` 和 `D_min`，信号时间仍为 `t`，不回填到 `p`。
- [x] MACD 专项 22 项、回测模块 468 项均通过；`cargo check -p rust-quant-cli` 与 2000 行硬上限门禁通过，只有仓库既有 warning 和 1000 行目标提醒。
- [x] 回测前已冻结 `Z={0.10,0.20,0.30}`、`D_min={0.0005,0.0010,0.0020}`、开发/未见窗口、选择规则和 30 分钟事件聚类口径；九组开发集均完成。
- [x] 九组 MACD-only 仅 5～15 笔，无一达到 100 笔门槛；证伪诊断点 `Z=0.20,D_min=0.0010` 在未见段为 `-0.130231R/PF 0.833924`，略差于 MACD 关闭组。
- [x] 完整窗口 3,176 笔，净 EV `-0.140171R`、PF `0.821720`、Sharpe `-3.505425`、最大单币隔离回撤 `55.2408%`；四个时间分段均为负 EV。
- [x] RSI/EMA 独立分支分别为 `-0.133530R/-0.147239R`；MACD-only 虽为 `+0.482055R`，但只有 14 笔且未见段仅 4 笔，样本和 PF/Sharpe 均不达标。
- [x] 3,176 笔按预注册规则聚为 1,914 个事件；结论为 `research_rejected_no_edge`，本轮未落库、无新回测 ID、未进入 Paper/Live。

## 2026-07-22：同币种周 `vol_ccy` 过滤 + 1% 风险定仓 V3

- [x] 按 `docs/strategy_list/15min_velocity_all_sybol_strategy.md` 新增独立 Research-only 策略键 `market_filtered_volume_weekly_base_volume_rsi_ema_macd_15m_v1`，保留过滤量比 V1/V2 行为与结果不变。
- [x] 当前信号同时通过因果过滤量比 `>=3` 和同币种前 672 根 `vol_ccy` nearest-rank P90；缺失、非法或不连续一律失败关闭。
- [x] RSI、EMA、修正后的 MACD-DIF 枢轴分支按文档合并；同向合并、多空冲突不交易，信号只读取已完成 15m K 线。
- [x] 成交改为下一根开盘；RSI 反转形态参与时采用吞没/长影线结构止损，否则按实际成交价重锚 `1.5*ATR14` 止损。
- [x] 止盈改为过滤后量比对应的固定 `2.7/3.6/4.5*ATR14` 价格距离，不再按止损距离换算固定 R；移除最长持仓，保留窗口末研究结算。
- [x] V3 不再受通用 3% 最大止损收紧；成交 K 线内立即检查止损/止盈，形态止损在实际成交后失效时立即按双边成本退出并保留审计原因。
- [x] 长周期门槛直接读取现有每币 15m 分表 `vol_ccy`；已移除本轮新增的 `volCcyQuote/market_candles` 抓取与回填路径。
- [x] 单笔风险按入场前币种隔离权益 `1% / 实际止损距离` 计算仓位；2,204 笔落库交易风险比例最小/最大均为 1%，偏差笔数 0。
- [x] Top60 全窗口 MACD 关闭基线已落库：`back_test_log_id=17172`，2,204 笔、42 币、4,408 行明细；开平仓配对错配 0。
- [x] 基线净 EV `-0.161325R`、净 PF `0.800122`、胜率 `29.9909%`、Sharpe `-4.8145`、净和 `-355.559956R`，明确不满足晋级门槛。
- [x] `final_fund=3867.496270` 是 42 个币各 100U 隔离账户之和，初始合计 4,200U；不是单账户从 100U 增长到 3,867U。
- [ ] 统一组合资金、最大并发风险、交易所数量精度与杠杆上限仍未实现；不得把本轮最大单币回撤当作生产组合回撤。

## 2026-07-22：BB(12,2.6) 反向冲突缓冲 V4

- [x] 保留 V3 与 `back_test_log.id=17172`，新增独立 Research-only V4 规则和预设。
- [x] 只有 V3 已存在唯一方向时才允许 BB 投反向票：下轨触达抵消空单，上轨触达抵消多单；BB 不得独立开仓。
- [x] 使用当前及此前共 12 根已完成 15m 收盘、总体标准差 2.6 倍；最高/最低触轨包含等号，不读取未来 K 线。
- [x] EIGEN `2026-07-17 21:00` 信号柱复算下轨 `0.227177390623`，最低价 `0.2256`，V4 成功取消 `21:15` 的原空单。
- [x] V4 专项 5 项、过滤量比策略族 33 项、全库 784 项通过；唯一沙箱监听权限失败在沙箱外精确复跑通过，二进制构建、格式、diff 和 2000 行硬上限检查通过。
- [x] 同口径 Top60 全窗口回测已落库为 `back_test_log.id=17173`：1,457 笔、42 币、2,914 行明细，开平仓缺配 0，逐笔 BB 证据和 1% 风险复算错配 0。
- [x] 净 EV `-0.173468R`、PF `0.789385`、胜率 `29.7186%`、Sharpe `-4.1501`、最大单币隔离回撤 `40.4499%`、总利润 `-234.1356U`。
- [x] 相比 V3 少 747 笔且绝对亏损减少 98.3681U，但 EV/PF/胜率均略差；结论为 `research_rejected_no_edge`，不进入 Paper/Live。
- [ ] 若继续，先诊断被过滤的 756 笔交易并预登记“真反转/沿轨延续”确认条件；不在已见样本上扫描 BB 参数。

## 2026-07-22：EMA144 一 ATR 距离门禁 V5

- [x] 保留 V3/V4 与既有落库结果不变，新增独立 Research-only V5 规则和预设；不接入 Paper/Live。
- [x] 仅对 EMA 延续候选计算 `abs(signal_close-EMA144)/ATR14`，距离 `<=1.0` 放行，严格大于 1.0 删除；RSI/MACD 独立候选不受影响，多空完全镜像。
- [x] V5 不叠加 V4 布林带，形成相对 V3 的单变量 A/B；逐笔明细保存实际距离、阈值与 EMA 候选是否被门禁删除。
- [x] EIGEN `2026-07-17 21:15` 原空单距 EMA144 为 `4.68 ATR`，V5 已取消该交易。
- [x] 6 项 V5 专项、41 项过滤量策略族、全库 790 项通过；唯一沙箱监听权限失败在沙箱外精确复跑通过，二进制构建成功。
- [x] 同口径 Top60 回测已落库为 `back_test_log.id=17174`：1,109 笔、42 币、2,218 行明细，1,109 组开平仓和距离证据完整。
- [x] 净 EV `-0.213129R`、PF `0.751294`、胜率 `27.8629%`、Sharpe `-4.3486`、最大单币隔离回撤 `37.5170%`、总利润 `-220.1199U`。
- [x] V5 删除的 1,091 笔 V3 EMA 交易平均 `-0.115892R`，保留 22 笔反而平均 `-0.321468R`；“靠近 EMA144 更好”的假设被证伪。
- [x] V4 在 EV、PF、胜率、Sharpe 及 EMA 子集质量上均优于 V5；但两者都为负优势，结论均保持 Research-only rejected。
- [ ] 不在已见样本扫描 EMA 距离阈值；若继续，先预登记距离分桶、MFE/MAE、趋势斜率和回踩确认假设，再用未见窗口验证。

## 2026-07-23：DOGE 背离、长下影与盈利观察 V6-V8

- [x] 逐根复核 DOGE `2026-07-05 15:30`：截图中的有效底背离实际发生在 `12:00 -> 13:30` 两个严格枢轴，RSI14 改善 `1.370191`；右侧三根确认后对应 `14:30` 事件，但枢轴与确认 K 的过滤量比/P90 均未通过。
- [x] 逐根复核 DOGE `2026-06-25 01:45`：价格与 RSI14 底背离数学条件成立，RSI 改善 `1.610902`；确认事件为 `02:45`，因确认 K 的过滤量比和 P90 双双失败而未进入 RSI 分支。
- [x] 结论不是 RSI 改善 1 点或左右各 3 根过严，而是“枢轴背离发生在 p、极端成交量却要求确认 K t 同时成立”的时间口径错位；仅把量门槛移到 p 仍无法覆盖这两例。
- [x] 新增独立 Research-only V6：只在 V3 无任何候选时补充 `30 < RSI14 <= 70`、下影占振幅至少 60%、实体占比大于 10% 的双重放量做多，不改变已有 V3 交易。
- [x] 新增独立 Research-only V7：0.5R 仅观察；1R 前后续完整 K 收盘跌回 0.25R 下退出；1R 后按真实 ATR 目标完成比例生成单调保护位。
- [x] 新增 V8 组合版，并补齐 V3 隔离、边界、出场优先级、同根等价、详情截断与长历史独立保存测试；回测模块 496 项通过，盈利观察专项 5 项通过，构建及 2000 行硬上限检查通过。
- [x] 同 Top60、同窗口、同成本和 1% 风险回测已落库：V6=`17175`、V7=`17177`、V8=`17178`；开平仓一一配对，净 R 无缺失。`17176` 是详情字段超长导致事务未写入明细的空失败记录，不是有效回测。
- [x] V6 为 3,904 笔，净 EV/PF `-0.199152R/0.759961`；新增分支自身 1,704 笔、胜率 `27.88%`、净和 `-450.3685R`，42 币仅 4 币为正，拒绝该通用入场假设。
- [x] V7 为 2,228 笔，净 EV/PF `-0.160452R/0.694167`、胜率 `43.2675%`、最大单币隔离回撤 `28.9540%`；胜率和回撤改善，但净和比 V3 多亏 `1.9279R`，PF 与 Sharpe 明显恶化。
- [x] V7 在匹配事件中救回 `382` 笔原亏损单、合计改善约 `788.0132R`，同时伤害 `89` 笔原盈利单、合计回吐约 `782.3984R`；新增释放出的 24 笔后续交易再亏 `7.5427R`，解释了总体未改善。
- [x] DOGE `2026-07-01 21:30` 满足 V6 新分支：过滤量比 `3.214638`、周 P90 通过、RSI14 `65.826567`、下影占振幅 `75%`；V6 该笔净 `+1.007642R`，V8 被盈利状态机提前保护为 `+0.494847R`。
- [ ] V6/V7/V8 均保持 `research_rejected_no_edge`，不进入 Paper/Live。下一轮若继续，只预注册退出规则的独立消融：优先研究 1R 前二次收盘确认或取消提前退出，以及 1R 后基于已完成收盘峰值的回撤保护；不继续叠加入场指标。

## 2026-07-23：量比 2.5 + 周 P90 放量锚点 RSI 背离 V9

- [x] 新增独立 Research-only V9：历史锚点与当前比较点分别按自身时点通过过滤量比 `>=2.5` 和前 672 根 `vol_ccy` P90；使用最近 48 根内最近同方向极值锚点，不等待右侧三根。
- [x] V9 使用独立策略键 `market_filtered_volume_weekly_p90_anchor_rsi_divergence_15m_v1`，未覆盖 V3，也未进入 Paper/Live。
- [x] 同一冻结 Top60 下 fresh V3 基线已落库为 `17179`；最终 V9 已落库为 `17181`，共 4,961 笔、9,922 行明细，开平仓配对与平仓净 R 完整。
- [x] DOGE `2026-07-05 10:45 -> 17:30` 已触发，17:45 开多，净 `+1.487005R`；q/p 各自量比、`vol_ccy` 和 P90 已写入 `back_test_detail.id=2244857`。
- [x] V9 整体净 EV/PF 为 `-0.181400R/0.771945`，最大单币隔离回撤 `71.640724%`；比 V3 更差。
- [x] 新锚点背离 2,640 笔，净 EV/PF `-0.205270R/0.744091`、净和 `-541.912245R`；13 个月全部亏损，拒绝该通用入场假设。
- [x] V9/V3 聚焦测试、独立身份和 manifest 测试、二进制构建、`git diff --check` 与 2,000 行硬上限门禁通过。
- [ ] V9 保持 `research_rejected_no_edge`；若继续验证“只把 V3 量比改为 2.5”，必须保留 V3 严格背离并建立单变量未见窗口版本。

## 2026-07-23：锚点 RSI 背离下一根收盘确认 V10

- [x] 保留 V9 不变，新建独立 Research-only V10；底背离只接受紧邻下一根已完成 K 收盘严格突破 `p.high`，顶背离按 `p.low` 镜像，未立即确认则候选过期。
- [x] 确认 K 不参与 q/p、RSI、过滤量比、周 P90 或 ATR 计算；确认后只在再下一根 15m K 开盘成交，EMA/MACD/影线不得在 V10 独立开仓。
- [x] 同一 Top60、同窗口、同成本和 1% 风险回测已落库为 `back_test_log.id=17182`：454 笔、908 行明细、41 币，开平仓缺配和净 R 缺失均为 0。
- [x] 454/454 笔满足 `confirmation=p+15m`、`entry=confirmation+15m` 和方向性严格突破；预注册的 60 分钟同方向连接簇为 349 个有效市场事件。
- [x] V10 净 EV/PF `-0.063616R/0.917649`、胜率 `33.4802%`、Sharpe `-0.857140`、最大单币隔离回撤 `18.044188%`、净和 `-28.881840R`。
- [x] 相比 V9 的 2,640 笔锚点交易，V10 只保留 17.20%，EV/PF 明显改善但仍未转正；做多 `-0.023495R/PF 0.969279`，做空 `-0.099392R/PF 0.872476`。
- [x] DOGE `2026-07-05 17:30` 因 17:45 收盘 `0.07553 < p.high 0.07577` 被正确淘汰；首次收盘突破在第 5 根 18:45，不能按单笔事后盈利修改 V10。
- [x] V10 专项 6 项与策略族 57 项测试通过，局部格式与 2,000 行硬上限通过；完整结果见 `docs/MARKET_FILTERED_VOLUME_ANCHOR_RSI_NEXT_CLOSE_CONFIRMED_15M_V10_RESULT.md`。
- [ ] V10 保持 `research_rejected_no_edge`，不进入 Paper/Live；下一轮如继续，只把确认突破幅度作为预注册新假设，并使用未见窗口验证。

## 2026-07-23：锚点影线立即入场 / 下一根盘中触价 V11

- [x] 保留 V9/V10 不变，新建独立 Research-only V11；底背离锚点为方向性长下影、顶背离锚点为方向性长上影时，在锚点完成后按紧邻下一根开盘成交。
- [x] 非方向性长影锚点只观察紧邻下一根 15m K：做多严格突破 `p.high`、做空严格跌破 `p.low` 即成交；跳过触发价时按下一根开盘成交，未触发则立即过期。
- [x] V11 逐笔冻结触发模式、锚点影线占比、触发价、触发 K、成交价与成交来源；同根止盈止损缺少 tick 路径时继续使用保守的止损优先口径。
- [x] V11 专项 6 项及过滤量比策略族 64 项通过，专用回测二进制构建、格式与 2,000 行硬上限检查通过。
- [x] 同一 Top60、同窗口、同成本和 1% 风险回测已落库为 `back_test_log.id=17183`：1,017 笔、2,034 行明细、41 币，开平仓与逐笔入场证据完整。
- [x] V11 净 EV/PF `-0.212154R/0.739640`、胜率 `29.1052%`、Sharpe `-4.538888`、最大单币隔离回撤 `26.2327%`、净和 `-215.760828R`；显著差于 V10。
- [x] 影线直接入场 339 笔为 `-0.227886R/PF 0.722744`，非影线盘中触价 678 笔为 `-0.204288R/PF 0.748783`；去掉全部不利的同根触价歧义后，乐观上界仍为负 EV。
- [ ] V11 结论为 `research_rejected_no_edge`，不进入 Paper/Live；提前成交解决了追晚问题，但也移除了 V10 下一根收盘确认的有效质量门禁。

## 2026-07-23：趋势止盈 + 持仓放量阶梯保护 V12

- [x] 完整继承 V11 入场，新建独立 Research-only V12；按信号时点已完成 K 线冻结长期 EMA 与平台破位趋势，价格单独穿越 EMA696 不构成趋势。
- [x] 顺势和中性沿用量比目标；逆势默认 `1 ATR`，仅在信号量比至少 4 且前 96 根沿趋势方向净变动至少 8% 时恢复量比目标。
- [x] 持仓完成 K 再次满足因果过滤量比 `>=2.5` 时，依次把止损推进到真实保本、`1 ATR`、`2 ATR`；旧止损/止盈先判断，新保护从下一根生效。
- [x] 修复框架只传 1 根 K 导致持仓量比永远未就绪的问题；窗口改为精确所需的 21 根，并用端到端测试固定止损更新和结构化证据落库。
- [x] 最终有效回测为 `back_test_log.id=17186`：1,020 笔、41 币、2,040 行明细、659 个有效市场事件；初始风险、净 R 和止损更新证据完整。
- [x] V12 胜率 `38.9216%`、净 EV `-0.227954R`、PF `0.644265`、Sharpe `-6.139853`、最大单币隔离回撤 `24.9450%`、净和 `-232.513076R`。
- [x] 135 笔发生至少一次动态保护，共 184 次更新；对共同 1,017 笔改善 `+6.147650R`，但新增释放的 3 笔交易均亏损，整体仍明显负期望。
- [x] 26 项 equity、5 项趋势策略、结构化止损证据和 legacy 兼容测试通过；构建、`cargo check`、格式和 2,000 行硬上限检查通过。
- [ ] V12 保持 `research_rejected_no_edge`，不进入 Paper/Live。`17184/17185` 是本轮无效中间记录，因删除未获单独授权暂时保留；查询只使用 `17186`。

## 2026-07-23：ATR 审计 + 普通逆势 1.5 ATR V13

- [x] 核对 ATR14 为标准 TR + Wilder RMA：首个完整窗口使用 14 根 TR 均值，后续按 `1/14` 平滑；冻结锚点 `p` 的已完成 K ATR，不读取未来 K。
- [x] 增加前收跳变、SMA 种子与 RMA 递推测试；审计 41 个交易币约 92.4 万根 K，非法 OHLC、非正价格、时间错位和重复 K 均为 0。
- [x] 发现 ACT/GIGGLE 两处长期断档，但断档恢复后 696 根预热期内交易为 0；加载器不拒绝断档仍是独立数据质量缺口，不在本轮改写共享输入。
- [x] 保留 V12，新增独立 Research-only V13；唯一参数变化是普通逆势目标 `1.0 ATR -> 1.5 ATR`，初始止损、入场、极端例外、中性/顺势目标与成本均不变。
- [x] V13 已落库为 `back_test_log.id=17187`：1,020 笔、2,040 行明细、41 币、659 个有效市场事件；1,020 笔成交价、初始止损、状态和趋势关系与 V12 全部一致。
- [x] V13 净 EV/PF `-0.212383R/0.685039`、胜率 `35.7843%`、Sharpe `-5.483132`、最大单币隔离回撤 `23.4394%`、净和 `-216.630636R`；普通逆势组改善 `+15.882440R` 但仍为负期望。
- [x] 拆解 V12 中性 381 笔：317 笔实际为趋势冲突，64 笔才是无趋势；294 笔止损，88/120/156 笔在 1/2/4 根内止损，272 笔亏损中 134 笔从未达到 0.25R、69 笔曾达到 1R 后回吐。
- [x] 聚焦测试、V13 风险合同测试、CLI check、二进制构建、`git diff --check` 和 2,000 行硬上限通过；结果见 `docs/MARKET_FILTERED_VOLUME_ANCHOR_RSI_TREND_MANAGED_COUNTER15_15M_V13_RESULT.md`。
- [ ] V13 保持 `research_rejected_no_edge`，不进入 Paper/Live；若继续，先分别预注册冲突状态拆分、无二次放量的峰值回撤保护和 K 线连续性门禁。

## 2026-07-23：混合策略拆分为三个独立策略家族

- [x] 保留 V9～V13 身份与历史结果不变，新增三个互斥的 Research-only 策略键、规则版本、预设和评估清单。
- [x] 动量衰竭反转只读取 96 根净移动、当前过滤量比/周 P90 与影线/下一根触价；RSI、MACD、EMA 和平台不参与决策。
- [x] 放量锚点 RSI 背离只读取最近合格 q/p 锚点、价格极值、RSI 与影线/下一根触价；96 根净移动、MACD、EMA 和平台不参与决策。
- [x] 放量平台破位趋势只读取平台、破位 K、两根接受确认、EMA 顺序与 EMA696 斜率；RSI、MACD、96 根净移动和反转影线不参与决策。
- [x] 三个家族共用同一执行基线：已完成 15m K、current-live Top60、量比 `>=2.5`、周 P90、实际成交价 `1.5 ATR14` 止损与止盈、固定毛 `1R`、48 小时、1% 风险、5/3 bps 成本。
- [x] 构建、格式、家族因果隔离、未来 K 隔离、manifest 身份与固定风险合同测试通过；主入口 1,998 行，未超过 2,000 行硬上限。
- [x] 三个有效落库 ID：动量衰竭 `17188`（2,515 笔/5,030 行）、放量锚点 RSI `17189`（1,020 笔/2,040 行）、平台破位趋势 `17190`（1,373 笔/2,746 行）；开平仓、初始风险与净 R 缺失均为 0。
- [x] 三者净 EV/PF 分别为 `-0.142040R/0.752168`、`-0.205382R/0.660656`、`-0.154756R/0.732361`；前后半段、多空和全部入场方式均未形成正优势。
- [x] 三个版本均定为 `research_rejected_no_edge`，不进入 Paper/Live，不部署、不触发真实交易。
- [ ] 任何 V2 必须冻结新的单一假设、精确事件聚类规则与未见样本；不得在当前已见窗口继续扫描阈值。

## 2026-07-23：动量衰竭反转 V2 + 水平平台破位趋势 V2

- [x] 保留两个 V1 不变，新增两个独立 Research-only V2 策略身份、规则版本、预设和查看结果前评估清单。
- [x] 动量 V2 只增加方向性影线极值限价 12 根与量比 ATR 目标；非影线仍仅观察紧邻下一根盘中触价，RSI/MACD/EMA 不参与信号。
- [x] 平台 V2 只替换平台质量定义：宽度使用 `ATR14[b-1]`，校验前后收盘重心、回归漂移以及上下沿分散触碰；V1 破位、接受、EMA 与 1R 合同保持不变。
- [x] 专项 11 项、格式、构建和 2,000 行硬上限通过；完整库 856 项通过，唯一失败为沙箱禁止 localhost 监听的既有 HTTP 测试。
- [x] 动量 V2 落库为 `17191`：2,096 笔、4,192 行明细、42 币、1,174 个有效事件；净 EV/PF `-0.187622R/0.754882`，胜率 `29.1985%`。
- [x] 平台 V2 落库为 `17192`：126 笔、252 行明细、34 币、103 个有效事件；净 EV/PF `-0.273161R/0.576139`，胜率 `42.0635%`。
- [x] 旧 EIGEN `2263912` 在 V2 下被明确拒绝：宽度 `4.1094 ATR`、重心偏移 `1.8264 ATR`、`R²=0.7094` 且拟合漂移 `2.4284 ATR`，上沿只有一次触碰。
- [x] 两个版本均定为 `research_rejected_no_edge`；结果归档到对应 `*_V2_RESULT.md`，未进入 Paper/Live、未部署、未触发真实交易。
- [ ] 如继续研究，动量家族必须把限价成交与动态止盈拆成独立消融；平台家族只研究新的入场/退出假设，不得在已见窗口放宽平台质量阈值。

## 2026-07-24：放量锚点 RSI 背离周期门禁 V2

- [x] 冻结 V1 与 `17189`，新增独立 Research-only V2；最近合格 q 固定后要求 q/p 中间至少 4 根完整 K，做多途中 RSI `>60`、做空途中 RSI `<40` 时锚点失效，边界 60/40 允许，缺失 RSI 失败关闭。
- [x] 明确最近 q 门禁失败后不得回退旧锚点；V2 不读取 96 根净移动、MACD、EMA、平台、布林带、BOS、FVG 或 CHoCH。
- [x] 专项 5 项、策略族 91 项、manifest 风险合同、CLI check、二进制构建、`git diff --check` 和 2,000 行硬上限检查通过。
- [x] 同一 Top60、窗口、成本、1% 风险和固定 `1R` 退出回测已落库为 `back_test_log.id=17194`：296 笔、592 行明细、40 币、230 个有效事件；开平仓、初始止损、初始风险和净 R 无缺失。
- [x] V2 胜率 `44.9324%`、净 EV/PF `-0.249429R/0.607387`、Sharpe `-4.262597`、最大单币隔离回撤 `10.704554%`，均未形成优势。
- [x] V2 的 296 笔是 V1 严格子集且共同交易净 R 完全一致；被保留组比删除组更差，证明退化来自入场筛选而非止盈止损。
- [x] 间隔门禁删除 711 笔 V1 成交，却保留最差的 16 根以上陈旧锚点组；RSI 重置只影响 13 笔实际成交，假设被否证。
- [ ] V2 定为 `research_rejected_no_edge`，不进入 Paper/Live；不得在当前已见窗口继续扫描 4、60、40 或最大间隔。

## 2026-07-25：TradingView RSI 背离收回门禁消融

- [x] 在同一 `OKX:ETHUSDT` 15分钟图、同一空头扩展退出下逐项关闭创新比例、ATR 创新、中间摆动与当棒收回门禁，确认交易骤减主要由“信号 K 线必须收回锚点”造成。
- [x] 冻结 Research 图表版 `rsi_divergence_structure_v2`：保留 `0.35%` 与 `0.5 ATR` 创新、`1 ATR` 中间反向摆动、RSI 50 中轴连续性、最近完整量能锚点和禁止回退，只把收回状态降为悬浮诊断。
- [x] 同图结果由硬收回的 6 笔、`+47.66 USDT / PF 1.9301` 恢复为 11 笔、`+186.10 USDT / PF 3.1738`；最大回撤由 `30.90` 增至 `52.35 USDT`，未计手续费与滑点。
- [x] TradingView 编译 0 错误，编辑器源码与本地 Pine 逐字符一致；入场标签审计中没有 `2026-07-13 22:15` 的 7 根 / 0.29% 背离信号。
- [ ] 该结果仍是单币种、单窗口、11 笔 Research 样本；在冻结版本完成 BTC、ETH、其他币种及未见月份验证前，不进入 Core、Paper 或 Live。

## 2026-07-25：20根放量锚定区间上破做多 V1

- [x] 冻结独立 Research 身份 `volume_anchor_range_upside_break_long_15m_v1`：只使用突破棒前20根高低点、阳线收盘上破、过滤后量比 `>=3`、周P90、`EMA12 > EMA144 > EMA696` 与 `RSI < 70`。
- [x] 首个合格上破产生一次做多候选并同步启动既有8根失败观察；观察期内继续冲高不重复开多、不移动边界、不重置倒计时，后续有效失败空单仍可平多反手。
- [x] 单独命中时使用 `1.5 ATR` 止损和突破棒量比对应的 `2.7 / 3.6 / 4.5 ATR` 止盈；悬浮信息展示冻结区间、突破收盘、量比、EMA/RSI门禁与版本身份。
- [x] 目标 ETH 样本在 `2026-07-14 18:45` 确认，`19:00` 以 `1800.16` 模拟成交并在 `1823.78` 达到 `4.5 ATR` 目标；`18:30` 因量比仅约 `2.62x` 被明确拒绝。
- [x] 当前同图共6个本分支独立入场、2胜4负，直接路径 `+17.60 USDT / PF 1.5575`；组合由11笔、`+186.10 / PF 3.1738` 变为16笔、`+211.72 / PF 2.9395`，最大回撤由 `52.35` 增至 `59.00 USDT`。
- [x] TradingView 编译0错误，本地与编辑器源码逐字符一致；策略规范、主流程图、同图对照和 Research 风险边界已同步。
- [ ] V1 保持 `research_only_not_promoted`：当前固定1单位、手续费/滑点为0且来自已见 ETH 样本；先完成费用后、风险归一化、BTC/ETH/其他币种、多月份和未见窗口验证，再讨论 Paper/Core/Live。

## 2026-07-25：大型上升三角放量突破做多 V1

- [x] 冻结独立 Research 身份 `volume_large_ascending_triangle_break_long_15m_v1`，不覆盖20根区间突破，不进入 Core、Paper、Live 或生产默认消费路径。
- [x] 只检查突破棒前 `96 / 120 / 144 / 168 / 192` 根已完成K线并取最长有效窗口；三段峰差 `<=0.5%`、上沿至少3组且间隔8根、低点逐段抬高、总抬高至少1 ATR、末段/首段宽度 `<=70%`。
- [x] 入场冻结为前一根RSI `<70`、当前RSI `70～80`、EMA12当前刚上穿EMA144、EMA144高于EMA696、量比 `>=3` 且周P90通过；信号在突破棒收盘确认，下一根开盘成交。
- [x] `2026-07-10 09:45` 目标样本确定性命中：192根窗口、阻力 `1763.94`、三段峰差 `0.24%`、4组触碰、收敛比例 `63.25%`；下一根以 `1767.79` 模拟成交并在 `1798.79` 止盈。
- [x] 新鲜同图消融：关闭分支为16笔、`+211.72 / PF 2.9395`；开启后为17笔、`+242.72 / PF 3.2235`，最大回撤均为 `59.00 USDT`。
- [x] TradingView 编译0错误；目标箭头、诊断标签和悬浮信息已验证，策略规范及研究记录已同步。
- [ ] V1 保持 `research_only_not_promoted`：目前只有1笔已见ETH样本且手续费/滑点为0；下一步必须完成费用后风险归一化、未见月份、BTC/ETH/其他币种和参数邻域验证。

## 2026-07-25：TradingView 弱背离收回 + 背离反转退出 V3

- [x] 冻结版本 `rsi_divergence_weak_reclaim_regime_exit_v3`：最近完整量能锚点仍只取 `t-5～t-32` 且禁止回退；仅 `q/t` 相隔 `5～7` 根的弱背离必须在信号棒收盘收回锚点极值。
- [x] 相隔 `8～32` 根并通过既有价格创新、ATR 独立摆动与 RSI 中轴连续性门禁的完整背离继续有效，避免把 ETH 的完整结构样本一刀切删除。
- [x] 背离反转与趋势延续退出拆分：严格逆势继续使用冻结横盘结构目标；中性/过渡制度的纯背离反转在 `1R` 后近似保本并于 `1.5R` 全平；严格同向 EMA 趋势继续使用原 ATR 延续目标。
- [x] 同图同窗固定1单位、零手续费/滑点复测：BTC 从 `-125.70 / PF 0.9604 / 10笔` 改善到 `+1296.10 / PF 1.5600 / 9笔`；ETH 保持 `+249.86 / PF 3.6474 / 16笔`。
- [x] BTC 目标子样本中，未收回的 5 根弱顶背离被过滤；保留的完整顶背离从原 `-375.30` 止损改为 `+562.90` 的 `1.5R` 止盈。
- [x] TradingView 编译0错误，编辑器与本地源码逐字符一致，临时诊断标签为0，Pine文件1404行且未使用 `volume × close`。
- [ ] 版本保持 `research_only_not_promoted`：当前是已见同图样本且未计交易成本；必须用费用后风险归一化、未见月份和更多币种验证后，才能讨论 Paper/Core/Live。

## 2026-07-25：突破多单接受期普通超买空单保护 V1

- [x] 新增 Research-only 版本 `post_breakout_long_acceptance_short_guard_15m_v1`；仅覆盖20根确认箱体、大型水平箱体和大型上升三角产生的真实突破多单。
- [x] 入场信号确认时冻结突破线；同棒多个结构取最高有效线。后续仅在确认收盘 `<=` 冻结线，或既有第二次扫高并收回结构成立时解除，影线触碰不解除。
- [x] 保护只拦普通 RSI 超买吞没/长上影空单；RSI 顶背离、EMA 空头、锚区假突破、重复扫高假突破和 EMA 压缩扩张空单保持独立。
- [x] 同图诊断中 ETH 出现4次保护启动、1次扫高解除和2次收盘跌回解除；BTC未出现保护启动。两个窗口均没有普通超买空单落在有效保护期内，因此成交结果不变。
- [x] 固定1单位、零手续费/滑点同窗复测：BTC 保持 `+1296.10 / PF 1.5600 / 9笔`，ETH 保持 `+249.86 / PF 3.6474 / 16笔`；未触发回滚门槛，但也没有样本内收益提升证据。
- [ ] 版本保持 `research_only_not_promoted`：需在未见窗口捕获真实受保护空单，并加入费用、滑点和风险归一化后再判断该保护是否具有收益价值。

## 2026-07-26：TradingView 布林带迁移到 K 线主图

- [x] 主图 Research 策略新增价格尺度的 `BOLL(20,2)` 上轨、中轨、下轨与轻量填充，直接覆盖在 K 线图上。
- [x] 独立副图移除全部 BOLL 计算、曲线、分区线与表格字段，只保留 RSI、MACD 和按 `vol_ccy` 直接使用的成交量。
- [x] 两份 Pine 在 TradingView Desktop 编译均为0错误，保存后的编辑器源码与本地文件逐字符一致。
- [x] BTC 当前图表仍为6笔、净利润 `-662.40 USDT`、PF `0.554988`，与迁移前基线一致；未修改任何交易信号、退出或风控逻辑。
- [x] 主图与副图分别为1个实例，截图确认 BOLL 位于 K 线价格窗格，副图不再包含 BOLL。
- [x] 将副图原有但不清晰的 RSI 50 灰色点线强化为蓝色2像素实线；真实 RSI=50 继续映射到组合副图坐标75，指标计算与策略结果不变。

## 2026-07-26：开仓价 ±1 ATR 主图风险范围

- [x] 仅在 Research 主图持仓期间显示实际开仓均价、`开仓价 + 1 ATR`、`开仓价 - 1 ATR` 与淡色填充；多仓使用绿色，空仓使用红色，平仓后隐藏。
- [x] ATR14 冻结为下单信号 K 线收盘时的已确认值，中心线使用下一根实际模拟成交价，避免把开仓后 K 线的高低波动回填进入场风险尺度。
- [x] 未修改任何信号、止损、止盈、仓位或订单语义；BTC 当前图仍为6笔、净利润 `-662.40 USDT`、PF `0.554988`。
- [x] TradingView 编译0错误、离线静态检查0问题，编辑器源码与本地 Pine 逐字符一致；文件1493行，低于2000行硬上限。

## 2026-07-26：放量卖出高潮后三棒强势反包做多 V1

- [x] 将截图前提校正为“前三棒净下跌且至少两根阴线”：`2026-07-17 21:15` 实际为小阳线，不把视觉判断伪装成三根连续阴线。
- [x] 冻结独立 Research 身份 `volume_three_bear_bullish_engulfing_reversal_long_15m_v1`；当前强阳必须吞没三棒实体包络，并通过 75% 实体、1.5 ATR、顶部10%收盘、卖出高潮量能、48根底部、EMA 下跌结构和 RSI 穿回50。
- [x] 信号棒收盘确认，下一根开盘使用独立 `Long 3Bar` 订单；四棒低点止损，1R 后近似保本，1.5R 全平，不回退逆势横盘目标或旧 ATR 档位。
- [x] 同币种同窗开关消融：BTC 从6笔、`-662.40 / PF 0.555` 改善到7笔、`+265.00 / PF 1.178`；新增交易 `63154.7 → 64082.1`，贡献 `+927.40 USDT`。
- [x] ETH 开关前后均为14笔、`+331.42 / PF 8.709`，新增样本为0；TradingView 编译0错误，主 Pine 1602行，无行尾空白。
- [ ] 状态为 `research_only_not_promoted_insufficient_cross_symbol_samples`：保留独立研究分支，但不能宣称 ETH 已验证盈利；须补费用后风险归一化、未见月份和更多币种样本。

## 2026-07-26：TradingView 当前策略同步 Rust 与等价审计

- [x] 冻结当前 Pine 身份：本地与编辑器均为 FNV-1a 32 `66d3937e`；TradingView BTC 基线为7笔、`+265.00 / PF 1.1780 / DD 976.20`，ETH 为14笔、`+331.42 / PF 8.7092 / DD 22.06`。
- [x] 在 `15min_velocity_all_sybol_strategy.md` 增加当前信号总表、20根确认箱体接受 V2、三棒反包、Pine/Rust 执行合同、多窗口结果、首差异层和四层认知审计；历史版本与当前规则分开标记。
- [x] 新增独立 `tradingview_velocity_parity_15m_research_v1`：复刻 Pine EMA/RMA/RSI/ATR、量能事件、14个入场家族枚举、状态门禁、下一根开盘、反手、保护位、动态保本和默认 OHLC broker path。
- [x] BTC 30天7笔逐笔 exact parity，净利润、PF 和最大回撤全部一致；ETH 最后11笔可见交易一致，完整结果差异可归因于图表隐式加载边界和末笔 `0.03 USDT` 价格差。
- [x] 修正三个实现口径：EMA596/696 使用60天预热；RSI 忽略首根缺失 `ta.change`；最大回撤使用 TradingView“已平仓峰值 + 持仓不利价格偏移”公式。
- [x] 最终聚焦测试 `13 passed`、CLI 编译和 scoped rustfmt 通过；BTC/ETH 30/60/90天零成本与每边 `5bp + 3bp` 成本压力均已输出。
- [x] 迁移 manifest 与 evidence 已归档；只读 OKX 公共行情，不写数据库、不注册 Paper/Live、不触发交易所 mutation，旧 V1～V13 保持不变。
- [ ] 状态保持 `research_only_not_promoted`：BTC 成本后30/60天为负且90天 PF 仅 `1.317`；下一步必须预先冻结未见窗口、风险归一化、更多币种和组合资金模型，禁止在本轮已见交易上继续补阈值。

## 2026-07-26：高位放量努力无结果 + 次高点布林拒绝做空 V1

- [x] 在读取结果前冻结独立 Research 身份 `volume_effort_no_result_lower_high_boll_reclaim_short_15m_v1`，不覆盖 RSI 顶背离或普通超买空单。
- [x] q 固定为 `t-20～t-80` 内最高高点，同高取最近、失败不回退；严格实现 `0.5% OR 0.5 ATR`、`1.25×vol_ccy`、RSI衰减、强阴收低和布林上轨收回。
- [x] t 收盘确认、下一根开盘使用独立 `Short ENR`；止损为两高较高者上方1 tick，1R激活近似保本、1.5R全平。
- [x] 保留同棒多空冲突和既有冻结突破线保护；保护解除后使用独立风控旁路逆势横盘目标门禁。
- [x] TradingView 编译0错误；BTC 新增1笔 `66632.5 → 66148.5`，组合由7笔 `+265.00 / PF 1.1780` 改善到8笔 `+749.00 / PF 1.5032`，最大回撤保持 `976.20`。
- [x] ETH 仍为14笔 `+331.42 / PF 8.7092 / DD 22.06`，没有新增样本；规则、结果、四层认知和源码身份 `403adb2b` 已归档。
- [ ] 状态为 `research_only_not_promoted_insufficient_cross_symbol_samples`：Rust parity仍冻结旧源码 `66d3937e`；后续须独立同步并执行成本后、多窗口、跨币种和参数邻域验证。

## 2026-07-26：放量布林下轨收回做多 V1

- [x] 在读取结果前冻结独立 Research 身份 `volume_bollinger_lower_reclaim_long_15m_v1`，不修改现有 RSI 超卖反转。
- [x] 完整实现量能事件、下轨收回、50%下影、顶部75%收盘、前48根底部15%、RSI 35～50连续回升、MACD负柱连续收缩和信号收盘 `>=1.1R`。
- [x] 信号收盘确认、下一根开盘使用独立 `Long BLR`；止损冻结在信号低点下方1 tick，中轨目标冻结并100%全平。
- [x] 旁路无关逆势横盘目标门禁，保留同棒多空冲突；不复用量比至少3的旧 ATR 目标，也不启动突破线保护。
- [x] `2026-07-09 11:15` BTC 逐项诊断完成：量能、布林、形态、RSI、MACD和 `1.2152R` 均通过，前48根底部位置 `16.95% > 15%` 为唯一失败项，因此无入场。
- [x] BTC 与 ETH 均为 `0` 个 BLR 原始信号、`0` 笔成交；总策略分别保持 `+749.00 / PF 1.5032` 与 `+331.42 / PF 8.7092`，不能宣称新分支正收益。
- [x] 最终 Pine 为1830行、FNV-1a 32 `90c8fc84`，TradingView 写入1831行并编译0错误；临时诊断标签已清除。
- [ ] 状态为 `research_only_not_promoted_no_samples`：保持规则冻结，后续只能扩展预注册历史窗口后再评估成本后 EV/PF/回撤，不能围绕目标棒把15%放宽到17%。

## 2026-07-26：EMA596 收复后放量 HH/HL 离轨做多 V1

- [x] 在读取收益前冻结独立 Research 身份 `volume_ema596_reclaim_departure_hh_hl_long_15m_v1`；旧多头候选优先，不覆盖既有策略家族。
- [x] 实现最近32根收复并持续站稳EMA596、`0.5 → 1.0 ATR`离轨转换、前4根高点突破、HH/HL、周P90与前20根中位数2.5倍双量能、60%强阳线。
- [x] 信号收盘确认、下一根开盘使用 `Long EMA596-D`；前4根结构低点下方1 tick止损，信号收盘冻结2R目标并100%全平。
- [x] `2026-07-02 16:00` BTC 审计完成：旧规则因上穿已过去26根、实体不足1%和过滤后量能失败未入场；新分支在16:15以`60309.7`成交，`60095.6`止损，`60737.8`目标在17:30止盈。
- [x] BTC 由8笔`+749.00 / PF 1.5032 / DD 976.20`变为11笔`+1548.70 / PF 1.5854 / DD 2133.20`；新分支3笔、2胜1负、`+799.70`。
- [x] ETH 由14笔`+331.42 / PF 8.7092 / DD 22.06`变为18笔`+244.85 / PF 2.7006 / DD 83.12`；新分支4笔、1胜3负、`-86.57`。
- [x] 最终 Pine 编辑器1914行、UTF-16长度113685、FNV-1a 32 `26058470`；本地/编辑器哈希一致且TradingView编译0错误。
- [ ] 状态为 `research_only_not_promoted_cross_symbol_failure`：Rust parity继续冻结`66d3937e`；后续只能在预注册未见窗口做成本后、1%风险、BTC/ETH/其他币种与市场事件聚类验证，禁止围绕当前结果调阈值。

## 2026-07-27：EMA596 收复接受后二次加速做多 V1

- [x] 新增独立 TradingView Research 身份 `volume_ema596_reclaim_acceptance_reacceleration_long_15m_v1`；不覆盖原 EMA596 离轨分支，不修改 Core、Paper、Live 或真实下单。
- [x] 初次收复新增贴线、适度离轨、解除完整空头排列和 2 ATR 风险门禁；接受后二次加速要求持续站稳 8～24 根、均线过渡、慢线收敛、再放量和 HH/HL 结构。
- [x] BTC `2026-06-22 09:15` 过度离轨信号被过滤；`19:00` 信号收盘确认，下一根 `19:15` 以 `64308.1` 开多，`20:00` 于 `64771.6` 完成 2R 止盈，毛收益 `+463.50 USDT`。
- [x] ETH 同窗 0 笔：初次收复候选在当前距离合格后全部卡在双量能，二次加速候选在当前距离合格后全部卡在均线过渡与收敛。
- [x] Pine 编译 0 错误；本地与编辑器统一 LF 后均为 6281 字符、FNV-1a 32 `86af0424`；新脚本已独立保存，原 1914 行 Research Chart 已恢复。
- [ ] 状态为 `research_only_not_promoted_insufficient_cross_symbol_samples`：只有 BTC 一笔已见样本，ETH 无样本，且未计交易成本；后续须预注册未见窗口、参数邻域和成本压力验证。

## 2026-07-27：EMA596 收复接受后放量 HH/HL 离轨做多 V2

- [x] 复现 ETH `2026-07-24 15:00`：V1 将 `reclaimAge=0` 和前棒 `-1.3295 ATR` 的负离轨错误计入“收复后”；信号前 EMA144 / EMA596 仍为强负斜率。
- [x] 新增独立 Research V2：上穿至少发生在1根前，前棒必须位于 EMA596 上方 `0～0.5 ATR`；信号前两条慢线同时强负时阻断，V1 不覆盖。
- [x] 目标棒不再产生 `Long EMA596-D2`；反事实沿 V1 下一开盘追入会先触结构止损，固定1 ETH 毛亏约 `-29.73 USDT`。
- [x] TradingView 保存后重载仍为 V2，主图 Pine 编译0错误；BTC 为9笔 `+1177.10 / PF 1.7908 / DD 976.20`，ETH 为15笔 `+345.84 / PF 9.0447 / DD 40.84`。
- [x] 相对无该分支基线，V2 在 BTC / ETH 分别增加 `+428.10 / +14.42`；但相对 V1 的 BTC 净利润下降 `371.60`。
- [ ] 预注册的“双币净利润均不下降”门槛失败，状态保持 `research_only_not_promoted_preregistered_gate_failed`；Rust / Paper / Live 不切换，下一轮须在未见窗口分离验证接受门禁与慢线斜率门禁。

## 2026-07-27：当前 Pine `3cbbc9d8` 同步 Rust V2 与 Top60 数据审计

- [x] 保留 `tradingview_velocity_parity_15m_research_v1@66d3937e`，在同一 Research parity 模块以独立 `Current3cbbc9d8` 规则版本新增 ENR、BLR 与 EMA596-D2，并复刻候选优先级、下一根开盘、独立保护位和退出政策。
- [x] BTC 30天 V2 与当前 TradingView 一致：9笔、`+1177.10 / PF 1.790796 / DD 976.20`；旧 V1 同窗仍为7笔、`+265.00 / PF 1.178032`，未被覆盖。
- [x] ETH 可见逐笔规则、时序与成交一致；Rust 60天16笔 `+318.98` 与 TradingView 15笔已平仓 `+345.81` 的差异全部落在图表隐式历史边界，边界归一后首个规则差异为0；图表另有未平仓空单。
- [x] `cargo test -p rust-quant-cli tradingview_velocity_parity --lib` 29项、Top60 binary测试2项及两个 Research binary `cargo check` 均通过；scoped rustfmt完成。
- [x] Top60 runner 固定 manifest `3fd267ca…a4f`、`vol_ccy`、60天预热和完整覆盖 fail-close；默认严格模式在原始结束时点为0/60并阻塞，不生成伪完整报告。
- [x] 显式部分诊断只有14/60，窗口截到 `2026-06-30 15:45 UTC`；零成本1733笔、PF `1.0552`、平均 `-0.03893R`，每边5+3 bps事后成本压力为 PF `0.7673`、平均 `-0.21341R`。
- [x] 最大新文件 `signals.rs` 为1544行，低于2000硬上限但超过1000目标；仓库指定行数脚本不存在，使用 `wc -l` 兜底并登记拆分债务。
- [x] 按用户口径从 OKX 当次 `current-live` 快照排除已退市成员并冻结新 cohort；复用既有历史月包与增量 REST backfill，向本地 `quant_core` 写入/更新 `116334` 根15分钟 K 线，17个成员完成尾段缺口修复。
- [x] 重新审计达到 `sealed=true`、`60/60`：每个成员5760根预热、36826根评价 K 线，合计2555160根，原始 tick、`confirm='1'`、`vol_ccy`、首尾和15分钟连续性全部通过。
- [x] 正式双成本报告已生成：零成本7687笔、PF `1.1431`、平均 `-0.00044R`；每边手续费5bps+滑点3bps后 PF `0.8347`、平均 `-0.16292R`、12/60币盈利。成本路径只改变净值指标，逐币交易数、阻塞数和结束状态一致。
- [x] 状态更新为 `research_only_not_promoted_full60_cost_gate_failed`：数据门禁已解除，但成本后期望、PF和跨币覆盖不满足职业级晋级标准；未注册 Paper/Live、未部署、未触发真实交易。

## 2026-07-28：EMA压缩扩张结构破位与多头死叉保护 V3

- [x] 保留 V1 `66d3937e`、V2 `3cbbc9d8`，新增独立
  `tradingview_velocity_parity_15m_research_v3@7827654b`；strict Top60 默认仍为
  V2，V3 必须显式选择。
- [x] EMA压缩扩张空要求信号收盘严格跌破 `t-20～t-1` 冻结低点；未破位
  状态不触发上升沿，也不消耗12根冷却。
- [x] MACD 柱在 `t/t-1/t-2` 任一根由非负转负时，阻断 RSI底背离多与
  EMA压缩扩张多；四根柱值不完整时失败关闭，不影响任何空单家族。
- [x] RSR `06-15 02:30` 与 BCH `07-16 07:30` 两笔 `-1R` 已过滤；
  SATS `07-17 06:45` 实际为空单，满足结构破位且死叉支持空头，按设计保留。
- [x] Pine 在 TradingView 编译0错误；Rust parity lib 58项、三个 CLI
  共6项测试和全量 rustfmt 检查通过。
- [x] 同一冻结60/60报告：V3 零成本7318笔、`+73.62R`、平均
  `+0.01006R`、PF `1.117`；相对V2净R改善 `+77.04R`，但PF下降且最大回撤
  扩大。
- [x] 每边5bps手续费+3bps滑点后，V3为 `-1107.31R`、平均
  `-0.15131R`、PF `0.819`、11/60盈利；相对V2少亏 `145.02R`，仍未获得
  可晋级的正收益。
- [ ] 状态为 `research_only_not_promoted_mixed_ablation_cost_gate_failed`：
  V3 不替换 V2 默认入口；后续将 EMA 空单末端追空与底背离二次确认拆成独立
  假设，禁止继续围绕三笔已见样本叠加阈值。

## 2026-07-28：逆势1R移动保护、fresh结构目标与背离K线门禁 V4

- [x] 冻结独立 `tradingview_velocity_parity_15m_research_v4@9ab73288`；
  V1～V3 Pine 快照、规则入口与历史报告不覆盖。
- [x] 所有逆势结构仓使用实际下一开盘到初始止损定义不可变1R；完成棒达到
  1R后，下一棒启用保本1 tick与距最佳有利价1R的单向移动保护。
- [x] 最近有效横盘首次破位7根内使用结构短目标；信号前96根位移同时达到
  6 ATR和5%时放大到远边；最近横盘失效时不回退更老横盘。
- [x] RSI底/顶背离分别要求方向实体或至少25%且不少于2 tick的方向影线。
- [x] 修正 fresh空单与 transition sweep 同棒时的目标、止损优先级；
  Rust focused tests 67/67、CLI tests 4/4、rustfmt和Pine编译均通过。
- [x] schema 2 V3重跑与旧V3聚合、逐币、逐笔完全一致；V3/V4报告均通过
  同 manifest、同市场数据、60/60和逐笔 `exit_policy` 门禁。
- [x] V4-ABC 零成本为7153笔、`+50.76R / PF 1.167 / 胜率36.66%`；
  压力成本为`-1097.57R / PF 0.834`。回撤下降，但净R未改善且成本后仍负。
- [x] 逆势结构压力成本少亏18.23R，原始止损1051→635；RSI底背离少亏，
  但顶背离零成本净R由`+61.14R`降至`+5.48R`。
- [ ] 状态为 `research_only_not_promoted_abc_cost_gate_failed_ablation_pending`：
  保留独立Research，不注册Paper/Live；后续先做A/B/C单项消融和多空分离，
  再补不在冻结60成员中的ETH固定快照。

## 2026-07-28：未来架构与迁移文档一致性收敛

- [x] 固定“无 Core/平台自营账户”；平台固定 API Key 仅属于 Market 公共只读数据能力，不得进入用户账户、私有流、Risk、Execution 或 mutation。
- [x] 统一 ResearchBar、Paper simulated OMS 与 live Execution 的 Context、planning value、Aggregate、Evidence 和 promotion 语义。
- [x] 固定 `ExchangeAccountRef`、Account 私有流唯一归属、claim fence/expiry、Market resolved evidence、外部仓位风险占用与 SafetyMonitoring/Ack 闭合协议。
- [x] 将 Market Velocity C1 拆成 Web binding、Market evidence、Account admission、Exchange capability、Risk valuation、Safety monitoring、Risk approval 与 Execution recovery 的唯一 Owner DAG。
- [x] 补齐 machine Registry、Manifest/Evidence 模板、Verdict 依赖、Vegas V0～V12 映射以及架构 Skill 的实施护栏。
- [x] 完成 TOML/DAG/共享 Contract、V0～V12、Markdown 链接、冲突标记与 whitespace 检查。
- [ ] 本轮只冻结未来迁移设计；Owner 子 Manifest、实现、Evidence、Promotion/Cutover 与生产 mutation 授权均未开始，后续必须逐 Verdict 推进。

## 2026-07-28：V4 RSI背离亏损分型与TradingView目检

- [x] 导出全部压力亏损：底背离286笔、顶背离318笔，并区分真亏损与仅成本转亏。
- [x] 完成V3/V4共同交易迁移审计；确认顶背离恶化主因是逆势空单1R追踪截断，
  不是K线门禁。
- [x] 在TradingView可见历史窗口内目检YGG、NMR、ADA、ICP、ETC六个代表样本，
  区分入场失败、退出过紧和成本型亏损。
- [x] 预注册按方向、EMA市场状态、结构确认和成本门槛互斥分桶的最小消融顺序。
- [ ] 本轮只读诊断，不改Pine/Rust策略、不启动新回测；下一步需用户确认后执行
  A/B/C独立消融，V4仍为Research且不晋级。

## 2026-07-28：纯 RSI 背离 EMA 排列年龄 Research V5

- [x] 冻结独立 V5 `a36f0e19`，保留 V1～V4；EMA 严格排列年龄包含信号棒，
  中断归零并封顶 600。
- [x] 中性/过渡纯 RSI 禁止独立开仓；严格顺势沿用 V4；同棒独立信号家族
  不受影响。
- [x] 1～599 根严格逆势使用冻结横盘近边及 V4 的 1R 保护；>=600 根恢复
  远边目标，结构收盘确认后移至固定 8bps/side 净保本，确认且 MFE>=2R
  后才启用 2R 宽追踪。
- [x] Pine 在 TradingView 编译 0 错误；Rust parity 76 项、命令入口 4 项
  测试和全量格式检查通过。
- [x] sealed Top60 达到 60/60、60 天预热和 2,555,160 根正式覆盖；V4/V5
  schema 3 报告与重复运行结果一致。
- [x] V5 压力净 R `-968.01R`，相对 V4 少亏 `129.56R`，但 PF
  `0.834→0.810`、回撤 `21244.73→21954.31` 均恶化。
- [x] 中性门禁使纯 RSI 合计少亏 `133.70R`；1～599 近边目标对多单改善、
  对空单恶化，合计仅 `+1.14R`。
- [ ] 状态为
  `research_only_not_promoted_cost_gate_failed_mature_branch_unobserved`：
  >=600 分支 0 笔、BTC 恶化、ETH 未纳入 cohort；不改 600 阈值，不进入
  Paper/Live，不部署。

## 2026-07-28：当前亏损归因与 EMA 趋势多结构接受 V6

- [x] 按最终唯一信号家族重新归因 V5 的 `6,662` 笔交易；压力成本亏损
  `4,427` 笔，其中真亏损 `4,222` 笔、仅成本转亏 `205` 笔。
- [x] 冻结选择规则：样本不少于 100 笔时累计压力亏损最大的
  `ema_trend_long` 为唯一优化对象；V5 为 `1,070` 笔、`-272.21R`。
- [x] 在 TradingView 目检 ATOM、LTC、FLOW 和 RSR 对照，确认主要失败
  形态为极端量能末端扩张、局部突破撞大级别供应、慢均线未成熟和追价确认。
- [x] 预注册并实现独立 V6：20 根高点突破来源、1～3 根接受窗口、冻结
  来源 ATR/量比/目标，Pine 与 Rust 保持相同状态机。
- [x] 修正 V6 接受棒非独立 RSI 候选污染家族归因和绝对止损的问题，并新增
  回归测试。
- [x] Pine 编译 0 错误；Rust parity `82/82`、strict Top60 `4/4` 通过。
- [x] sealed Top60 达到 60/60、每币 60 天预热、`2,555,160` 根正式覆盖。
- [x] V6 全策略压力净 R 改善 `172.16R`，但仍为 `-795.85R`；纯
  `ema_trend_long` 从 `-272.21R` 变为 `-92.28R`，同时平均 R
  `-0.2544→-0.2866`、PF_R `0.690→0.656`，目标质量门禁失败。
- [x] 主 Pine 恢复为 V5 `a36f0e19`；V6 仅保留冻结快照、Rust 候选和否证
  报告。
- [ ] 状态为 `research_only_rejected_target_quality_gate_failed`：不进入
  Paper/ReadOnly/Live。下一步先补逐笔来源/确认审计字段，再在未见窗口研究
  真实回踩收回、慢均线正斜率、48/96 根结构空间和极端量能独立分支。

## 2026-07-28：RSI 反向长影门禁 Research V7

- [x] 核对 ATOM 时间并将用户口述 `05:45` 纠正为截图实际 `07:45`。
- [x] 冻结信号 OHLC、`72.73%` 上影和既有 `60%` 对称反向长影门禁。
- [x] 创建 V7 预注册评估清单，明确只影响四个 RSI 家族。
- [x] 同步 Pine/Rust 候选，实现 ATOM、镜像、恰好 60% 和低于 60% 测试。
- [x] TradingView Pine 编译 0 错误，并确认 V7 候选不再标记目标 ATOM 多单。
- [x] sealed Top60 V5/V7 同源比较达到 60/60，共有 6,642 笔逐笔漂移为 0。
- [x] V7 删除 20 笔零成本 `+7.5791R`、压力成本约 `+2.9329R`；压力净 R
  与平均 R 恶化，按预注册规则拒绝。
- [x] 主 Pine 恢复 V5 `a36f0e19`；V7 仅保留 Research 快照、Rust 候选
  和正式报告，不进入 Paper/ReadOnly/Live。
- [x] 隔离 ATOM 7 月 17 日三棒阻力退出审计，本轮不修改 1R/1.5R 合同。
- [ ] 若继续，另立 48～96 根冻结历史阻力的三棒退出消融；未预注册和完成
  跨币种验证前不修改主退出规则。

## 2026-07-28：LTC 慢均线带收复空单门禁 Research V8

- [x] 核对 LTC `07-26 05:30` OHLC、量比、RSI、ATR 与 EMA596/696。
- [x] 纠正可视事实：紫线为 EMA596；目标棒重新收复整个慢均线带上沿。
- [x] 预注册 V8，只阻断普通 RSI 超买长上影空，隔离所有结构空单。
- [x] 同步 Pine/Rust，冻结 Pine `252225ec`，TradingView 编译 0 错误。
- [x] Rust focused `5/5`、完整 parity `91/91`、strict CLI `4/4` 通过。
- [x] TradingView LTC 快速对照：18→17 笔，净利润 -0.06→+0.17 USDT，
  PF 0.9822→1.0541，最大回撤不变。
- [ ] 按用户要求停止数据库复审；正式跨币种 60/60 结论延期，V8 只保持
  Research-only，不进入 Paper/Live。

## 2026-07-29：五类低质量入场结构接受 Research V10/V11

- [x] 冻结 V9 基线并按累计亏损/平均 R 选出 EMA 趋势多、压缩扩张多空、
  普通 RSI 反转和 EMA596 收复离轨多五个问题集合。
- [x] 新增 V10 状态机：冻结 20 根结构线、1～3 根回踩接受、RSI 离开
  30/70 极值区、压缩多空结构穿越和 EMA596 慢线/离轨门禁。
- [x] V10 43/60 诊断使全策略统一风险净值从 `-629.20R` 改善到
  `-106.85R`，但压缩多、普通 RSI 和 EMA596 仍负。
- [x] 在查看 V11 结果前冻结残差规则：压缩多来源量比 `>=3`、普通 RSI
  必须跨回 EMA12、压缩空接受 RSI `>=35`、EMA596 RSI `<=70`。
- [x] Pine V11 `53ba4291` 在 TradingView 编译 0 错误；Rust parity
  `98/98` 通过。
- [x] V11 五项集合达到 `160 笔 / PF_R 1.018 / +1.42R`，相对 V9/V10
  正向；全策略为 `-49.52R / PF_R 0.967`，仍未转正。
- [ ] 状态为 `research_only_partial_43_of_60_not_promoted`：普通 RSI
  仍 `-8.87R`，压缩多仅 1 笔亏损；不得继续在同一窗口追参或进入
  Paper/Live，下一步先做新时间窗口冻结验证。

## 2026-07-29：入场去耦 Research V12（拒绝）

- [x] 在读取 V12 收益前冻结 setup 与第 2～4 根确认、分阶段最多两个硬条件。
- [x] 新增 Rust `CandidateV12`、独立 Pine `34752685` 和同源 Top60 runner。
- [x] TradingView V12 编译 0 错误；Rust parity 相关 `99/99` 通过。
- [x] 43 个完整成员中，六个目标方向仅 `138 笔 / 126 事件`，未恢复样本；
  成本后 `PF_R 0.589 / -50.39R / -0.3651R每笔`，显著劣于 V11。
- [x] V12 冻结为失败诊断，当前 TradingView 主 Research 与本地主 Pine
  恢复 V11 `53ba4291`；未进入 Paper、ReadOnly、Live 或部署。
- [ ] 下一次不得继续在同一窗口微调统一确认期；应按家族分别提出独立证据，
  并使用新时间窗口或 walk-forward 验证。

## 2026-07-29：压缩扩张分阶段接受 Research V13（拒绝）

- [x] 在查看结果前冻结单变量假设：只把压缩释放、放量突破和回踩接受拆成
  setup→0～2 根 impulse→1～3 根 acceptance；首次 setup 冻结结构线。
- [x] EMA 趋势多、普通 RSI 多空和 EMA596 多完整复用 V11，并把逐笔身份
  一致列为实现正确性的硬门禁。
- [x] 新增 Rust `CandidateV13` 与 Pine `b81e5d25`；TradingView 编译
  0 错误，parity lib `100/100`、三个 CLI `7/7` 通过。
- [x] 同一 43/60 诊断中，四个冻结家族逐笔一致；压缩多从 1 笔降为 0，
  压缩空仍只有 MOVE 一笔 `+5.22R`，占家族毛利润 100%。
- [x] 六方向合计从 `160 笔/+1.42R` 变为 `159 笔/+2.53R`，全策略从
  `-49.52R` 变为 `-48.40R`；改善完全来自删除一笔亏损，不是样本恢复。
- [x] V13 判为 `rejected_research_diagnostic`，TradingView 主 Research
  已恢复 V11 `53ba4291`；无 Paper/Live、部署、数据库写入或交易 mutation。
- [ ] 下一版不再延长同一压缩状态的时间窗口；应重新定义可重复出现的
  “压缩制度事件”，并先在未见窗口做发生率统计，再决定是否继续该家族。

## 2026-07-29：无方向压缩制度 Research V14（拒绝）

- [x] 在读取结果前冻结 V14：12 根 EMA12/576 无方向压缩 setup、8 根
  impulse 窗口、1～3 根回踩接受，以及交易数/方向/币种/事件/集中度门槛。
- [x] Pine 快照 `45391eac` 与 Rust `CandidateV14` 同源；未覆盖 V11。
- [x] TradingView 编译 0 错误；Rust parity `101/101`、三个 CLI `7/7`
  通过；四个冻结家族与 V11 逐笔一致。
- [x] 同一 43/60 诊断中，压缩多 0 笔，压缩空仅 WIF/ICP 两笔且合计
  `-2.31R`；六目标方向降为 `PF_R 0.938 / -5.00R`。
- [x] 全策略从 V11 `-49.52R` 恶化为 V14 `-50.48R`；V14 判为
  `rejected_research_diagnostic`，TradingView 已恢复 V11 `53ba4291`。
- [ ] 不再在当前窗口继续放宽压缩阈值；下一步先冻结未见窗口并输出
  setup→impulse→acceptance 的纯发生率漏斗，再决定是否保留该家族。

## 2026-07-29：真实箱体突破接受与分阶段保护 Research V15（拒绝）

- [x] 在查看收益前冻结独立 V15：48 根真实箱体、双边独立触碰、区间包含
  与收缩、冻结放量突破、1～4 根回踩接受、结构止损和量度目标。
- [x] 实现实际成交后 1R 平 33%、收盘确认净保本、2R 后 1 ATR 单向追踪
  以及重新进入箱体退出；V11 主策略保持不变。
- [x] Pine `28f2817f` 编译 0 错误；Rust parity `108/108`、Top60 CLI
  `3/3` 通过。
- [x] 43 个完整成员产生 75 笔、34 币、74 个时间方向事件，解决 V11
  压缩家族只有 2 笔的样本过少问题。
- [x] 零成本为 `+8.85R / PF_R 1.226 / +0.118R每笔`，压力成本后为
  `-44.97R / PF_R 0.371 / -0.600R每笔`，核心经济门槛失败。
- [x] 25 笔入场当根止损贡献 `-25R`；达到 1R 保护的 34 笔压力成本后
  仍为 `+21.11R`，确认首要问题是入场接受和风险过窄，而不是止盈不足。
- [ ] 状态为 `research_only_rejected_cost_and_entry_quality_gate_failed`；
  不替换 V11，不进入 Paper/ReadOnly/Live。下一版只在未见窗口研究
  实际开盘接受门禁与最小风险噪声地板，退出合同保持冻结。

## 2026-07-29：箱体突破右侧触发 Research V16（过度过滤，拒绝）

- [x] 在查看结果前冻结 V16：V15 接受棒只建立候选，后续 1～3 根突破
  接受棒高/低点外 1 tick 才成交；成交前结构失效、重新进入箱体或超时撤单。
- [x] 入场前冻结 `0.35～1.50 ATR` 风险、`>=1.80R` 预期和
  `<=0.30R` 双边预计成本门禁；V15 退出合同保持不变。
- [x] Pine `5ac357c1` 在 TradingView 编译 0 错误；Rust parity
  `111/111`、三个 CLI `7/7` 通过。
- [x] 同一 43/60 诊断从 V15 的 75 笔、74 事件、34 币降为
  2 笔、2 事件、2 币，且只有多单；组合门禁造成 `97.33%` 样本塌缩。
- [x] 两笔均盈利、压力成本后 `+1.94R`、无当根止损，但 VIRTUAL 占
  正贡献 `79.21%`，全部样本与集中度门槛失败，不能视为优化成功。
- [x] TradingView 当前主策略已恢复 V11 本地源码且逐字一致；V16 不挂载、
  不替换 V11/V15，不进入 Paper/ReadOnly/Live。
- [ ] 若继续，只能另立未见窗口的“纯右侧触发”消融，不能在当前窗口追调
  `0.35 ATR / 1.80R / 0.30R`。

## 2026-07-29：箱体突破纯右侧触发消融 Research V17（成本后拒绝）

- [x] 在查看结果前冻结单变量消融：保留 V15 接受资格与 V16 的 1～3 根
  stop-entry，只移除 V16 新增的三项经济门禁。
- [x] Pine `7097ee03` 与 Rust `CandidateV17` 同源；TradingView 候选和
  恢复后的 V11 均为 0 错误、0 警告。
- [x] Rust parity `112/112`、三个 CLI `7/7`、格式检查通过；触碰代码
  均低于 2000 行硬上限。
- [x] 同一 43/60 诊断恢复到 32 笔、31 个事件、22 个币种，多空
  17 / 15；V15 的 25 笔入场当根止损降为 0。
- [x] 零成本为 `+4.08R / PF_R 1.262 / +0.128R每笔`，压力成本后为
  `-7.70R / PF_R 0.659 / -0.241R每笔`；平均摩擦占 `0.368R`。
- [x] 最大单币/事件正贡献为 `31.59% / 12.92%`，覆盖和集中度通过，
  但净收益与 PF 门槛失败。
- [x] V17 冻结为
  `research_only_rejected_negative_net_edge_after_cost`；不替换 V11，
  不进入 Paper/ReadOnly/Live，不部署、不写数据库、不触发交易。
- [ ] 下一步只能在新时间窗口预注册“额外箱体外收盘接受”或“更高周期
  经济性”中的一个单变量方向，不能继续在当前窗口追调阈值。

## 2026-07-29：V11 合并 V17 箱体右侧触发 Research V18（成本后拒绝）

- [x] 在查看结果前冻结 V18：V11 同棒优先、V17 仅补充未占用机会、单持仓、
  未成交 stop-entry 遇 V11 主信号撤销，以及收益/样本/集中度门槛。
- [x] 新增 Rust `CandidateV18`、CLI 入口和组合状态机；V11 既有成交合同
  保持冻结，V17 继续使用独立分阶段退出。
- [x] Pine `9f26295a` 已切换为当前 TradingView 主 Research，编译
  0 错误；Rust parity `112/112`、三个 CLI `7/7`、格式检查通过。
- [x] 同一 43/60 诊断中，V11 的 2,262 笔逐笔不变；V17 实际新增
  29 笔、26 个事件，多空 15 / 14，V18 总交易为 2,291 笔。
- [x] 新增交易零成本 `+5.40R / PF_R 1.398`，压力成本后
  `-5.47R / PF_R 0.726`；V18 总压力净 R 从 `-49.52R` 恶化为
  `-54.99R`，收益门槛失败。
- [x] V18 标记为
  `research_only_rejected_negative_incremental_edge_after_cost`；仅保留
  TradingView 图表研究，不进入 Paper/ReadOnly/Live、部署或真实下单。
- [ ] 后续只能在未见窗口预注册“更高周期经济性”或“箱体外收盘接受”
  中的一个方向，不能继续在当前窗口同时调整多项阈值。

## 2026-07-30：EMA 空头趋势五项单变量消融

- [x] 结果前冻结 V19、43 个本地完整冻结成员、评价窗口、60 天预热、
  每边 8 bps 成本、退出合同与五个互斥单变量，未补造数据。
- [x] 新增 Research-only Rust 消融入口；V19 基线与既有报告除新增版本字段外
  完全一致，未修改 Pine、Paper、Live 或生产策略。
- [x] `slope_spread`、`structure_break`、`right_side_retest`、
  `distance_guard`、`extreme_volume_acceptance` 已依次独立回放。
- [x] 仅 `structure_break` 取得明显改善：EMA 空头
  `-19.38R → +38.90R`，全策略 `-51.65R → +18.90R`，同棒及一根内
  止损率均下降。
- [x] 审计确认被删 507 笔为 `-69.15R`，但持仓路径释放后新增 27 笔为
  `-10.87R`；BTC 仍为负，ETH 无同口径成员证据，每边 12 bps 时优势转负。
- [x] 完成 Rust parity `121/121` 与 Top60 CLI `4/4` 测试；结果与局限写入
  `tradingview_velocity_v19_ema_short_single_variable_ablation_result_20260730.md`。
- [ ] V19 与当前 Pine 保持冻结；下一轮只把 `structure_break` 设为新基线，
  预注册“结构破位深度”一个变量并补绝对正期望门槛，不直接合并主策略。

## 2026-07-30：结构破位深度参数邻域 V2（拒绝）

- [x] 结果前冻结 D0、D10=`0.10 ATR`、D20=`0.20 ATR`，并新增平均 R、
  PF_R、全策略正收益、样本、快速止损、10 bps 成本和集中度绝对门槛。
- [x] Rust 仅新增两个 Research-only 深度身份与边界测试；D0 当前回放和上一轮
  JSON 忽略生成时间后逐字段一致，Pine 与其他家族保持冻结。
- [x] D10：786 笔、`+25.19R / +0.0320R每笔 / PF_R 1.043`；
  全策略 `+10.61R`，10 bps 时转负，淘汰。
- [x] D20：745 笔、`+33.81R / +0.0454R每笔 / PF_R 1.062`；
  全策略仅较 D0 增加 `0.22R`，但目标家族三项经济指标均退化，淘汰。
- [x] D10 删除的 36 笔 D0 交易为 `+15.37R`；D20 删除的 78 笔为
  `+5.66R`，证明固定“破得越深越可靠”的线性假设不成立。
- [x] BTC 三档均保持 4 笔 `-1.84R/PF_R 0.479`；2025-08/09 弱势仍在，
  ETH 无完整成员证据，固定深度没有解决市场状态问题。
- [ ] 保留 D0 为唯一 Research 候选；禁止在当前窗口后验排除
  `0.10～0.20 ATR`，下一轮应预注册市场状态归因而非继续调深度。

## 2026-07-30：EMA676 斜率与 2025-08/09、BTC 市场状态审计

- [x] 结果前冻结 D0 与唯一变量 S20：
  `EMA676[t] < EMA676[t-20]`；其他信号、退出、成本和 43/60 回放口径不变。
- [x] S20 仅删除 4/821 笔、改善 `+4.51R`，但 PF_R 仅 `1.073`，
  快速止损率略升，且 2025-08/09 与 BTC 结果完全不变，按门槛淘汰。
- [x] 后验市场状态审计确认 2025-08/09 的 157 笔仅对应 39 个同步市场事件，
  主要呈现局部破位后快速均值回归；简单 EMA、RSI、量比、BTC 趋势或波动率
  均不能稳定隔离该制度。
- [x] BTC 4 笔全部位于 96 根区间底部 20%、慢 EMA 下斜且 4H 空头；
  3 笔亏损更符合跌势末端追空，但样本不足以冻结专用阈值。
- [x] Research 测试 `9/9`、CLI 测试 `4/4`、Rust 格式和 2000 行硬上限通过；
  Pine、Paper、ReadOnly、Live、数据库和交易执行均未改动。
- [ ] 下一轮若继续，只能在未见窗口预注册“入场延伸度”或“96 根区间位置”
  中的一个变量；当前窗口不得继续后验调阈值。

## 2026-07-30：迁移 V1 Binance + OKX

- [x] 冻结 V1 provider inventory 为 `binance_usdm + okx_swap`，区分 legacy
  六家历史解析与 V1 实际运行支持。
- [x] 完成 Binance、OKX SDK 只读能力审计，冻结可复用入口、必须修复项、
  public-only 边界与 contract test 矩阵。
- [x] 识别 migration-check 的跨仓库硬编码阻塞，并登记
  `MIG-20260730-CROSS-REPOSITORY-MIGRATION-CHECK-P2`。
- [x] 登记 `MIG-EXSDK-I1-BINANCE-OKX-PUBLIC-INSTRUMENT-V1`，业务上不依赖
  F4B，实施治理依赖 P2。
- [x] 登记 `MIG-MKT-F4C-BINANCE-OKX-INSTRUMENT-INGEST-V1`，依赖 F4B 与
  Exchange SDK I1。
- [x] 冻结 P2 三仓上下文、显式 repository root、目标 repo Git/Cargo/path、
  registration/current Registry 与 non-deployable library profile 的验收合同。
- [x] 修正 Program state/child 矩阵：active 只表示父编排已启动，不使未创建
  sibling 满足依赖或取得实施授权。
- [x] 为阶段 1、2、4 增加稳定父计划锚点，并把所有 Registry `parent_plan`
  更新为可解析的 `#stage-*` 引用。
- [x] 冻结全局依赖图规则：`depends_on` 按全 Registry 唯一 child ID 解析，
  跨 Program 仅允许 current-migration 边，并全局检查缺失、重复、historical
  引用和依赖环。
- [x] 矫正 Owner：SDK I1 只新增两家 typed protocol capability；runtime inventory、
  `UnsupportedProvider`、perpetual/settle/quote source profile 归 Market F4C。
- [x] 消除 completeness 术语冲突：SDK 只拥有 provider protocol/decode completeness
  evidence；Market F4C 拥有 source snapshot 的业务完整性与 readiness。
- [x] 同步 `rust-quant-architecture` 技能的两阶段登记、三仓预检与 SDK/Market
  Owner 硬边界。
- [x] 复验 F4B migration-check：48 个 changed paths，0 error、0 warning。
- [ ] 先提交本轮 `not_created` registration revision；提交前不得创建 P2/I1/F4C
  Manifest 或源码目录。
- [ ] 开始 SDK I1 前，单独保存/提交或以其他可恢复方式清空
  `crypto_exc_all/README.md` 的既有 allowlist 外修改；迁移任务不得覆盖。
- [ ] registration 提交后实施 P2，再进入 Binance/OKX SDK I1；CI/CD 继续按决定
  延后到迁移完成后。

## 2026-07-30：D0 EMA 空头交易解剖与市场事件归因

- [x] 在查看新结果前冻结实际成交样本、1/2/4/8/16 根 MFE/MAE、退出棒保守口径、
  破位线收回、止损后恢复、48 根方向效率、TR 波动阶段和 60 分钟事件聚类。
- [x] 新增 Research-only 诊断模块；未来 K 线只生成 outcome label，不参与信号、
  入场、阻塞、止损或目标。
- [x] 43/60 同源回放生成 821/821 笔有效 EMA D0 记录，0 笔无效；定向测试 8/8、
  格式与代码行数门禁通过。
- [x] 确认 536 笔亏损中 165 笔先达到至少 1R 再转亏，占 30.78%；其中 157 笔为
  `Fixed/StopLoss`，2025-08/09 的 50 笔全部属于该路径。
- [x] 确认 2025-08/09 的 157 笔仅对应 39 个事件，多币事件平均 `-5.423R`、
  贡献 88.96% 亏损；其他月份多币事件平均 `+2.004R`，禁止无条件拒绝同步信号。
- [x] BTC 4 笔中 3 笔亏损均未达到 1R；净保本变量不会直接修复 BTC，后续需扩大
  样本后单独研究破位接受与跌势末端。
- [x] 按预注册优先级选择下一轮唯一变量：完成棒收盘达到 1R 后，从下一根启用成本后
  净保本，原目标不变；已冻结独立研究清单，尚未修改交易逻辑。
- [ ] 下一轮按新清单实现 Research-only 退出版本并重跑同一 43/60、目标月份、BTC
  和 10/12 bps 压力；未通过全部门槛则淘汰，不叠加分批或追踪。

## 2026-07-30：D0 EMA 空头完成棒 1R 净保本单变量（淘汰）

- [x] 使用冻结 D0 的 821 笔成交做逐笔隔离退出反事实，避免提前平仓释放共享仓位后
  污染后续信号；旧/新全策略 1,837 笔成交 identity 哈希完全一致。
- [x] 仅对 803 笔 `ema_trend_short + Fixed + Short` 启用完成棒收盘 1R；
  279 笔激活、124 笔实际净保本退出。
- [x] 完成棒、下一棒、跳空和同棒目标/保护 OHLC 路径测试通过；focused suite
  `14/14`。
- [x] 8 bps 下全部 D0 从 `+38.90R/PF_R 1.065` 降至
  `-8.04R/PF_R 0.985`；54 笔赢家损失 `127.70R`，超过 70 笔亏损减少的
  `80.76R`。
- [x] 2025-08/09 仅减亏 13.12%，其他月份净 R 下降 38.70%，10 bps 变体
  `-36.01R`；按预注册门槛淘汰。
- [x] 结果写入
  `tradingview_velocity_v19_d0_ema_short_completed_close_1r_net_be_result_20260730.md`；
  Pine、Paper、ReadOnly、Live、数据库和交易执行均未改动。
- [ ] 保留 D0 原固定退出；若继续研究，先预注册失败制度/结构确认变量，不在本窗口
  继续微调 1R、1.2R 或 1.5R 保本阈值。

## 2026-07-30：D0 EMA 空头结构破位失败回抽净保本（淘汰）

- [x] 结果前冻结严格三阶段时序：完成棒 1R、后续完成棒跌破此前 20 根最低点、再后续
  回抽冻结线失败；不增加 ATR 容差、K 线方向、量能或最长等待窗口。
- [x] 新增逐笔隔离 Research 反事实与 4 个结构路径测试；不释放共享仓位、不改变信号、
  入场、初始止损、固定目标或其他信号家族。
- [x] 同一 43/60 回放保持全策略 1,837 笔身份一致；D0 821 笔、74 笔失败回抽激活、
  45 个 60 分钟事件、26 笔实际提前退出。
- [x] 8 bps 下原亏损减少 `20.5952R`，原赢家损失 `20.4998R`，最终仅改善
  `+0.0954R`；10 bps 仍为正，但 12 bps 两者均为负。
- [x] 2025-08/09 仅 3 笔实际保护，减亏 `2.73%`，远低于预注册 `25%`；BTC
  `4笔/-1.8387R` 完全不变，整体晋级门槛失败。
- [x] 中文结果报告、18/18 focused tests、格式、JSON 与代码行数门禁完成；未修改
  Pine、Paper、ReadOnly、Live、数据库或交易执行。
- [ ] 保留 D0 原固定退出；下一轮只研究预注册的事前市场状态入场门禁，不再在当前窗口
  微调结构 lookback、容差、等待期或保本 R 阈值。

## 2026-07-31：V19 EMA 趋势多突破深度单变量

- [x] 冻结 A3 的 P80、量比 2.5、实体 0.3%、V10 回踩接受和既有退出，只新增
  信号收盘越过前 20 根高点的 `0.20 / 0.30 / 0.40 ATR` 门槛。
- [x] 43/60 同源回放与双边 8 bps 成本完成；0.20 ATR 增量
  `18笔/-5.95R`，0.30 ATR `11笔/+2.49R`，0.40 ATR `7笔/+1.92R`。
- [x] 确认 0.30 ATR 的全策略净 R 从 A2 `-52.75R` 改善到 `-49.15R`，
  但 PF_R 仅 `0.968`，未达到研究晋级线，不合并主策略。
- [x] 审计发现 Candidate V19 的 V10 路径仍硬编码距 EMA12 `1.25 ATR`，
  既有 distance 1.5 阶梯实际无效；ETH 07-14 虽通过 0.40 ATR 突破，仍被距离阻塞。
- [ ] 下一轮先修正距离实验合同并单变量验证 `1.25 / 1.35 / 1.50 ATR`；
  不与确认量延续门禁同时修改。

## 2026-07-31：V19 EMA 趋势多 EMA12 距离单变量

- [x] 修复 Candidate V19/V10 未读取 Research 距离参数的实验合同；默认仍为
  `1.25 ATR`，未改变原 V19。
- [x] 新增 `0.30 ATR` 冻结突破深度下的 `1.35 / 1.50 ATR` 独立研究版本，
  其他来源门槛、接受状态机、退出和信号家族保持不变。
- [x] 契约测试证明约 `1.29 ATR` 来源在 1.25 档被拒绝、在 1.35 档进入 setup；
  V10 8/8、Research helper 2/2、Top60 runner 18/18 通过。
- [x] 同一 43/60、60 天预热、双边各 8 bps 回放完成：1.25 / 1.35 / 1.50
  全策略分别为 `-49.15R / -59.19R / -85.71R`，PF_R 分别为
  `0.968 / 0.962 / 0.946`。
- [x] 边际 1.25→1.35 为 `13笔/2盈11亏/-10.04R/PF_R 0.245`；
  1.35→1.50 为 `43笔/9盈34亏/-26.53R/PF_R 0.357`，两档均淘汰。
- [x] 保留 1.25 ATR，不合并 Pine、Paper、ReadOnly、Live 或主策略。
- [ ] 若继续，冻结 0.30 ATR 突破深度与 1.25 ATR 距离，只单变量验证
  “V10 接受 K 必须为阳线”，不再围绕 ETH 单点微调距离。

## 2026-07-31：V19 EMA 趋势多阳线回踩接受单变量

- [x] 冻结 P80、量比 2.5、实体 0.3%、突破深度 0.30 ATR、EMA12 距离
  1.25 ATR、V10 窗口和退出，只对补充来源要求回踩接受 K 收阳。
- [x] 阳线合同在来源 K 写入 pending；原 V19 来源不受影响，后续调用不能改变
  已冻结 setup。
- [x] 当前二进制复跑基线与旧 JSON 忽略生成时间后逐字段一致，逐交易 identity
  SHA256 为 `592e4ccbc8c97a51ac4378ea7f03d4a3ce2eef38c468c1d640a65118adb6cc32`。
- [x] 同一 43/60、60 天预热、双边各 8 bps 下，全策略从
  `-49.1495R / PF_R 0.9680` 恶化到 `-50.1726R / PF_R 0.9673`。
- [x] EMA 趋势多从 `+3.9094R / PF_R 1.2607` 降到
  `+2.8864R / PF_R 1.2081`；4 根内亏损仍为 7 笔。
- [x] 边际为删除 4 笔、替代 2 笔；删除一笔 `+2.1504R` DOGE 赢家和一笔
  `-1.1281R` ORDI 亏损，最终少 `1.0230R`。
- [x] V10 9/9、Research helper 2/2、Top60 18/18、release 构建、格式和
  2000 行硬上限通过。
- [x] 淘汰纯阳线门禁；未修改 Pine、主策略、Paper、ReadOnly、Live、数据库或
  交易执行。
- [ ] 若继续，只能在未见窗口预注册“守住冻结线后突破回踩 K 高点的 stop-entry”
  单变量，并统计未成交率与延迟赢家。

## 2026-08-01：HYPE 放量杀跌后低位拒绝与强阳收回

- [x] 冻结独立 `volume_sell_climax_base_reclaim_long_15m_v1`，不放宽既有布林下轨
  单棒反转规则；信号收盘确认、下一根开盘成交。
- [x] 目标 HYPE 样本逐条件审计：杀跌源、5 根低位拒绝、强阳收回 EMA12/短结构、
  RSI 与 MACD 改善均成立，但收盘仍低于下行 EMA576，目标未被识别。
- [x] 完成 43/60 单变量回放；新增家族 0 笔，候选与基线 2,298 笔逐币结果完全
  一致，按预注册规则判定 `rejected_definition_mismatch`。
- [x] focused tests 3/3、Top60 18/18、格式与 2000 行硬上限通过；未修改 Pine、
  Paper、ReadOnly、Live、数据库或交易执行。
- [ ] 如继续，另立“慢均线下方逆势卖压衰竭反弹”假设，以 EMA576/最近破位阻力
  为冻结目标；不得事后删除 EMA576 条件让本轮版本迎合截图。

## 2026-08-01：V20 锚区上破后扫高失败接受空单

- [x] 冻结 V19 原“8根内收盘跌破下沿”状态机，新增独立
  `volume_anchor_upthrust_failed_acceptance_short_15m_v1`，不覆盖旧信号。
- [x] Pine 与 Rust 统一为：突破后第1～2根完成棒扫过突破高点、阴线跌回冻结上沿、
  收盘位于振幅底部25%、拒绝量不低于突破量50%、到冻结下沿至少1.5R。
- [x] BTC `2026-07-13 08:15 +08:00` 因果样本通过：信号收盘确认，最早
  `08:30` 开盘成交，止损 `64398.1`，冻结目标 `63639.0`。
- [x] TradingView Pine 编译 0 错误；Rust 早期/旧版/低RR/旧下沿分支 5/5 通过，
  V5～V20 冻结源码身份测试通过，V20 指纹为 `a755168d`。
- [x] 仅接入 Research parity 与 CLI 版本选择；未注册 Paper、ReadOnly、Live、
  调度器或生产策略目录。
- [x] 重新打开已保存脚本后，后台 Pine 模型与可见 Monaco 模型已归一；V20 已另存为
  独立 TradingView 脚本并加载到 `OKX:BTCUSDT.P` 15m 图表，图例仅保留 V20 与
  RMV 副图。07-13 放量上破样本在图表显示早期空单，并于冻结下沿 `63639.0`
  显示 `TP_STRUCT`；截图验收完成。
- [x] 当前 BTC 可见窗口为8笔、5胜3负、PF `3.406`、净利润 `3767.3 USDT`，但
  TradingView 该口径佣金为0且样本极小，只作为加载/时序 smoke，不作为晋级证据。
- [x] 结果前冻结同一 Top60、60 天预热、`vol_ccy` 与双边各 8 bps 成本口径；本地
  43/60 完整成员完成 V19/V20 同范围回放，17 个数据不完整成员按既有规则跳过。
- [x] 新家族共 217 笔、190 个 60 分钟有效事件；零成本 `+27.57R/PF_R 1.186`，
  成本后转为 `-10.04R/-0.046R每笔/PF_R 0.942`，21 个币种正、21 个币种负。
- [x] V20 全策略成本后从 V19 的 `-51.65R/PF_R 0.9663` 恶化到
  `-59.68R/PF_R 0.9649`；paired delta 为 `-8.03R`，不满足晋级门槛。
- [x] 审计确认早期空单会反手已有多仓，且与 RSI 顶背离同棒重合时接管结构退出；
  该交互不是纯独占信号效果，已写入结果报告，V20 不再继续事后调参。
- [x] 多币种结论写入
  `tradingview_velocity_v20_anchor_upthrust_multisymbol_result_20260801.md`；V20 继续
  Research-only，不进入主策略、Paper、ReadOnly、Live 或生产执行。
- [ ] 若继续，另立 V21 并只验证“无 V19 同方向候选、无反向持仓时才允许早期空单”这
  一个独占门禁；ETH 需单独事前冻结补充对照，不能混入本轮 43/60 结果。

## 2026-08-01：V21 扫高失败右侧确认多币种验证

- [x] 先审计 V20 的 217 笔成交：43 笔在入场后 0～1 根止损、75 笔在 4 根内
  止损，且 RSI、量比与初始风险分组均无稳定单调关系，确认核心问题是入场过早。
- [x] 结果前冻结唯一变量：V20 拒绝棒仅形成 setup；下一根完成 K 线必须收盘跌破
  拒绝棒低点且未触及冻结止损，才在再下一根开盘入场。止损、结构目标、最低 1.5R
  与其余信号族全部保持不变。
- [x] 新增独立 Research 版本
  `volume_anchor_upthrust_failed_acceptance_right_side_short_15m_research_v21`，未覆盖
  V20，也未注册 Pine、Paper、ReadOnly、Live 或生产执行。
- [x] 同一 current-live Top60、43/60 完整成员、60 天预热、`vol_ccy` 和双边各
  8 bps 成本完成回放；17 个数据不完整成员按既有规则跳过。
- [x] V21 家族为 52 笔、22 胜 30 负、胜率 `42.31%`；成本后
  `+13.27R / +0.255R每笔 / PF_R 1.383`，相对 V20 的
  `-10.04R / PF_R 0.942` 明显改善。
- [x] 多币种覆盖 27 个币种、45 个 60 分钟事件簇、13 个月份；移除最大 3 笔后
  仍为 `+2.69R`。BTC 仅 1 笔且为 `-1.42R`，ETH 不在冻结币种池，均不能宣称改善。
- [x] 全策略成本后总 R 从 `-59.68R` 改善到 `-34.88R`，PF_R 从
  `0.9649` 改善到 `0.9777`；方向有效但整体仍为负期望。
- [x] 按预注册判定保留 V21 Research 代码与报告；因平均期望、PF_R、BTC/ETH
  证据均未达到职业级门槛，拒绝晋级主策略与运行路径。
- [ ] 下一轮如继续，只预注册“确认棒是否已经消耗冻结结构目标”一个变量；不得用
  本轮结果事后筛 RSI、量比或盈利币种。

## 2026-08-02：V22 确认棒结构目标消耗验证

- [x] 结果前冻结唯一变量：确认棒相对 setup 收盘到冻结结构目标的已消耗比例；只使用
  信号时点已完成 K 线，不读取实际入场开盘或未来路径。
- [x] 冻结 V21 无上限、V22A `25%`、V22B `33%`、V22C `50%` 四组，其余
  setup、确认、止损、结构目标、成本、币种池与资金路径全部保持不变。
- [x] 新增目标消耗审计字段、三个独立 Research 版本与边界测试；新增字段未改变 V21
  基线，标准化逐笔哈希与旧报告一致。
- [x] 同一 current-live Top60 完成 43/60 有效成员回放；17 个缺数据或预热不足成员
  按冻结规则跳过，不补造 K 线。
- [x] 52 笔目标消耗均值 `9.78%`、中位数 `8.64%`、P90 `16.85%`、最大
  `19.51%`；25% / 33% / 50% 三档均未删除交易，四组逐笔结果完全一致。
- [x] V22A/B/C 均判定为 `Research rejected — redundant threshold`；不修改主 Pine、
  Paper、ReadOnly、Live 或生产执行，V21 仍是该家族唯一保留的 Research 候选。
- [ ] 若继续，二选一重新预注册：在未见窗口验证固定 15% 上限；或审计确认收盘到下一根
  实际开盘的额外结构消耗。不得用本轮 10 笔高消耗样本事后定阈值。

## 2026-08-02：L0–L3 默认研究工作流与回测运行时加速

- [x] 固化 L0 形态分析、L1 快速研究、L2 多币种诊断、L3 晋级验证四级流程；默认从
  L0/L1 开始，只有本级结果正向才升级。
- [x] 固化每轮唯一假设卡、无标签覆盖扫描、候选账本、只回放一个主候选、正向后才打开
  TradingView，以及批次只保留预注册/机器结果/结论三份产物。
- [x] Top60 runner 增加 Pine 校验、数据加载、数据/程序指纹、成员筛选、回放、分析、
  候选账本和序列化阶段计时。
- [x] 增加绑定数据、可执行文件、Pine、窗口、成本和币种池身份的 baseline 缓存；候选
  变体不走缓存，避免隐藏仓位冲突与路径变化。
- [x] 增加信号时可见特征、冻结止损/目标/退出政策、事件簇、成交/阻塞和成本前后标签分栏
  的候选账本；L1 筛选禁止读取 outcome。
- [x] 重复 `--anchor-upthrust-variant` 可在一次行情加载中按声明顺序运行多个注册变体；
  单变体 JSON 形状兼容，多变体使用批次外壳。
- [x] focused tests 23/23、release build、格式检查和 2000 行硬上限通过；43/60 本地可用
  成员只作 L2 运行验收，缺失成员未补造。
- [x] 最终 release 二进制首次基线约 83.2 秒，同身份缓存命中约 3.8 秒；两变体批次
  约 81.0 秒且只加载一次行情、共享一个数据指纹，证明基线复用和单进程批次生效。

## 2026-08-02：L0～L3 项目硬规则与策略技能归一

- [x] 在 umbrella 根 `AGENTS.md` 写入 L0～L3 硬约束，覆盖适用边界、逐级升级、唯一变量、
  无标签扫描、候选账本、L2 缺失成员跳过、L3 晋级验证、TradingView 与批次产物边界。
- [x] 明确“实现/回测”请求不豁免研究门禁；同一轮只能在每级证据通过后继续升级，纯编译、
  运行错误和冻结行为 parity 修复不强制退回 L0。
- [x] 将 `strategy-iteration` 从旧的 933 行双重探索流程重写为 245 行单一路径，删除截图后
  直接生成代码、L2 自动补数据、每个阈值独立文档等冲突规则。
- [x] 技能现在直接引用根硬规则和 `STRATEGY_RESEARCH_WORKFLOW.md`；发生漂移时以根规则优先，
  并要求先修正规则再继续研究。
- [x] 完成关键词、禁止项、链接、YAML frontmatter 与差异检查；未修改策略代码、Pine、数据库
  或生产运行路径。

## 2026-08-02：15 分钟动量布林长影回归 L1

- [x] 将目标定位到 `market_momentum_exhaustion_reversal_15m_v2` 的 60% 方向长影 cohort；
  本轮唯一变量为信号 K 方向极值触及 `Bollinger(20, 2.5)` 外轨。
- [x] 新增独立 Research-only L1 扫描器、预注册和无标签候选账本；不读取成交、MFE、MAE、
  退出、R、胜负或 PnL。
- [x] current-live Top60 中 44/60 成员具备完整 672 根预热和评价窗口；16 个缺数据成员跳过，
  未补造 K 线。
- [x] 896 个来源长影 setup 中 673 个触轨，保留 `75.11%`；多空 340/333，44 个币种、
  13 个月、369 个 60 分钟有效事件。
- [x] 因保留比例超过预注册 `10%~60%`，判定 `stop`；未运行结果回放，未实现趋势/中轨过滤、
  分批退出或任何 Paper/Live 路径。
- [ ] 若继续，用户需确认高覆盖触轨是否仅作为家族身份，并提供多/空/边界目标图；随后逐个
  预注册趋势年龄、中轨距离和退出合同，不能在本轮结果上修改门槛。

## 2026-08-02：15 分钟动量布林长影回归趋势年龄 L1

- [x] 后续经用户纠正，确认该批次把 96 根净移动年龄误当成 EMA 趋势年龄；最终状态改为
  `rejected_definition_mismatch`，错误研究入口已删除，统计只保留审计。
- [x] 按用户澄清将长上影 + `high` 触上轨做空、长下影 + `low` 触下轨做多冻结为基础家族身份；
  `0.5R` 统一解释并命名为中轨回归时平掉原始仓位 50%。
- [x] 预注册唯一变量“96 根/8% 来源趋势连续成立年龄”，同一账本比较拒绝前 4/8/12 根
  三个变体；禁止读取成交后 K 线和结果标签。
- [x] 新增 Research-only 趋势年龄扫描器与 4 个边界测试；Bollinger 基础形态完整回归共
  9/9 通过，新二进制编译和格式检查通过。
- [x] 同一 44/60 完整成员、同一数据指纹生成 673 个基础 setup 账本；前 4/8/12 根分别
  拒绝 327/415/458 个，即 48.59%/61.66%/68.05%。
- [x] 三组唯一失败门槛均为拒绝比例超过预注册 5%～25%，判定 `stop`；没有回放成交、收益、
  中轨过滤或退出，也没有修改 Pine、Paper、ReadOnly、Live、数据库或生产执行。
- [ ] 若继续，需另立批次确认“刚形成”是否严格等于 `trend_age_bars == 1`，并补充多/空/边界
  目标图；当前 164 个年龄 1 setup 只是无标签分布，不能事后并入本批次晋级。

## 2026-08-02：15 分钟动量布林长影 EMA 趋势新形成 L1

- [x] 用户最终纠正：该批次额外加入连续三根排列和 EMA696 斜率，状态改为
  `rejected_definition_mismatch`；其 5 个命中只保留审计，不再作为策略结论。
- [x] 复用现有 Core 严格趋势合同：连续三根 `EMA12/144/169/696` 同向排列，最近四个
  EMA696 同向；当前确认 true、前一根 false 才定义为“刚形成”。
- [x] 新增独立 Research-only EMA 信号时账本，并为 699 根慢线预热生成独立行情指纹；不把
  H1 的 672 根成交额窗口指纹冒充 EMA 输入身份。
- [x] focused tests 10/10、新二进制 `cargo check` 与 `cargo fmt --check` 通过；原错误趋势年龄
  模块和二进制已移除。
- [x] 同一 673 个基础 setup 全部 EMA-ready；487 个处于对向确认趋势，但只有 5 个（0.7429%）
  恰在首次确认 K，做多/做空为 1/4，覆盖 5 个币种、4 个月和 5 个事件。
- [x] 因命中不足 10、影响低于 2%、多单不足 2 个，按预注册判定 `stop`；没有读取成交后结果、
  中轨过滤或退出，也没有修改 Pine、Paper、ReadOnly、Live、数据库或生产路径。
- [x] “年轻确认窗口”后续项因最终单根定义已明确而取消；不得把 `age<=8` 事后并入新口径。

## 2026-08-02：15 分钟动量布林长影单根 EMA 排列刚形成 L1

- [x] 后续经用户明确只保留 EMA12/144/576，本节 `EMA12/144/169/696` 批次已标记为
  `rejected_definition_mismatch`；其 1 个命中只作审计，不再作为当前策略结论。
- [x] 最终冻结单根口径：当前信号 K 严格 `EMA12/144/169/696` 排列、上一根未按同方向排列；
  上一根只用于 `false -> true` 判断，不要求连续 3 根，也不读取 EMA696 斜率。
- [x] 新增独立 Research-only 扫描入口、预注册和信号时 EMA 账本；旧三根/斜率入口已移除，
  不覆盖基础 Momentum V2/V3 或任何运行版本。
- [x] 因果边界与基础形态 focused tests 11/11、格式检查和独立二进制编译通过；候选账本字段
  复核确认不含成交、未来 K 线或 outcome 字段。
- [x] 同一 44/60 完整成员、同一 673 个基础 setup 全部 EMA-ready；494 个当前对向排列，
  其中 493 个上一根已经排列，只有 1 个首次排列（0.1486%）。
- [x] 唯一命中为做空方向，覆盖 1 个币种、1 个月份和 1 个事件；除 EMA 完整性外其余预注册
  门槛全部失败，状态为 `stop`，没有读取任何结果标签。
- [ ] 若继续，只能另立批次量化“信号 K 收盘接近中轨”这一个变量；需先冻结距离口径与阈值，
  不得用本轮唯一命中的后续盈亏反选参数。

## 2026-08-02：15 分钟动量布林长影 EMA12/144/576 单根排列刚形成 L1

- [x] 冻结真实来源链：既有 `market_momentum_exhaustion_reversal_15m_v2` 先按量比、周 P90、
  前 96 根正负 8%、实体与 60% 方向长影产生多空，再检查对应 Bollinger(20,2.5) 外轨；EMA
  只否决候选，不产生或改写方向。
- [x] 最终只判断信号 K 单根 `EMA12/144/576` 严格排列，上一根仅用于 `false -> true`；明确
  排除 EMA169、EMA696、连续 3 根确认、EMA576 斜率及均线距离。
- [x] 独立实现同口径 SMA 种子的 EMA576 递推；新增 Research-only 扫描入口、预注册、信号时
  账本和精确 V2 版本分派回归测试，未覆盖 Momentum V2/V3 或运行入口。
- [x] focused tests 14/14、`cargo fmt --check` 和独立只读扫描通过；机器身份明确记录既有 V2
  多空定义与 `V2 -> Bollinger -> EMA veto` 固定顺序。
- [x] 同一 44/60 完整成员与 673 个基础触轨 setup 全部 EMA-ready；514 个当前对向排列，
  其中 512 个上一根已经排列，只有 2 个 `false -> true`，影响 `0.2972%`。
- [x] 两个命中均为做空，仅覆盖 2 个币种、2 个月和 2 个事件；除 EMA-ready 外全部预注册
  门槛失败，命中 1～2 个触发立即停止条件，未读取任何 outcome。
- [x] 机器报告 SHA-256 为 `05de0fa466be6b40d8a7c1cd2b6ce6897eeb4636b62932f45729c740603a08d0`；
  未实现中轨过滤、退出回放、Pine、Paper、ReadOnly、Live、数据库或生产路径。
- [ ] 当前 L1 已停止；若提出新假设，必须重新冻结唯一变量，不能依据这 2 个命中的后续结果
  放宽“刚形成”或选择中轨阈值。

## 2026-08-02：V23 最近有效横盘首次突破失败接受 L1

- [x] 冻结唯一变量为 V20 空单家族的锚点合同：最近已完成的 8 根有效横盘、首次放量收盘
  突破并冻结上沿、突破后第 1 或第 2 根扫高且收盘跌回冻结上沿下方；量能、确认时限、
  止损、目标和成交路径保持不变。
- [x] 新增独立 Research-only V23 版本与 CLI 变体；使用独立 pending 状态，未覆盖 V20、V21、
  V22，也未接入 Pine、Paper、ReadOnly、Live 或生产调度。
- [x] 增加首次突破、旧突破拒绝、趋势阶梯拒绝、有效正例和 SHIB 目标反例测试；策略库
  148/148、Top60 入口 23/23、格式和 2,000 行硬上限检查通过。
- [x] 对冻结 current-live Top60 做无标签 L1 扫描；43/60 成员可用，V20 为 217 个候选，
  V23 为 7 个候选，覆盖 7 个币、5 个 UTC 月份和 7 个独立事件。
- [x] SHIB 2026-07-06 目标反例已被拒绝：06:15 前横盘上沿已有完成棒收盘突破，不能在
  06:15 重新冻结旧上沿，也不能在 06:30 形成该家族空单。
- [x] 因 13 个月评价窗仅剩 7 个无标签候选，覆盖不足以进入 L2；停止在 L1 并保留为
  Research-only，未读取 outcome、MFE、MAE、R、胜负或 PnL。
- [ ] 若继续，必须另立 L0/L1 单变量假设重新定义有效横盘的时间尺度或边界稳定性；不得用
  这 7 个候选的后续结果反向选择窗口或阈值，也不得直接晋级现有 V23。

## 2026-08-02：V24 跌回横盘上沿但不要求再创新高 L1/L2

- [x] 冻结唯一变量：保留 V23 的 8 根横盘、首次放量收盘突破、两根确认窗口和全部质量/风险
  合同，只删除确认棒最高价必须超过突破棒最高价。
- [x] 新增独立 V24 Research 版本与 CLI 变体；V23 继续要求再次扫过突破高点，旧版本身份和
  行为未被覆盖，V24 止损仍冻结在两棒最高点加 1 tick。
- [x] 增加 V23 拒绝而 V24 接受“不创新高但跌回上沿”样本的边界测试；SHIB 在 V23/V24 中
  都继续因不是横盘首次突破而拒绝。策略库 149/149、Top60 入口 23/23 通过。
- [x] 同一冻结数据身份完成 L1：V23 7 个候选，V24 14 个；原 7 个全部保留并新增 7 个，
  V24 覆盖 12 币、7 个 UTC 月份和 14 个独立事件，实际增加 100%。
- [x] L1 通过后才打开 outcome 做 L2；V24 成本后 `-8.1333R`、平均 `-0.5809R`、
  `PF_R 0.4156`，新增 7 笔单独为 `-4.2096R`、平均 `-0.6014R`。
- [x] 新增样本分布于 7 币、6 个月和 7 个事件，但零成本仍为 `-1.1995R`；按成本后边际门禁
  停止在 L2，保留 Research-only，不创建 Pine、不接 Paper、ReadOnly、Live 或生产调度。
- [x] 逐笔拉取 14 笔对应的本地确认 15m K 线并完成交易解剖；12 个币中 ZRO、IOST 各两笔，
  4 笔止盈、10 笔止损，10 笔亏损全部在入场后 5 根内触发冻结止损。
- [x] 核对亏损机制：毛保本胜率约 36.7%、实际 28.6%；每边 8 bps 压力使平均成本侵蚀
  `0.359R/笔`，并在 BNB 的 0.130% 窄风险上把 `-1R` 放大到 `-2.235R`。
- [ ] 若继续，只能另立无标签 L1，逐项统计固定 8 根横盘的长度、触边或漂移拒绝数量；一次
  只改变一个变量，不得根据本轮 14 笔盈亏反向选择参数。

## 2026-08-02：15 分钟动量布林长影中轨过滤与首次减半 L1/L2

- [x] 另立“信号 K 收盘接近中轨”单变量 L1，结果前冻结主阈值为距中轨不超过 0.25 个
  半带宽；0.10 与 0.50 只作预注册敏感性，不允许结果后替换主阈值。
- [x] 同一 673 个基础 setup 中，0.10/0.25/0.50 分别命中 3/8/56 个；主阈值只影响
  `1.1887%`，未达到预注册覆盖门槛，按 L1 停止且未读取 outcome。
- [x] 将用户所说 `0.5R` 归一为“首次因果回归中轨平掉原始仓位 50%”，并把余仓移保本、
  对侧外轨/`+5R` 拆成后续独立变量；本批只研究首次中轨减半。
- [x] L1 几何检查确认 673/673 个 setup 的计划限价与原目标之间存在合法中轨路径，覆盖
  做多 340、做空 333、44 个币、13 个月和 369 个事件，允许进入 L2。
- [x] 结果前冻结 L2 时序、成本和门禁；新增 Research-only 回放及 5 个边界测试，验证止损优先、
  上一根完成 K 的中轨、一次减半、同棒中轨/目标和旧限价盘中成交优先。
- [x] 只读本地 `quant_core` 完成 L2：673 个 setup 中 283 个限价成交，持仓冲突后 279 笔
  完整交易；110 笔触发中轨减半，做多/做空 47/63，覆盖 35 币和 89 个事件。
- [x] 基线成本后 `-52.2604R / -0.1873R每笔 / PF 0.7644`；减半变体
  `-48.1010R / -0.1724R每笔 / PF 0.7407`。总体改善 `+4.1594R`，但做多恶化
  `-0.6737R` 且 PF 下降，两项预注册门禁失败。
- [x] 状态固定为 `stop_at_l2_keep_research_only`；不继续叠加余仓保本或对侧外轨/`+5R`，
  不创建 Pine，不接 Paper、ReadOnly、Live、策略注册或生产执行。

## 2026-08-02：V25 横盘方向效率 L1/L2

- [x] 将 ICP 2026-06-29 09:00 误报归因到 8 根横盘只比较极值、不检查收盘路径；目标窗口
  05:45～07:30 的方向效率为 `0.52`。
- [x] 结果前冻结唯一变量 `abs(C8-C1)/sum(abs(Ci-C(i-1)))`，主阈值为 `0.35`，`0.30/0.40`
  只作同变量邻域；V24 的横盘、首次突破、确认、止损、目标和成交合同保持不变。
- [x] 新增独立 V25A/B/C Research 版本和 CLI 变体；ICP 在三档均拒绝，SHIB 继续拒绝，
  V24 原行为和版本身份未覆盖。
- [x] 同一数据指纹完成 L1：V24/V25A/V25B/V25C 分别为 14/9/10/11 个候选；主候选过滤
  4 笔并保留 8 币、6 个月、10 个独立事件，满足预注册覆盖门禁。
- [x] 只对预注册 V25B 打开 L2 标签：零成本 `+0.886R / PF 1.148`，成本后
  `-3.259R / -0.326R每笔 / PF 0.640`；成本后边际仍为负，停止在 L2。
- [x] parity library 151/151、Top60 CLI 3/3、Rust 格式和 2,000 行硬上限通过；批次只保留
  预注册、机器结果和结论三份研究产物，未创建 Pine 或接入任何运行态。
- [ ] 若继续，另立 L0 只定义“初始风险距离相对固定交易成本的最低倍数”；不得在本批次
  同时调整方向效率、确认棒、止损或目标。

## 2026-08-02：15 分钟动量布林长影近期 EMA12 领先双慢线 L1

- [x] 结果前冻结唯一变量：做空检查 `EMA12>EMA144 && EMA12>EMA576`，做多镜像；
  EMA144/576 相对顺序无关，连续年龄 `1..=192` 根时拒绝，明确排除连续三根和斜率。
- [x] 保持既有 Momentum V2 量比、周 P90、96 根净移动、实体、方向长影和限价定义先执行，
  再检查 Bollinger(20,2.5) 外轨，EMA 只做最后否决。
- [x] 新增独立 Research-only 模块、只读 CLI、全候选信号时账本、5 个目标样本复核和年龄
  边界测试；旧 `false -> true` EMA 研究版本未覆盖。
- [x] 5 个目标样本年龄精确复算为 AGLD 3、YFI 62、NMR 158、ORDI 53、SATS 29，全部被
  新规则拒绝，证明漏判定义已修复。
- [x] 同一 673 个基础 setup 全部 EMA-ready；537 个被拒绝，占 `79.7920%`，覆盖做多/做空
  261/276、43 个币、13 个月和 313 个事件。
- [x] 影响比例超过预注册上限 45%，状态为 `stop`；按 L1 门禁没有读取 outcome，也没有执行
  L2、退出叠加、Pine、Paper、ReadOnly、Live、数据库或生产改动。
- [x] 定向单元测试 6/6 通过；机器报告 SHA-256 为
  `93711524b2bd33d6dcf4ab93759807d8cbfc5ad7857dcb9c07d97c81cf5d447d`。
- [ ] 若继续，必须返回 L0 冻结更有选择性的“早期趋势事件”单变量；不得在本批次查看盈亏后
  缩短 192 根阈值或叠加均线距离来追求通过。

## 2026-08-02：15 分钟动量布林长影近期 EMA12 领先 96 根 L1

- [x] 按用户要求另立 `age<=96` Research 版本，只把连续年龄上限从 192 改为 96；方向、
  EMA12 对双慢线关系、Momentum V2、Bollinger(20,2.5) 及原停止条件保持不变。
- [x] 参数化现有扫描器为两个固定版本入口，旧 192 根规则仍可复现；没有开放任意阈值参数，
  避免运行时身份漂移。
- [x] 新增 96/97 边界测试；同一 673 个 setup 全部 EMA-ready，96 根版本拒绝 423 个，
  影响 `62.8529%`，做多/做空为 204/219，覆盖 43 币、13 月和 245 个事件。
- [x] 相比 192 根版本少拒绝 114 个、降低 `16.9391` 个百分点，但仍超过 45% 覆盖上限。
- [x] AGLD、YFI、ORDI、SATS 继续拒绝；NMR 年龄 158 被放行，原 5/5 目标门禁失败。
- [x] 状态为 `stop`，未读取 outcome、未进入 L2，也未修改退出、Pine、Paper、ReadOnly、
  Live、数据库或生产路径。
- [x] 机器报告 SHA-256 为
  `63f9c36660b18386660bfa63443f1801a2b40b5ba5d298631e8d8aac9f994e8e`。

## 2026-08-02：15 分钟动量布林长影首次重测收回确认 L1

- [x] 结果前冻结唯一变量：来源 setup 后首次重测方向极值时，必须等待该 K 收盘重新进入
  当根 `Bollinger(20,2.5)`；确认时间为该 K 收盘，最早下一根开盘激活。
- [x] 使用独立候选键 `market_momentum_bollinger_wick_reentry_confirmation_15m_v1`；未叠加
  已停止的 age96、中轨减仓、TTL、止损或目标变化。
- [x] 同一 673 个来源 setup 中，283 个发生首次重测，197 个确认、86 个带外失效；确认覆盖
  做多/做空 81/116、43 个币、13 个月和 148 个事件。
- [x] 十个固定失败目标全部找到，但只有 YFI、EIGEN、OP 三个被拒绝，未达到预注册至少五个；
  NMR、SATS、WIF、BABY 虽收盘越过来源高点，仍因动态上轨扩张而被确认。
- [x] 状态为 `stop`；未读取 outcome、未进入 L2，5 个定向测试和目标文件行数检查通过。
- [ ] 若继续，返回 L0 只冻结一种确认边界：动态外轨、来源极值或冻结来源外轨三选一；不得
  在本批根据目标结果叠加多个边界。

## 2026-08-02：V26 最长有效父横盘 L1/L2

- [x] 结果前冻结唯一变量：V25B 的固定 8 根/48 根旧窗口改为紧贴突破棒的 8～96 根有效
  父横盘；先按 P90/P10、触碰、容纳、漂移和方向效率选择最长父区，再判断收盘突破。
- [x] 新增独立 `volume_active_parent_horizontal_first_break_close_back_short_15m_research_v26`
  身份；V25B 保持原样，确认棒仍不要求再次超过突破棒高点。
- [x] ALGO 指定样本由旧 02:00～03:45 的 8 根上沿 `0.08495`，纠正为 7 月 14 日 16:00
  至 15 日 13:30 的 87 根父横盘上沿 `0.08520`；突破收盘 `0.08512` 未突破，信号被拒绝。
- [x] ICP 指定修复和 SHIB 趋势延续继续拒绝；L1 形成 203 个候选，覆盖 41 币、13 月、
  167 个事件。相对 V25B 删除 5、保留 5、新增 198，明显超出预注册覆盖预期但通过正式门禁。
- [x] L2 203 笔为 58 胜 145 负；零成本 `-20.687R / -0.102R每笔 / PF 0.857`，成本后
  `-64.356R / -0.317R每笔 / PF 0.634`，四个父横盘长度档全部为负。
- [x] parity library 153/153、Top60 CLI 23/23、release 构建和格式检查通过；机器结果
  SHA-256 为 `3b6586f34dadec2bd30eb9c3530ccb578eda7615c955c2af46f2dbd8d9d74a09`。
- [x] 状态固定为 `stop_at_l2_keep_research_only`；未创建 Pine，未接 Paper、ReadOnly、Live、
  数据库写入或生产调度。
- [ ] 若继续，只能另开 L0/L1 定义一个“跌回父横盘后的真实拒绝接受”变量，不得在本批次
  根据 203 笔结果同时调整 RSI、突破幅度、确认棒、止损和目标。

## 2026-08-02：V27 突破实体完全否定 L1/L2

- [x] 结果前冻结唯一变量：V26 全部合同不变，第 1/2 根确认棒收盘必须不高于已完成突破棒
  开盘价，完整否定突破棒上涨实体。
- [x] 新增独立 `volume_active_parent_horizontal_breakout_body_rejection_short_15m_research_v27`
  身份；V26 行为、账本形状和 203 个候选保持可复现。
- [x] L1 无标签保留 65/203（32.02%），覆盖 36 币、13 月和 62 个事件簇；新增 0、删除
  138，ALGO/ICP/SHIB 三个冻结反例继续为 0。
- [x] 65 个保留身份的信号、冻结风险和 outcome 与 V26 对应身份均无漂移；账本完整记录突破
  开盘、确认收盘和实体否定 tick 深度。
- [x] L2 65 笔为 24 胜 41 负；零成本 `+11.298R / +0.174R每笔 / PF 1.276`，成本后
  `-2.107R / -0.032R每笔 / PF 0.957`，冻结成本拖累 `13.405R`。
- [x] parity library 154/154、Top60 CLI 23/23 和 release 构建通过；机器结果 SHA-256 为
  `bf9752dfd789274d4c80b4a120c836ceef264dbc60d2d9499d606bb60ed732ef`。
- [x] 状态固定为 `stop_at_l2_keep_research_only`；未创建 Pine，未接 Paper、ReadOnly、Live、
  数据库写入或生产调度。
- [ ] 若继续，另开 L0/L1，只研究按父横盘高度归一化的实体否定深度；必须在读取盈亏前冻结
  唯一比例阈值，不得从本轮 tick 深度分布事后挑参数。

## 2026-08-02：V28 父横盘归一化实体否定 10% L1/L2

- [x] 在覆盖率和逐笔 outcome 前冻结唯一变量：V27 首个有效确认的
  `(breakout_open-confirmation_close)/(anchor_upper-anchor_lower) >= 0.10`；其余合同不变。
- [x] L1 无标签保留 V27 的 38/65（58.46%），覆盖 24 币、9 个有信号月份和 36 个事件簇；
  归一化证据缺失 0，ALGO/ICP/SHIB 三个冻结反例继续为 0。
- [x] 初次实现发现 TURBO 第 1 根只差深度后继续等待第 2 根会制造新身份；在读取 V28 outcome
  前修正为立即消费 setup，最终新增身份 0，保留身份的信号、冻结风险和 outcome 漂移 0。
- [x] L2 38 笔为 12 胜 26 负；零成本 `-0.296R / -0.008R每笔 / PF 0.989`，成本后
  `-8.168R / -0.215R每笔 / PF 0.734`，成本拖累 `7.872R`。
- [x] V28 保留队列成本后亏 `-8.168R`，被删除的 27 笔反而赚 `+6.061R`，确认 10% 深度门禁
  产生反向选择；只有 2/9 个有信号月份成本后为正。
- [x] parity library 155/155、Top60 CLI 23/23 和 release 构建通过；机器结果 SHA-256 为
  `a3f32962c72afaf0a485dfd9e0405756021d80ef09c688576ec013d96ff1edfc`。
- [x] 状态固定为 `stop_at_l2_keep_research_only`；未创建 Pine，未接 Paper、ReadOnly、Live、
  数据库写入或生产调度。
- [ ] 若继续，禁止再搜索归一化深度阈值；必须另开预注册版本，只选择一个入场几何或父横盘
  质量变量，并在 outcome 前先做无标签覆盖。

## 2026-08-02：V29 父横盘浅突破 10% L1/L2

- [x] 在突破幅度分布和 outcome 前冻结唯一变量：V27 全部合同不变，首次突破收盘超出父横盘
  上沿的幅度除以父横盘高度不得超过 10%；未叠加已经失败的 V28 确认深度。
- [x] L1 无标签保留 V27 的 27/65（41.54%），覆盖 21 币、9 个有信号月份和 26 个事件簇；
  三个冻结反例继续为 0，归一化证据完整。
- [x] 初次实现提前拒绝强突破 pending，导致后续 ALGO 浅突破制造 1 个新身份；在读取 outcome
  前改为完整推进 V27 pending、只在 V27 本应发信号的位置过滤，最终新增与风险漂移均为 0。
- [x] L2 27 笔为 12 胜 15 负；零成本 `+11.666R / +0.432R每笔 / PF 1.778`，成本后
  `+5.128R / +0.190R每笔 / PF 1.277`，成为本分支首个成本后正向版本。
- [x] 删除的 38 笔成本后为 `-7.235R`，证明门禁有分层作用；但移除最大两笔后净 R 变为
  `-0.565R`，且 EV/PF 远低于 `0.6R/2.2`，集中度门禁失败。
- [x] parity library 155/155、Top60 CLI 23/23 和 release 构建通过；机器结果 SHA-256 为
  `1adf5ba6253af4c253a2a4359a8d3babe748e07f3a106a60f73b4c9757b33c74`。
- [x] 状态固定为 `stopped_at_l2_keep_research_only`；未创建 Pine，未接 forward shadow、
  Paper、ReadOnly、Live、数据库写入或生产调度。
- [ ] 冻结 V29；下一步只允许用冻结点之后的新 15m 数据做同合同 forward OOS，不在当前账本
  搜索 5%/15%/20% 或叠加 RSI、EMA、确认棒、止损和目标。

## 2026-08-02：15 分钟动量布林长影来源极值收回确认 L1/L2

- [x] 在查看全账本分布前冻结唯一边界变化：首次重测做空必须严格收回来源 setup high，
  做多镜像严格收回来源 setup low；等于边界拒绝，首次失败后不等待第二次重测。
- [x] 只消费冻结的动态布林 L1 无标签账本，并校验报告 SHA、来源候选账本、行情指纹、
  673 个来源 setup、283 个首次重测以及 `outcome_evaluation_performed=false`。
- [x] L1 确认 143、拒绝 140，做多/做空确认 56/87，覆盖 41 币、13 月和 114 个事件；
  固定十笔失败样本拒绝 YFI、NMR、SATS、WIF、BABY 共 5 笔，全部门禁通过。
- [x] L1 通过后才冻结 L2：同一 143 个确认 setup 配对比较来源极值被动成交与确认后下一根
  开盘，保持来源 ATR、1.5 ATR 止损、量比分档目标、成本、48 小时和共同同币种冲突不变。
- [x] L2 成功重建 143 个 pair，共同冲突过滤 4 个；139 个完整 pair 覆盖做多/做空 55/84、
  41 币、13 月和 110 个事件，风险与退出合同一致性通过。
- [x] 配对基线成本后 `+27.1246R / +0.1951R每笔 / PF 1.2897`；下一根开盘候选为
  `-12.4691R / -0.0897R每笔 / PF 0.8834`，净增量 `-39.5938R`。
- [x] 下一根开盘在 139 笔中 137 笔相对影线极值成交更差，平均牺牲 `0.4806R`；13 笔由
  基线止盈翻为候选止损，仅 2 笔反向改善，多空净增量都为负。
- [x] 状态固定为 `stop_at_l2_keep_research_only`；L1 5/5、L2 4/4 测试通过，未创建 Pine，
  未接 Paper、ReadOnly、Live、数据库写入、策略注册或生产执行。
- [ ] 若继续，返回 L0 只定义“确认后有限期等待来源极值再挂单”；必须在结果前冻结 TTL 和
  过期处理，不得同时改变止损、目标或中轨/5R 退出。

## 2026-08-02：EMA144/576 重扩张稳定成交额面板 V11/V12

- [x] V11 在结果前冻结“V6 重扩张完成 K 同时出现同方向、`delta_rank>=0` 的 96 根成交额
  排名事件”；54,837 个 V6 候选保留 41,564 个，减少 24.2045%，但用户目标只命中 1/3，
  按 `rejected_definition_mismatch` 停止且未读取 outcome。
- [x] 只读定位两个 BTC 漏判：两根重扩张 K 都是上涨 K、BTC 排名 1、`delta_rank=0`；CVX
  的正成交量窗口不完整使全员面板只有 43/44，旧完整面板政策抹掉了整张排名快照。
- [x] V12 只把面板政策改为覆盖至少 95%，且相邻快照实际成员集合完全一致；生产完整面板
  加载器保持原行为。L1 保留 48,048 个候选，减少 12.3803%，多空 19,824/28,224，覆盖
  44 币、13 月、3,917 个事件，三张用户目标 3/3。
- [x] L1 通过后才冻结 L2：继续使用 `EMA144±0.30ATR` 因果限价、4% 止损、0.52R、24 小时、
  8 bps/side、同棒止损优先与同币种单持仓锁，只过滤 V12 授权身份。
- [x] L2 共 16,941 笔；毛 `EV +0.023886R / PF 1.083335`，成本后
  `EV -0.016095R / PF 0.946614`。多头 `-0.039634R / PF 0.876428`，空头仅
  `+0.002157R / PF 1.007527`，移除头部交易或事件后仍为负。
- [x] V12 状态固定为 `stop`，未创建 Pine，未注册 Paper/ReadOnly/Live，未改生产 preset、
  数据库或执行路径；排名测试 8/8、EMA 聚焦测试 38/38 和新增文件行数检查通过。
- [ ] 若继续，必须回到新的 L0 单变量假设；禁止在本批次根据 V12 outcome 搜索排名、止损、
  目标或面板覆盖率。当前证据不允许把该形态加入运行中的 15 分钟动量策略。

## 2026-08-02：EMA144/576 稳定面板结构目标 V13

- [x] 在读取 outcome 前冻结唯一变量：V12 固定 `0.52R` 改为信号开始前已确认的最近
  2 左 2 右盈利侧摆动高/低；只回看 96 根已完成 15m K，不设置目标 R 阈值。
- [x] L1 有效结构目标 48,019/48,048，覆盖 99.9396%；仅 29 个无目标，多空
  19,808/28,211，覆盖 44 币、13 月、3,918 个事件，用户目标 3/3。
- [x] 结构目标 R 的信号时分布为中位数 0.301026R、P90 0.750370R；这些数值只诊断、
  不筛选，L1 `outcome_evaluation_performed=false`。
- [x] L1 通过后才冻结 L2：V12 限价、实际成交 4% 止损、24 小时、8 bps/side、同棒
  止损优先和同币种持仓锁不变，只消费 L1 的绝对结构目标价。
- [x] L2 共 18,337 笔；毛 `EV +0.008861R / PF 1.033335`，成本后
  `EV -0.031118R / PF 0.888715`。多头 `-0.060354R`、空头 `-0.010134R`，均为负。
- [x] 逐笔合同校验通过；移除最优两笔或最高事件后仍为负。V13 固定为 Research-only
  `stop`，未创建 Pine，未接 Paper/ReadOnly/Live，未改生产 preset、数据库或执行路径。
- [x] EMA144/576 聚焦回归 42/42、成交额排名回归 8/8、Rust 格式检查和定向代码行数
  检查通过；V13 新增核心文件均低于 1,000 行目标。
- [ ] 若继续，回到 L0 只定义一个信号时入场质量变量；不得根据 V13 outcome 事后加入
  结构目标 R 下限、上限、分档或同时改变止损。

## 2026-08-02：rust_quant_alpha 首个 Vegas 冻结迁移闭环

- [x] 冻结 `vegas / legacy-mysql-1 / ETH-USDT-SWAP / 4H`、14,372 根旧混源 K 线和
  `quant_core.back_test_log:17196`；该 Dataset 永久标记为 legacy parity、不可晋级。
- [x] 一次迁完 Feature、10,773 次 Strategy 决策、Research-owned 旧仓位/风险/退出状态机、
  447 笔交易、894 条明细、447 个权益点和五项汇总指标。
- [x] 纠正 Owner：Strategy 只保留候选保护证据；旧账户风险与报表口径隔离在
  `research/simulation/legacy_vegas_v1`；Quant 只保留通用指标、显式成本和纯统计公式。
- [x] 删除 Strategy 重复 Candle 结构，EMA 集合泛化为任意固定数量，吞没阈值改由 Vegas
  显式传入；同步能力总账、W2 plan/evidence 与 `rust-quant-architecture` 技能。
- [x] 最终 Feature/Signal 指纹分别为
  `b6957fbb666767d5018b04951215b6c2f911df647994588656bd418e95bae392`、
  `5a71e0b01743cd7d046f94e2d6ce9e616cf3c4aeed72661c7b49c016a01b4d22`；交易、权益和
  summary 浮点按 IEEE-754 bits 全部一致，最终资金 `4421.393678523676`。
- [x] 全工作区 351 个测试通过、9 个预期忽略；W2 scoped Clippy 与 `arch-check` 通过。
- [ ] 整个 W2 仍有 31 个 planned/implementing capability；canonical OKX/Binance 研究、
  Strategy release、signal-worker、L0～L3 晋级和生产切换另开后续闭环，不复用本 parity 资格。

## 2026-08-03：EMA576 首次突破后 EMA144 回踩守稳 V1 L1

- [x] 撤销旧 V12/V13 作为本形态最优版本的解释，冻结独立 Research 身份；长期状态为连续
  144 根 EMA144/576 关系加至少 80% 价格位于慢线规定一侧，两收盘确认首次突破。
- [x] 实现首次回踩 `EMA144±0.30 ATR14`、576 根等待、成功或失败一次性消费和空头镜像；
  9 个状态机测试全部通过，新增核心文件均低于 1,000 行。
- [x] L1 仅使用信号时可见字段：2,066 个候选，多空 1,221/845，覆盖 44 币、13 月和
  1,129 个一小时事件；未读取成交、未来 K、成本、R、胜负或 PnL。
- [x] NMR 7 月 1 日、BTC 7 月 2 日命中；ALGO/MERL 两个错误空单时间零触发。BTC 7 月 2 日
  验收窗口的转录错误已更正为 06:00～12:00，实际信号为 08:30，策略账本没有改变。
- [x] BTC 7 月 12 日未命中，五张图门禁仅 4/5；V1 固定为
  `rejected_definition_mismatch`，不进入 L2、不创建 Pine、不接运行态。
- [ ] 下一步退回 L0，只确认一个优先变量：同一首次突破是否允许多次 EMA144 回踩再武装，
  或“不有效跌破”是否允许多 K 回收；两者不得在一轮同时修改。

## 2026-08-03：EMA576 / EMA144 用户定义 V2～V4 L1 生命周期迭代

- [x] V2 只把 V1 一次性 episode 改成最新方向独占、可重复离区再回踩；44,781 个候选、
  3,032 个事件，正样本 2/3、反样本 2/2，BTC 7 月 12 日因活动方向被空头接管而漏判。
- [x] V3 只把同 K 守稳改成收盘未越出 `0.30 ATR14` 缓冲；候选 49,282，仍为正样本 2/3、
  反样本 2/2，证明该漏判不是收盘缓冲导致。
- [x] V4 只把全局互斥活动方向改成多空分别保存；三张正样本 3/3，但 ALGO/MERL 两个错误
  空点同时复现，候选 92,661 并超过 80,000 上限，停止在 L1。
- [x] V2～V4 全程使用同一行情指纹，只读信号时字段；未执行 outcome、资金回放、Pine、
  Paper、ReadOnly、Live、数据库写入或生产变更。
- [ ] 下一步先由用户冻结“反向结构出现后旧方向何时失效/何时恢复”的唯一规则；禁止用五张
  图事后拟合 1,011～1,264 根之间的寿命阈值，也不得同时搜索 ATR、确认棒和退出。

## 2026-08-03：TradingView Velocity V30 父横盘三次边界交替

- [x] L0 后冻结唯一变量：V29 已选父横盘在突破前必须完成至少三次上下边界切换；固定
  20% 边界带，不改父区间选择、浅突破 10%、确认、风险或退出。
- [x] V30 以确认点严格过滤并消费同一 setup，保留 20/27 笔，覆盖 18 币、9 月、20 个
  事件簇；删除 7 笔、新增 0，风险、边界、浅突破证据和 outcome 漂移均为 0。
- [x] L2 成本后为 `-2.155R / -0.108R每笔 / PF 0.865`，差于 V29 的
  `+5.128R / +0.190R每笔 / PF 1.277`；删除队列反而为 `+7.284R / PF 3.818`。
- [x] 状态固定为 `stopped_at_l2_keep_research_only`；未创建 Pine，未接 Paper、ReadOnly、
  Live、数据库或生产策略。机器结果 SHA-256 为
  `536e812a4805d5cdf559425738a58699cd9ef32f7db82f1f623fb5eb44620135`。
- [ ] 若继续，把“视觉横盘”和“交易锚点”拆成独立任务；不得在本批次根据 outcome 搜索
  交替次数或 10%/15%/25% 边界带。

## 2026-08-03：TradingView 严格视觉横盘 V1（与 V29 交易边界拆分）

- [x] 冻结用途：新 Pine 只做视觉横盘标注，不产生开仓/退出/告警，不成为 V29 的新增门禁。
- [x] 实现 P90/P10 稳健边界、8～96 根离散父区间、双边独立触碰、80% 收盘容纳、上下沿
  漂移、0.35 方向效率及至少三次上下沿交替；同棒触及两侧不补造盘中顺序。
- [x] 只在完整 K 线收盘后确认；首次确认带因果标记，收盘离开冻结区间后结束绘制，突破棒
  不会在同一根反向创建新区间。
- [x] Pine 静态差异检查通过，脚本无 `strategy.*`、`alertcondition`、`request.security` 或
  lookahead；V29 三个核心 Rust 文件 SHA-256 与实施前完全一致。
- [x] 已通过本地 TradingView Desktop MCP 保存并加载 `15m Strict Visual Consolidation V1`：
  Pine 编译为 0 错误、0 警告，线上保存源码与仓库源码按换行归一后 276 行逐字一致。
- [x] 隔离隐藏旧横盘与 Velocity 指标后复核 ADAUSDT.P 15m：连续推进段不画区间；区间离开后
  必须从离开棒之后重新积累完整窗口，不能复用旧区间连续生成离区标签；当前最多保留 30 个
  区间及对应 60 个确认/离区标签。
- [x] 本地 Basic 方案只能读取 5,000 根 15m K 线，无法直接跳到 2026-05-28 目标旧图；已在
  当前可见 5,000 根内完成编译、对象读取与 2026-08-01～08-03 的放大图复核。

## 2026-08-03：EMA576 / EMA144 Episode V5～V10 L1 中断与建立顺序收敛

- [x] V5～V9 均保留独立预注册与机器报告并按定义冲突淘汰；全过程只读取信号时可见字段，
  未用收益、MFE/MAE 或未来 K 反向选择中断规则。
- [x] 按用户纠正将 BTC 日期链固定为：空头资格 7 月 16 日 14:45，最后 episode 7 月 17 日
  09:45，均线方向交叉 17 日 18:15，回踩消费武装 18 日 00:45，反向两收盘 EMA576 中断
  18 日 01:15；到 19 日 21:15 无新空头 episode。
- [x] V10 只增加“新 episode 的价格突破必须先于 EMA144/576 同方向交叉”门禁；episode
  建立后的交叉前/后 EMA144 回踩均保留，历史 144 根/80% 资格继续锁存。
- [x] 六张定义图通过 6/6；候选 26,656，多空 15,037/11,619，覆盖 44 币、13 月和
  3,333 个一小时事件；所有 L1 覆盖门禁通过。
- [x] V3～V10 生命周期聚焦回归 31/31 通过；V9/V10 新增核心文件分别 886/297 行，低于
  1,000 行目标，定向代码行门禁通过。
- [ ] 下一步只能新建 V10 的 L2 预注册后做本地多币种诊断；当前没有收益结论，不创建 Pine、
  不接 Paper/ReadOnly/Live，也不改生产配置。

## 2026-08-03：严格视觉横盘首次放量上破做多 V1 L1/L2

- [x] 冻结独立 Research 身份并保持 V20/V29 原行为；只新增“已确认严格横盘首次放量阳线
  收盘上破，下一根开盘做多”这一变量。
- [x] 实现只按完成棒推进的 Rust 横盘状态机；首次确认不能同棒突破，父边界升级只向后生效，
  离区后必须从下一根重新积累，结构离开无论是否满足量能都消费区间。
- [x] L1 无标签扫描得到 1,969 个候选、43 币、13 月、1,083 个 60 分钟事件簇，覆盖门禁通过。
- [x] 接入 Candidate V20 同一主回放、冲突、反手与风险退出路径；定向测试 26/26、两个二进制
  `cargo check` 和 Release 构建通过。
- [x] L2 新家族 1,377 笔：零成本 `-54.079R / -0.0393R每笔 / PF 0.944`，成本后
  `-340.731R / -0.2474R每笔 / PF 0.706`；13 月仅 2 月为正，43 币仅 7 币为正。
- [x] 主回放成本后 PF 从 0.907 降至 0.757，每笔 R 从 -0.0239R 降至 -0.1047R，最大回撤
  增加 5,240.645 固定价格单位；状态固定为 `stopped_at_l2_keep_research_only`。
- [ ] 不创建 Pine 策略、不接 Paper/ReadOnly/Live 或生产注册。若继续，另立单变量预注册研究
  “突破后接受/回踩确认”或“箱体结构风险合同”，不得在本轮 outcome 上组合调参。

## 2026-08-03：EMA576 价格先突破、EMA144 回踩 V10 L2

- [x] 在读取 outcome 前冻结下一根连续 15m 开盘、4% 止损、0.52R、24 小时、8 bps/side、
  同棒止损优先、同币种持仓锁和一小时方向事件归并；不接受命令行调参。
- [x] 校验冻结 L1 SHA、策略身份、无 outcome 门禁和行情指纹；重载后 26,656 个候选逐字段一致，
  下一根开盘、风险、退出、成本、事件和持仓锁合同逐笔复算通过。
- [x] 同币种锁后 8,671 笔完整交易，覆盖多空 4,900/3,771、44 币、13 月和 3,396 个事件；
  17,985 个重叠信号被阻塞，无 forward 不完整。
- [x] 零成本已经为 `EV -0.008041R / PF 0.973661`；8 bps/side 后为
  `EV -0.048018R / PF 0.850443 / -416.3624R`，多头、空头、交叉前后均为负。
- [x] 44 币仅 6 币净正、13 月仅 3 月净正；移除最好两笔或最好事件仍为负。V10 固定为
  Research-only `stop`，未创建 Pine，未接 Paper/ReadOnly/Live，未改生产配置或数据库。
- [x] V2～V10 与 L2 聚焦回归 43/43、二进制 `cargo check`、格式化和定向文件行数门禁通过；L2 主文件
  976 行。机器报告 SHA-256 为
  `ff97d13e0375db0898eca45d79fdbb191ae93e19c74af821cbd23b770a77834f`。
- [ ] 若继续，返回新 L0/L1 只验证“每个 episode 仅第一次有效 EMA144 回踩”；不得在本批次
  outcome 上同时搜索目标、止损、交叉阶段、成交量、斜率或 RSI。

## 2026-08-03：严格视觉横盘上破回踩接受 V2 L1/L2

- [x] 冻结唯一变量：V1 合格首次放量阳线上破不再直接入场，只等待其后 1～3 根第一次轻触
  冻结上沿且收盘仍在上方；跌回上沿立即失效，第三根未接受即过期。
- [x] L1 无 outcome 扫描：1,969 个突破源中接受 770 个、失效 457 个、过期 742 个；覆盖
  43 币、13 月、568 个事件簇，门禁通过。
- [x] 接入同一 Candidate V20 Rust 回放，冻结源棒 ATR、量比与目标档位；V1 同可执行文件结果
  与上一轮报告一致，证明本轮状态拆分未改变直接突破基线。
- [x] L2 V2 家族 550 笔：零成本 `-50.065R / -0.0910R每笔 / PF 0.872`，成本后
  `-167.088R / -0.3038R每笔 / PF 0.647`；43 币仅 7 币、13 月仅 1 月为正。
- [x] 511 笔配对样本平均改善 `0.1806` 源 ATR 入场价，却比 V1 再少 `4.226R`；接受条件
  筛中的 V1 源队列也比未接受队列更差，确认该几何发生反向选择。
- [x] 定向测试 28/28；拆分后 `signals.rs` 1,987 行。状态固定为
  `stopped_at_l2_keep_research_only`，未创建 Pine，未接 Paper/ReadOnly/Live 或生产。
- [ ] 停止搜索回踩等待根数和距离阈值；如继续，回到新 L0 提出不同的单变量，不得基于本轮
  outcome 叠加 EMA、RSI、K 线颜色、结构止损或目标调参。

## 2026-08-03：严格视觉横盘突破实体中点守稳 V3 L1/L2

- [x] 在读取 outcome 前冻结唯一变量：V2 首次回踩接受确认收盘必须不低于冻结突破棒实体
  中点；未守住时立即消费来源，不等待后续 K 线补开。
- [x] L1 保留 627/770 个 V2 候选，拒绝 143 个；覆盖 43 币、13 月和 483 个事件簇。
  两笔指定 BNB 被拒绝、ADA 2026-07-18 08:15 被保留；新增原始信号为 0。
- [x] 同一数据与可执行身份 L2：V3 家族 445 笔，零成本
  `-23.945R / -0.053809R每笔 / PF 0.923006`；成本后
  `-118.606R / -0.266531R每笔 / PF 0.684766`。
- [x] 被删除的 106 笔 V2 已执行队列为 `-45.752R / -0.431622R每笔 / PF 0.529463`，
  说明过滤有弱正向选择；但 444 笔共同保留队列仍为负，未达到晋级门槛。
- [x] ADA 目标样本盘中确认达到 1R，但冻结目标为 0.1714，未配置 1R 退出，最终止损；该
  观察仅记录为新的退出假设线索，不反向改变本轮入场结论。
- [x] 状态固定为 `stopped_at_l2_keep_research_only`；未创建 Pine，未接 Paper、ReadOnly、
  Live 或生产。按预注册停止，不启动横盘长度止盈批次。
- [ ] 若用户决定继续退出研究，另开 L0/L1 单变量预注册；不得同时搜索横盘长度阈值、1R/
  部分止盈/保本及新的入场过滤器。

## 2026-08-03：EMA576 首信号后 24 小时交叉确认超时 V11 L1/L2

- [x] 冻结唯一变量：旧资格链首个交叉前信号启动一次 96 根 15m K 计时；到期未完成方向性
  EMA144/576 交叉则清空 episode 与旧资格，后续突破和信号均不能重置截止点。
- [x] L1 保留 ALGO 07-15 06:00 首信号，07-16 06:00 准时失效，07-16 07:15 陈旧突破未建
  episode；六张既有目标图 6/6 通过。
- [x] V10 的 26,656 个候选降至 17,041 个，删除 9,615、新增 0；覆盖 44 币、13 月和
  3,108 个事件，2,533 个计时中 982 个确认、1,543 个超时。
- [x] L2 同口径 5,791 笔：零成本 `EV -0.019244R / PF 0.938159`，8 bps/side 后
  `-342.920R / EV -0.059216R / PF 0.819124`；多头、空头均负。
- [x] V11 总亏损因少做 2,880 笔而减少 73.442R，但相对 V10 的单笔 EV 下降 0.011198R、
  PF 下降 0.031319；被删除/重排的 V10 队列反而只有 `EV -0.026606R`，属于反向选择。
- [x] 状态固定为 `stop / Research-only`；状态机 3/3、共用 L2 合同 6/6，通过 L1 候选逐字段
  和 L2 逐笔合同校验；机器报告 SHA-256 为
  `4a0c9245ca5fe26f14b6bf2937ab0ec15273a7ed11629b714f8d39e09a4ebcb8`。
- [ ] 不继续搜索 4h/8h/12h/48h 超时阈值。若继续，下一轮只做“有效站上 EMA576 后移动
  止损到开仓价”的独立 L0 定义，不同时调整 24h、入场、初始止损或目标。

## 2026-08-03：严格视觉横盘短区间 1R 目标 V4 L2

- [x] 原 L1 比例上限因 502/627=`80.0638%` 超出 80% 一笔而如实暂停；用户在未读取 V4
  outcome 前确认保留 `<=32根 -> 1R`，并以短/长分支的候选、币种、月份和事件簇绝对覆盖
  修订门禁，原失败预注册保持不变。
- [x] 新增独立 Research-only V4 身份；横盘长度和短分支在突破源完成时冻结，V3 信号状态机、
  1.5 ATR 止损、长区间量能目标与高优先级特殊退出保持不变。
- [x] 同一 Release/行情身份下，V3/V4 严格家族均为 445 笔且逐笔信号身份差异为 0；356 笔
  进入短分支，其中 335 笔 Fixed 目标全部满足 `target_ticks == stop_ticks`，21 笔特殊退出与
  89 笔长区间的冻结目标差异为 0。
- [x] V4 家族零成本 `-15.377R / EV -0.034556R / PF 0.937490`；成本后
  `-110.045R / EV -0.247293R / PF 0.630237`。相对 V3 少亏 8.561R、胜率升至 44.04%，
  但 PF 下降且仍未通过 `EV > 0 && PF > 1`。
- [x] ADA 2026-07-18 08:15 从 V3 的 `-1.1904R` 修复为 V4 的 `+0.8080R`；全体 335 笔中
  1R 救回 66 笔，同时截短 92 笔原量能目标赢家。8 根与 24 根队列反而恶化，长度不是单调
  的目标选择变量。
- [x] 提前退出额外释放 ACT/BAT 两笔其他家族亏损交易，合计 `-2.368818R`；完整回放总 PF
  从 0.830419 降至 0.807148，最大回撤从 13,728.76 增至 14,400.88 固定价格单位。
- [x] 格式检查、V4 聚焦 18 个库测试、CLI 参数测试和账本标签边界测试通过；Release 构建与
  两次约 125 秒完整回放通过。既有 `old_range_cannot_reappear...` 状态测试仍独立失败，本轮
  未修改横盘状态逻辑以免引入第二研究变量。
- [ ] V4 固定为 `stopped_at_l2_keep_research_only`；不创建 Pine、不接 Paper/ReadOnly/Live
  或生产。不得在当前 outcome 上继续搜索长度阈值或 1R/1.5R/2R。

## 2026-08-03：EMA576 首笔真实成交 V12 / EMA144 结构止损 V13

- [x] 冻结 V12 唯一变量：同一 `symbol × direction × setup_ts` 只允许第一笔真实成交；缺 K、
  无效风险价或同币种持仓锁阻塞不消费 setup，首笔成交后保留后续 L1 信号用于审计但禁止再开仓。
- [x] V12 从 V11 的 17,041 个候选得到 2,684 笔完整交易，14,230 个后续候选被
  `setup_already_filled_once` 阻塞；覆盖 44 币、13 月、1,406 个事件。
- [x] LAYER 指定链仅保留北京时间 07-19 10:15 信号、10:30 入场；11:45 信号仍在 L1 账本，
  但不得于 12:00 再成交。V12 成本后 `-164.693R / EV -0.061361R / PF 0.811146`，停止在 L2。
- [x] 独立冻结 V13 唯一变量：在 V12 成交生命周期上，把初始止损改为信号 K 已知的
  `EMA144 ± 0.30 × ATR14`，并以实际入场到结构止损的固定风险继续计算同一 `0.52R` 目标。
- [x] V13 得到 2,703 笔完整交易、37 个 `initial_stop_not_beyond_entry` 阻塞；止损距离中位数
  仅 0.4442%，8 bps/side 成本换算的中位成本为 0.3603R，成本后
  `-1768.009R / EV -0.654091R / PF 0.140181`，结构止损版本停止在 L2。
- [x] 共用回放聚焦测试 8/8、三个 Release bin 构建、V11 历史报告逐字段 parity、V12/V13
  已保存报告逐字段 parity 均通过；L2 主文件拆至 983 行。机器报告 SHA-256 分别为
  `c8bce8a28aaa742ab04d89db1eb93b89a616cb94a0d93b050a4b1ca17bbc28b3`、
  `6e231adbe25c4fb018cfb0c89e6d13e47904cf2dd7c364fe329fa5a56674818a`。
- [ ] 两版均保持 Research-only，不接 Paper、ReadOnly、Live 或生产。若继续，返回新 L0/L1
  单独验证“确认收盘距 EMA144 过远则不入场”；不得同时调整结构缓冲、目标或成本口径。

## 2026-08-03：严格视觉横盘突破强度 V5 / 弱离区观察期 V6

- [x] V5 只新增突破棒 `实体/振幅 >= 60%` 且方向实体涨跌幅 `>= 0.25%`；上下离区镜像，弱离区
  结束当前横盘但不形成锚点，继续继承 V3 入场与退出合同。
- [x] V5 L1 从 V3 的 1,969 个来源拒绝 688 个弱来源，候选由 627 降至 351；两笔用户指定
  BTC 弱突破均被拒绝，覆盖 43 币、13 月和 295 个事件簇。
- [x] V6 单独把 V5 弱离区立即消费改为一根完成棒观察期：下一根回到含边界的冻结区间则恢复，
  仍在任一侧区外则消费，确认棒不得补算旧横盘强锚点。
- [x] V6 L1 共 18,301 次 pending，其中 6,729 次紧邻回区（36.77%）；最大等待 1 根、未决 0、
  区外确认补锚点 0，候选由 V5 的 351 增至 421。
- [x] 同一 Release 与行情身份完成 V5/V6 L2：V6 严格家族 290 笔，成本后
  `-61.0147R / EV -0.2104R / PF 0.7502`；V5 为
  `-55.7493R / EV -0.2285R / PF 0.7271`。
- [x] V6 新增 58 笔零成本 `+9.4250R`，计成本后转为 `-3.6660R`；同时删除 V5 成本后
  `+1.5994R` 的 12 笔，Candidate V20 总净 PnL 再恶化 903.61。
- [ ] V6 固定为 `stopped_at_l2_negative_cost_adjusted_edge`；不进入 L3、不修改正式 Pine、不接
  Paper/ReadOnly/Live 或生产。若继续，必须另立“突破接受质量”的单变量 L0/L1。

## 2026-08-03：EMA576 确认收盘距 EMA144 上限 V14 L1/L2

- [x] 在查看 V14 outcome 前冻结唯一变量：信号完成 K 的
  `abs(close - EMA144) / ATR14` 上限；V13 首笔真实成交、结构止损、目标、成本和冲突全部不变。
- [x] 同一冻结 V11 账本无标签扫描 0.50/0.75/1.00 ATR 三个 cap；按“拒绝 LAYER 目标后取最大
  cap”事前规则唯一选中 1.00 ATR，保留 15,024/17,041 个候选，覆盖 44 币、13 月和 3,128 个事件。
- [x] LAYER 北京时间 07-19 10:15 距离 `1.463463 ATR` 被拒绝；11:45 距离
  `0.298203 ATR` 按冻结“首笔真实成交”语义仍保留，并于 12:00 入场、12:30 结构止损，成本后
  `-1.279745R`。
- [x] V14 L2 完整交易 2,651 笔、1,423 个事件；零成本
  `EV -0.115866R / PF 0.723030`，成本后
  `-1923.961R / EV -0.725749R / PF 0.106533`，多空、44 币和 13 月均负。
- [x] 归因完成：V13 被删除的 391 笔远距离首笔交易仅
  `EV -0.203669R / PF 0.517180`；共同 2,312 笔为
  `EV -0.730266R / PF 0.107304`，释放的 339 笔后续交易为
  `EV -0.694945R / PF 0.100968`，确认过滤产生反向选择并放大成本占 R。
- [x] 4 个 V14 边界测试、两个 Release 入口、L1/L2 重跑 parity 和 2,312 笔共同交易合同漂移
  校验通过；L1 与合并机器报告 SHA-256 分别为
  `23edf396fe1b82681789c9323d38a30b17e1defa03d9d8b4551ac2f059e7b475`、
  `099827aa7924f2e754fa5926e6b50299eaedfc03aa9c5ddc284ce2bf4fed51c1`。
- [ ] V14 固定为 `stop / Research-only`，不搜索其他距离 cap。下一步只确认“被过滤的第一次信号
  是否也消费 setup”；若确认消费，另开生命周期单变量 L0/L1，不能依据 V14 亏损直接回写。

## 2026-08-04：严格视觉横盘突破多单净保本激活 V1 L1/L2

- [x] 冻结唯一变量为净保本激活阈值，同轴预注册 `0.5R` 与 `1.0R`；V6 入场、初始止损、
  冻结目标、成本、冲突和资金路径保持不变。
- [x] L1 无标签几何扫描确认 290 笔严格横盘基线成交中，283 笔固定退出多单风险与目标完整；
  两个激活价均不高于目标，0.5R 中 6 笔使用双边成本保本下限。
- [x] 完成棒最高价只负责激活，保护从下一棒生效；tick 向上取整、同棒禁止反推和跳空实际开盘
  退出测试通过，交易身份变化为 0。
- [x] L2 成本后基线为 `-61.0147R / EV -0.2104R / PF 0.7502`；0.5R 为
  `-62.2187R / EV -0.2145R / PF 0.5249`，1R 为
  `-72.7651R / EV -0.2509R / PF 0.5899`，两者均恶化。
- [x] 0.5R 救回原止损 `+117.4345R`，但截断原止盈 `-118.6593R`；1R 分别为
  `+67.9956R / -79.7460R`，证实硬净保本无法区分正常回踩与真正失败。
- [x] CLI 全部 30 个测试、Rustfmt 2021、定向行数硬门禁和约 132 秒 Release 回放通过；机器结果
  SHA-256 为 `e2ddc000fdb6b9765b0fb3ebeec37ee51a2e40ba2e03da7a832ddd907e1b3676`。
- [x] 追加最近 12 笔实际亏损与最近 12 笔保本误伤原止盈审计：亏损条件样本中 0.5R 可把
  `-15.433R` 改善至 `-6.428R`，但全样本会截断 52/81 笔原止盈；确认不能用亏损样本反推规则。
- [ ] V1 固定为 `stopped_at_l2_negative_exit_edge`；不进入 L3、不创建或修改 Pine、不接
  Paper/ReadOnly/Live 或生产。若继续，另立“完成棒收盘站上 1R 才激活”的单变量 L1。

## 2026-08-04：严格视觉横盘突破多单完成收盘 1R 净保本 V2 L1/L2

- [x] 在查看 V2 outcome 前冻结唯一变量：把 V1 的“完成棒最高价达到 1R”替换为“完成棒收盘价
  达到 1R”；净保本价、成本、tick、下一棒生效、入场、止损和目标全部不变。
- [x] L1 无标签检查确认 290 笔基线身份、283 笔可应用 Fixed 多单和 7 笔保持原样交易；全部
  1R 激活价不高于冻结目标，允许进入部分成员 L2。
- [x] 上影触达不激活、确认棒不得自触发、下一棒保护和跳空实际开盘边界测试通过；focused
  `9/9`、完整 CLI `33/33`，交易身份变化为 0。
- [x] V2 完成收盘激活 101 笔、覆盖 91 个事件簇、改变 58 笔退出；成本后为
  `-75.1793R / EV -0.2592R / PF 0.6297`，相对 V6 少 `14.1646R`，比 V1 最高价 1R 也少
  `2.4142R`。
- [x] 归因完成：救回 34 笔原止损贡献 `+41.6654R`，截断 24 笔原止盈损失 `-55.8300R`；
  17/43 币改善、21/43 币恶化，6/13 月改善、7/13 月恶化。
- [ ] V2 固定为 `stopped_at_l2_negative_exit_edge`；不进入 L3、不创建或修改 Pine、不接运行态。
  下一轮退回 L0，只单独定义“横盘结构失效保护位置”，不继续搜索固定 R 或确认根数。

## 2026-08-04：EMA576 EMA144 结构止损净 2R 目标 V15

- [x] 冻结唯一变量：把 V14 成本前固定 `0.52R` 目标替换为按每边 `8 bps` 成本反解的
  成本后净 `2.00R` 目标；形态、首笔真实成交、下一根开盘、EMA144±0.30ATR 止损、
  24 小时、同 K 止损优先和同币种锁全部不变。
- [x] L1 仅用入场时字段检查 2,651 笔冻结计划，合法目标 2,651、非法 0；覆盖 44 币、
  13 个北京时间自然月和双方向，XRP、1INCH、UMA 均精确结算为净 2R。
- [x] V15 L2 完整交易 2,651 笔、1,424 个事件；毛
  `EV +0.070857R / PF 1.101154`，成本后
  `-1428.526R / EV -0.538863R / PF 0.526433`，多空均负、13 月全部为负。
- [x] 归因完成：目标赢家 794 笔、止损 1,857 笔；实际胜率 29.95%，按平均净亏损
  `-1.6244R` 需要 44.82% 才能盈亏平衡。平均成本拖累 `0.6097R/笔`，
  37.87% 交易成本超过 0.5R。
- [x] EMA144/576 模块 106 个测试、Rustfmt、定向行数门禁通过；V14 2,651 笔逐笔及指标 parity
  通过。V15 机器报告 SHA-256 为
  `802527ed3c58627077337aca5a03a46cf57102a3f9d2b3a68eaa67dc3bed161c`。
- [ ] V15 固定为 `stop / Research-only`；不进入 L3、不搜索其他目标 R、不创建 Pine、
  不接 Paper/ReadOnly/Live 或生产。若继续，另立“入场时最大成本 R / 最小可交易风险距离”
  单变量 L1，阈值必须按交易经济性事前冻结。

## 2026-08-04：严格视觉横盘突破多单 1R 后上沿收盘失效退出 V3

- [x] 冻结唯一变量：完成收盘达到 1R 后，只有更晚完成棒严格收盘跌破信号时冻结横盘上沿，
  才在下一根真实开盘全仓退出；不增加缓冲、连续确认、分批、保本、目标或入场过滤。
- [x] L1 无标签检查确认 290 笔严格视觉候选中，283 笔 Fixed 多单具备冻结上沿、横盘长度、
  正初始止损 tick 与目标 tick，覆盖 43 币、13 个上海月份和全部 8～96 根横盘长度。
- [x] L2 激活 101 笔、结构失效并改变退出 22 笔；20 笔原止损改善 `+9.8288R`，2 笔原止盈
  被截断 `-5.4603R`，8 bps 下相对 V6 净改善 `+4.3684R`。
- [x] V3 成本后仍为 `-56.6463R / EV -0.1953R / PF 0.7597`；182 笔从未完成收盘达到 1R
  的交易贡献 `-173.5169R`，确认 1R 后退出不是主亏损源。
- [x] 专项因果测试 6/6、CLI 完整测试 39/39、Rustfmt 与 2,000 行硬门禁通过；机器结果和结论
  SHA-256 分别为 `6ef2936b937e8d3921771bd3984bfe930a1cfc7880a569f9f1c1bfa44b741bed`、
  `28e19de1f25823f0101c17a39fbd3542d818478036f9bef2d199037b76b367b0`。
- [ ] V3 固定为 `stopped_at_l2_negative_cost_adjusted_edge`；不进入 L3、不创建或修改 Pine、
  不接 Paper/ReadOnly/Live 或生产。若继续，另立“信号确认收盘相对冻结上沿的 ATR 接受余量”
  单变量 L0/L1，必须使用事前阈值和新窗口验证。

## 2026-08-04：EMA576 EMA144 入场止损成本 0.50R 门禁 V16

- [x] 在查看 V16 outcome 前冻结唯一变量：下一根连续开盘与 EMA144±0.30ATR 结构止损已知后，
  只有止损成交双边成本 `<=0.50R` 才允许真实入场；拒绝不消费 setup，V15 净 2R 目标保持不变。
- [x] L1 完整扫描 15,024 个 V14 候选：14,987 个合法机会中拒绝 6,255、保留 8,732，影响
  41.7362%；覆盖多空 4,849/3,883、44 币、13 个北京时间月份和 2,908 个事件。
- [x] 1INCH 19:30、UMA 20:15、XRP 21:00 的止损成本分别为
  `1.355888R / 1.144888R / 2.459450R`，均在成交前拒绝；三个 setup 后续无实际成交。
- [x] 唯一一次 L2 完成 2,258 笔：毛 `EV +0.044726R / PF 1.065324`，成本后
  `-616.687R / EV -0.273112R / PF 0.697804`；多空和 13 个月全部为负。
- [x] 相对 V15 平均成本下降 47.87%、净 EV 改善 0.265751R、PF 改善 0.171371，但目标命中率
  31.53% 仍低于按平均净亏损计算的 39.76% 盈亏平衡线；44 币仅 7 币净正。
- [x] EMA144/576 相关测试 111/111、Rustfmt、定向行数门禁、V16 bin 编译与 V15 删除生成时间后的
  逐字段 parity 通过；共用主文件拆为 917 行、入场模块 110 行。机器报告 SHA-256 为
  `0c199ad758becefab58a03da53837e85c022b6e4d0e510fa7b34c26332307cf2`。
- [x] V16 固定为 `stop / Research-only`；不进入 L3、不搜索其他成本阈值、不创建 Pine、不接
  Paper/ReadOnly/Live 或生产。若继续，退回 L0/L1 单独定义顺势确认 K 的入场接受质量。

## 2026-08-04：严格视觉横盘突破确认收盘 ATR 接受余量 V7 L1

- [x] 冻结唯一变量
  `(confirmation_close - frozen_range_upper) / frozen_source_atr`，事前网格为
  `0.05/0.10/0.15/0.20 ATR`；禁止同时修改确认窗口、止损、目标或退出。
- [x] 只读取 V6 无标签 L1 账本的 421 笔候选；余量中位数 `0.5197 ATR`、P25
  `0.3049 ATR`，421/421 字段有效，未读取成交后结果。
- [x] 四档分别拒绝 1.90%、6.18%、9.98%、14.49%；最高 0.20 ATR 仍低于预注册 15%
  最小影响，`selected_threshold_atr=null`，L1 门禁失败。
- [x] 前向覆盖审计确认 43/43 成员各有 813 根新增完成 K，统一到上海时间 07-28 09:30；
  仅约 8.47 天，不足以作为正式 OOS。
- [x] 机器结果与结论报告 SHA-256 分别为
  `54ded4c292258ff58967e2c1277247bde32cd4b5467623b02dab7c205c918944`、
  `5c2a301b61e36749a7e3400f6e3dc40dfcd9a62906e187ab465395c1b1a5fa2e`。
- [ ] V7 固定为 `stopped_at_l1_insufficient_preregistered_impact`；不实现 Rust 变体、不跑 L2、
  不创建 Pine 或接运行态。若继续，另立只含 `0.30/0.40/0.50 ATR` 的新无标签预注册。

## 2026-08-04：严格视觉横盘突破确认收盘强接受余量 V8 L1/L2

- [x] 冻结唯一变量为确认收盘高出冻结上沿的来源 ATR 倍数，网格仅
  `0.30/0.40/0.50 ATR`；按无 outcome 拒绝比例最接近 30% 的规则选中 `0.40 ATR`。
- [x] L1 三档拒绝比例为 `24.23% / 34.20% / 46.56%`；Rust V8 精确复现
  144 笔拒绝与 277 笔保留，43 币、13 月、234 个保留事件，来源合同漂移为 0。
- [x] L2 实际删除 97 笔严格家族交易，覆盖 39 币、13 月、93 事件；家族净 R 从
  `-61.0147R` 改善为 `-40.6796R`，但平均净 R 从 `-0.210396R` 恶化为
  `-0.210775R`，PF 从 `0.750223` 恶化为 `0.747981`。
- [x] 被删除子集平均 `-0.209640R / PF 0.754590`，略好于保留子集；总 Candidate V20
  净 PnL 从 `-3386.57` 恶化为 `-4162.74`，PF 从 `0.877676` 恶化为 `0.849281`。
- [x] V8 专项 3 个测试、L1 runner 6 个测试、CLI 身份测试、Rustfmt、Release L1/L2 与
  2,000 行硬门禁通过；完整严格横盘名称过滤测试仍有 1 个不经过 V8 的 Baseline 旧断言失败。
- [x] V8 固定为 `stopped_at_l2_no_quality_separation_and_total_portfolio_regression`；不进入 L3、
  不再搜索接受余量阈值、不创建或修改 Pine、不接 Paper/ReadOnly/Live 或生产。若继续，另立
  “冻结初始风险中的往返成本占比”单变量 L0/L1。

## 2026-08-04：EMA576 资格关系周期、2.5 ATR 与 8 根接受 V17～V19

- [x] 逐根还原 ADA：07-03 08:30 空头资格在 07-08 18:15 EMA144 下穿 EMA576 时已经离开
  原关系周期，07-18 10:45 又上穿后属于新周期；旧逻辑仅重置当前连续计数，没有清除历史
  `latched`，且 24 小时超时只从首次交易信号启动，故错误复用到 07-19 00:30 突破。
- [x] V17 只增加“setup 到突破必须保持同一 EMA144/576 关系周期”：拒绝 V16 可入场机会
  2,620/8,732=`30.0046%`，ADA 在 07-08 18:15 准确失效；L2 成本后
  `EV -0.284635R / PF 0.686411`，未改善 V16。
- [x] V18 只增加突破确认收盘距离 `>=2.50 ATR14`：拒绝 8,178/8,732=`93.6555%`，ADA
  `0.8867 ATR`、ONT `1.1054 ATR` 均被拒绝；L2 仅 179 笔，毛与净分别为
  `EV -0.057388R / PF 0.919747`、`EV -0.353104R / PF 0.617412`。
- [x] V19 只增加连续 8 根完成收盘接受：ADA 只有 2 根、ONT 只有 6 根；实际影响
  3,282/8,732=`37.5859%`，低于事前 50% 下限，按门禁停止在 L1，不运行 L2。
- [x] EMA144/576 专项 114/114、三个 bin 编译、Rustfmt 与定向 2,000 行门禁通过；共用新模块
  955 行。三份机器报告 SHA-256 为
  `9cc9ad63c4f4230d5cde94c0236c13f4ba7d11d29b5cb6f4b944909382e5e77b`、
  `9445d82dc1ff898662a82d627220c4caf69cb91ecb3beccc5c652e33c8cee977`、
  `032d345888af7b20ed209198426697466c72f8377e97d5b0d5c8bc333629c9a0`。
- [x] 业务语义保留 V17 的“关系周期结束即清除旧资格”；V17～V19 均不晋级 L3，不合并条件、
  不创建 Pine、不接 Paper/ReadOnly/Live 或生产。后续若继续，应在新样本另立一个新的入场质量变量。

## 2026-08-04：EMA576 V17 + 2.5 ATR + 8 根接受组合 V20

- [x] 按用户明确决定建立独立 V20 组合合同：V17 资格关系周期失效、确认收盘距离至少
  `2.50 ATR14`、突破后连续 8 根同侧完成收盘必须同时通过；旧 V17～V19 身份不覆盖。
- [x] 在查看组合覆盖前冻结预计影响 `93.5%～99.9%`，并保留多空各至少 100、8 币、6 月、
  100 事件的 L1 停止门禁；V16 成交、结构止损、净 2R、成本和冲突路径保持不变。
- [x] L1 扫描 8,732 个 V16 合格机会，拒绝 8,407、保留 325，影响 `96.2781%`；覆盖
  41 币、13 月、214 事件，多头 230、空头 95。
- [x] ADA 位图 `0`，三项全失败且 07-08 18:15 先因关系周期失效；ONT 位图 `1`，只通过资格
  周期，2.5 ATR 与八根接受均失败。三位位图完整保存逐项无 outcome 证据。
- [x] 唯一失败门禁为 `both_directions_retain_at_least_100=false`；不把 100 事后改成 95，状态固定为
  `stopped_at_l1_insufficient_short_coverage`，没有运行 L2。
- [x] EMA144/576 专项 115/115、V20 bin 编译、Rustfmt、定向 2,000 行与 JSON 合同检查通过；
  共用实现仍低于 1,000 行。机器报告 SHA-256 为
  `d4724c475099f04b8ddc6d852b47e3f5310927b7e3ec924ee0724839bb103552`。
- [x] V20 保留为规则不再变化的 Research-only 组合合同；不创建 Pine、不接 Paper/ReadOnly/Live、
  调度或生产。若继续，只能使用新增非重叠窗口重新生成相同规则基线，补足空头覆盖后再决定 L2。

## 2026-08-04：严格视觉横盘突破外部结构上沿门禁 V9 L1/L2

- [x] 在查看 outcome 前冻结唯一变量：`visual_upper` 保留父横盘内部 P90；交易突破上沿在尚未
  解决时增加横盘开始前 `min(range_length, 32)` 根完成 K 的最高价，突破棒收盘必须越过 1 tick。
- [x] L1 保留 V8 候选 139/277、拒绝 138，新增候选 0、拒绝身份遗漏 0；覆盖 42 币、13 月、
  126 个事件。BTC 07-15 06:15 以外部高点 65,100、突破高点 65,097.1 被准确拒绝。
- [x] L2 严格家族成交 193→116，成本后净 R `-40.6796R→-28.0837R`；但平均净 R
  `-0.210775R→-0.242101R`、PF `0.747981→0.716759`、胜率 `30.05%→29.31%` 全部恶化。
- [x] 删除的 77 笔自身为 `-12.5960R / EV -0.163584R / PF 0.797701`，比保留集合更好；
  96 根横盘被删 5 笔合计 `+4.9868R / PF 3.0622`，确认 raw-high 窗口对长横盘误伤明显。
- [x] L1 runner 6/6、Top60 runner 39/39、普通库编译、Rustfmt 和 2,000 行硬门禁通过；全库
  lib test 被两个无关 EMA576 已声明但缺失文件的模块阻塞，未冒充全绿。
- [x] V9 固定为 `stopped_at_l2_no_quality_separation`；不进入 L3、不创建/修改 Pine、不接
  Paper/ReadOnly/Live 或生产。下一轮回到 V8，只研究“横盘持久性决定止盈”这一项。

## 2026-08-04：EMA576 2.0 ATR 与八根 EMA576 保持 V21～V22

- [x] 在新结果产生前拆成两个单变量版本：V21 只把 V20 距离 `2.50→2.00 ATR14`；V22 只把
  V21 的 EMA144±0.30ATR 提前回踩否决替换为 EMA576 盘中严格穿越，其他合同冻结。
- [x] V21 保留 623/8,732，较 V20 增加 298；多空 398/225、44 币、13 月、387 事件均足够，
  但影响 `92.8653%` 低于预注册 `93.5%` 下限，停止在 L1，未运行 L2。
- [x] V22 保留 613/8,732，多空 388/225、44 币、13 月、381 事件；全部 L1 门禁通过。相对
  V21 最终交集纯删除 10 个多头、增加 0 个；接受组件净减少 1,037 个机会。
- [x] V22 L2 完成 224 笔、196 事件：毛 `EV -0.01249R / PF 0.98218`，成本后
  `-70.0868R / EV -0.31289R / PF 0.65658`，多头与空头均负，停止在 L2。
- [x] EMA144/576 专项 117/117、两个 bin 编译、Rustfmt、JSON 合同与定向行数门禁通过；共用实现
  恰为 1,000 行。V21/V22 机器报告 SHA-256 分别为
  `d73a9d6e45f7ad5a723edbd338655825cdcf18294cb0c454edef5c2197b56f6b`、
  `99d490ee5e074179a569f9693e99a7d4207428532802e3ff78696ee3cb1453ee`。
- [ ] V21/V22 均保持 Research-only，不进入 L3、不创建 Pine、不接 Paper/ReadOnly/Live 或生产。
  若继续，只单独研究“删除盘中高低价否决，仅保留 8 根完成收盘同侧”的真正放宽版本。

## 2026-08-05：EMA144/576 关系周期延长到信号 V23 L1

- [x] 冻结唯一变量：相对 V22，原始 EMA144/576 资格关系必须从 setup
  持续到信号收盘；任何相等或交叉立即消耗旧资格与旧突破事件。
- [x] WOO 多、LTC 空、ACT 多分别在 07-15 02:30、07-13 14:30、07-11 02:15
  失效，全部在原定信号/入场前被 V23 拒绝。
- [x] 多空镜像关系失效、EMA576 盘中穿越否决和冻结组合门禁测试 8/8 通过；
  Release 入口成功生成当前报告。
- [x] L1 保留 123/8,732，剔除 8,609，影响 98.5914%；多空 101/22、32 币、
  12 月、91 事件。空头与分散覆盖门禁失败，固定 `stop`，未读取 outcome、未运行 L2。
- [x] WOO 的 V22 同棒止损符合价格路径，根因是不应入场；V23 修正入场生命周期，
  不放宽同棒止损保护。
- [x] 机器报告 SHA-256 为
  `a538880d277865ce316d0c5c0a002fc3f9a846f87d9e1295aa6014d721ec50b2`；V23 保持 Research-only，
  不创建 Pine、不接 Paper/ReadOnly/Live、数据库写入、调度或生产。

## 2026-08-05：严格视觉横盘突破 1.0H 成本后保本 V4

- [x] 冻结唯一变量：使用信号时已经冻结的 `H = visual_upper - visual_lower`；多单完成棒达到
  `max(entry + 1.0H, 8bps 单边成本净保本价)` 后，保护从下一根 K 线开始生效，空单镜像。
- [x] L1 无标签唯一匹配 176/176，非法高度 0；覆盖 43 币、13 月、157 事件，142/176 的
  激活价不越过原目标，按预注册进入 L2。
- [x] L2 实际激活 39 笔、37 事件，改变退出 20 笔；交易身份漂移 0，冻结 V8 核心回放逐字段一致。
- [x] 8bps 下基线 `-39.8712R / EV -0.22654R / PF 0.73319`，V4 为
  `-53.6273R / EV -0.30470R / PF 0.61600`，最大回撤从 `48.9607R` 恶化到 `60.0966R`。
- [x] 9 笔原止损得到 `+10.1255R` 改善，但 11 笔原止盈被截断并损失 `23.8817R`；净变化
  `-13.7561R`，确认达到 1H 后回踩开仓附近仍是部分成功突破的正常路径。
- [x] V4 固定为 `stopped_at_l2_unconditional_range_height_break_even_destroys_convexity`；不进入 L3，
  不合入主策略，不修改 Pine/Paper/ReadOnly/Live 或生产。机器报告 SHA-256 为
  `546d015d0246b6e63d64bd8957ed9a6f72be4a4e82d9e393f96131e056d97d1c`；结论报告 SHA-256 为
  `aaecb2ef88cca0256ae0f13cef1ef16bd97b6abb0ffc1b5a3f83508b035d5842`。

## 2026-08-05：严格视觉横盘 1H 后两次连续失守 V5 L1

- [x] 查看 V5 outcome 前冻结唯一变量：1H armed 后，必须两根连续完成 K 严格收在冻结上沿下方，
  第二根收盘后才允许净保本从下一根生效；单棒收复即重置，空单镜像。
- [x] 新增隔离 L1 决策账本，只读取第二次失守收盘及以前的完成 K、冻结边界/风险和实际入场；
  记录不含最终退出时间、原因、价格、MFE、MAE、R 或胜负。
- [x] 176 笔纯家族 Fixed 交易中 39 笔达到 1H、覆盖 37 事件；只有 4 笔形成两次连续失守，
  占 10.2564%，覆盖 4 币、3 月、4 事件。
- [x] 决策数、事件、币种、月份和预注册 15%～45% 影响区间全部失败；未读取 V5 决策后 outcome，
  未运行 L2，也没有事后降低门槛。
- [x] V5 固定为 `stopped_at_l1_insufficient_two_close_failure_coverage`；机器报告 SHA-256 为
  `912ffde937a117c7c1ac85e318b1ea99948ab465dc3af8bd46084515e10ccfbd`；结论报告 SHA-256 为
  `f6b4687dc1aa44deb467d31b3a92148cfd83e1d7562b9b46476ab5f15e61c773`。不创建 Pine、不接运行态。

## 2026-08-05：EMA576 八根接受窗口极值 2ATR V24 L1

- [x] 在查看 outcome 前冻结唯一变量：V23 的第 2 根确认收盘 2ATR 改为第 1～8 根中任一
  完成 K 的顺势最高/最低价距当根 EMA576 至少 2ATR；阈值、两收盘确认及其他合同不变。
- [x] 新增多头、空头镜像、第 9 根不得补认、极值不得绕过 8 根收盘或 EMA576 盘中穿越的
  回归测试；共用定向测试 12/12 通过。
- [x] L1 保留 715/8,732，多空 470/245、44 币、13 月、440 事件；相对 V23 增加 592，
  且 V23 的 123 个通过身份缺失为 0。
- [x] WOO、LTC、ACT 的窗口极值分别达到 5.1635、3.7838、3.9407 ATR，但仍在原关系
  首次失效处拒绝，确认本轮没有复活旧资格或旧突破。
- [x] 全部 L1 门禁通过，状态为 `coverage_pass_ready_for_l2_prereg`；outcome key 为 0，
  `l2 = null`，等待独立 L2 预注册。
- [x] 机器报告 SHA-256 为
  `e0c3ed91ba38ca6782d2e45bef35461898ca888d08ab2de1daa93f28a329055d`；V23 冻结报告哈希未变。
- [x] Rustfmt、定向行数硬门禁通过；共用文件 1,204 行超过 1,000 目标但低于 2,000 硬上限，
  本轮不做无关拆分。不创建 Pine、不接 Paper/ReadOnly/Live、数据库写入、调度或生产。

## 2026-08-05：EMA576 八根接受窗口极值 2ATR V24 L2

- [x] 在读取 outcome 前冻结 V24 L1 文件 SHA、payload SHA、715 个候选 ID 集合 SHA、多空
  470/245、V14/V16 源 SHA、行情指纹，以及 next-open/结构止损/净 2R/8bps/24h/首笔成交合同。
- [x] 新增独立 V24 L2 入口；先核对每条候选的 symbol、方向、setup、突破与信号时间及 V16
  合格身份，全部一致后才允许读取 forward。L1 文件 SHA 复核未变，候选漂移 0。
- [x] 唯一一次正式回放：715 个候选全部可解析，318 个因同 setup 已有首笔成交被阻塞，397 笔
  完整交易；多 248、空 149，覆盖 44 币、13 月、324 个事件，不完整交易 0。
- [x] 成本前 `+29.0685R / EV +0.07322R / PF 1.10846`；冻结成本 `123.3525R` 后为
  `-94.2840R / EV -0.23749R / PF 0.73236`。多头净 `-87.9370R`，空头净 `-6.3470R`。
- [x] 13/13 定向测试、Release L2、Rustfmt、JSON 合同和 2,000 行硬门禁通过；共用文件 1,494 行
  超过 1,000 目标但低于 2,000 硬上限，本轮不做无关拆分。
- [x] 四项预注册 L2 门禁失败，固定为 `stopped_at_l2_negative_cost_adjusted_edge`；不进入 L3，
  不继续搜索退出或阈值，不创建 Pine、不接 Paper/ReadOnly/Live、数据库写入、调度或生产。
- [x] 机器报告 SHA-256：
  `5fa3323a02a0558d5721b008c59f479e516563a7ab1a42d97f71d6a6557283fa`；结论报告 SHA-256：
  `330fc3de2629337cfb680d835e4057b0ed878ca3925795a8b49f39469e734924`；预注册清单 SHA-256：
  `8a56daf1450e2a74c96bedfaac0b659d450d75a1012bd447a03264626c21aabd`。

## 2026-08-05：严格视觉横盘双向保留突破合同 V1

- [x] 在查看结果前冻结完整新合同：50% 实体占比、15 bps 方向实体、第 1～5 根首次回踩、
  保留 25% 越界幅度、取消量能门禁与量比分档目标、一个横盘高度目标、多空镜像。
- [x] 新增独立策略身份，不覆盖严格视觉 V1～V9；旧实体中点、0.40 ATR 和外部结构门禁不进入新合同。
- [x] 完成 Rust 状态机、空头家族、量度目标和候选账本接入；把已达 2,000 行的 `signals.rs`
  入场构造拆到独立模块，相关文件均低于 2,000 行。
- [x] L1 无 outcome 通过：43/60 partial 成员共 15,998 个候选，多空 7,857/8,141、43 币、
  13 月、4,126 事件；弱离区未解决 0、最大年龄 1 根、旧拒绝路径 0。
- [x] L2 成本后失败：多头 7,517 笔，EV `-0.26152R`、PF `0.61689`；空头 7,680 笔，
  EV `-0.17231R`、PF `0.73041`。43 个成员的严格视觉交易成本后全部为负。
- [x] 固定为 `stopped_at_l2_negative_cost_adjusted_edge / Research-only`；不进入 L3，不创建 Pine，
  不接主策略默认版本、Paper/ReadOnly/Live、worker、调度或生产。

## 2026-08-05：严格视觉横盘突破棒极值止损 V1

- [x] 在读取结果前冻结唯一变量：多头止损为突破棒最低价外一 tick，空头为最高价外一 tick；
  突破棒完成时冻结，确认后下一开盘若已越过止损则直接阻塞。
- [x] 新增独立 V11 身份，保留 V10；横盘、突破、1～5 根接受、一个横盘高度目标、成本和币池不变。
- [x] L1 无标签候选身份一致：V10/V11 均为 15,998 笔，多空 7,857/8,141；去掉新增止损字段后
  候选 SHA 完全一致，V11 非法或缺失结构止损为 0。
- [x] L2 严格视觉家族 15,260 笔：零成本 EV `+0.00303R`、PF `1.00507`；8bps/边成本后
  EV `-0.29854R`、PF `0.62703`，差于 V10 的 `-0.21644R / 0.67239`。
- [x] 根因审计：722 笔风险 `<0.5 ATR`，平均成本侵蚀 `0.91692R`；极端一个 tick 风险样本成本侵蚀
  `16.4152R`。16 个下一开盘越过止损的候选被正确阻塞。
- [x] 固定为 `stopped_at_l2_negative_cost_adjusted_edge / Research-only`；不进入 L3，不创建 Pine，
  不接 Paper/ReadOnly/Live、worker、调度或生产。
- [x] 预注册、机器报告、结论报告 SHA-256 分别为 `4fda8d30...dfd09`、`13717e7a...3bfc9`、
  `822ac1db...e23e`；定向测试、两个入口编译和 2,000 行硬门禁通过。

## 2026-08-05：严格视觉横盘突破棒结构止损 + 最小 1ATR 风险 V12

- [x] 在读取 V12 outcome 前冻结唯一变量：保留 V11 突破棒极值结构止损，同时要求实际开仓到
  最终止损至少为确认棒 `1.00ATR14`；多头取更低者、空头取更高者。
- [x] 保留“下一开盘已越过结构止损则阻塞”，1ATR 下限不得复活失效结构；信号、横盘、回踩、
  一个横盘高度目标、成本、币池、冲突和资金路径不变。
- [x] L1 V11/V12 均为 15,998 个候选，多空 7,857/8,141；删除唯一新增字段后候选 SHA 同为
  `2f28aee3...34db4`，证明没有 outcome 驱动的信号漂移。
- [x] L2 V12 严格视觉家族已平仓 15,238 笔；风险 `<1ATR` 从 V11 的 5,254 笔降为 0，最小
  `1.000002ATR`；平均成本拖累从 `0.30156R` 降至 `0.23971R`。
- [x] 成本后改善到 `EV -0.24568R / PF 0.65780`，但仍为负且差于 V10 的
  `-0.21644R / 0.67239`；固定为 `stopped_at_l2_negative_cost_adjusted_edge / Research-only`。
- [x] 不搜索其他 ATR 下限，不进入 L3，不创建 Pine，不接 Paper/ReadOnly/Live、worker、调度或生产。
- [x] 预注册、机器报告、结论报告 SHA-256 分别为
  `c6564447a332c0c8c039e2ac96ba23745b781551c8b7270bfe3108b97ffcec5a`、
  `f9638cc8d8918de0140fc57b5e3d9d6e8914ceea3bee78772402211502bb7cd4`、
  `98cfad4a316fd6aa33d0fb12519346ebaa17788e0e70217dbefaff37f59292ad`。

## 2026-08-05：EMA576 六根确认与 EMA144±1ATR 结构止损 V25/V26

- [x] V25 只把 V24 的八根同侧收盘缩短为六根；窗口内任一顺势高/低点达到 2ATR、EMA576
  盘中保持、EMA144/576 关系周期和其余信号合同不变。17/17 定向测试通过。
- [x] V26 独立把初始止损改为多单 `EMA144-1ATR14`、空单 `EMA144+1ATR14`；V24/V25
  原版本未覆盖，下一根开盘、净 2R、0.50R 成本门禁、首笔 setup 和 24h 合同不变。
- [x] 无 outcome L1 从 1,207 个 V25 质量候选中保留 1,157 个，多空 754/403；旧 720 个
  成本合格身份全部保留，437 个旧成本拒绝因风险变宽重新合格，50 个仍被成本门禁拒绝。
- [x] 配对 L2：0.30ATR 基线 401 笔，成本后 `EV -0.32217R / PF 0.65007`；1ATR V26
  540 笔，成本后 `EV -0.19592R / PF 0.76219`，EV 与 PF 改善但仍为负。
- [x] 入场 K 止损占全部止损从 35.2313% 降至 12.5683%，60 分钟内止损从 74.7331%
  降至 51.0929%；止损过窄症状已改善，但多头 `EV -0.30722R`、空头 `EV -0.01828R`。
- [x] V26 固定为 `stopped_at_l2_negative_cost_adjusted_edge / Research-only`；不继续搜索其他
  ATR 缓冲，不进入 L3，不接 Pine/Paper/ReadOnly/Live、worker、调度或生产。
- [x] V26 L1/L2/结论报告 SHA-256 分别为 `56e3eb7d...ac478`、`91f95abf...24f05`、
  `627bb244...41e39`；入口编译、1ATR 镜像测试、候选/行情/风险/退出合同复核通过。
- [x] 当前结论报告已按实际开仓时间倒序补齐最近 10 笔成本后亏损交易；每笔包含完整北京时间
  生命周期、信号指标、入场/止损/目标/退出价格及 gross/cost/net R。该要求已写入研究工作流，
  后续所有存在完整亏损成交的 L2/L3 结论报告必须执行。

## 2026-08-05：V26 合并到 TradingView 并执行严格数据对账

- [x] 新增独立 Research-only Pine：
  `docs/strategy_list/15min_ema576_six_close_structural_stop_1atr_v26_strict_parity_research.pine`；
  不覆盖 V24/V25/V26 Rust 基线，不创建提醒、Paper、Live 或 broker emulator 订单。
- [x] 通过本地 TradingView MCP 保存为独立脚本
  `15m EMA576 Six-close Structural Stop V26 Strict Parity Research`；Pine v6 编译 0 错误，
  本地源码与编辑器源码除 CRLF 外完全一致。
- [x] 旧 `15m Strict Visual Consolidation V1` 已按本地 276 行源码恢复并逐字复核；受 Basic
  指标数量上限影响，只从当前图表布局移除，已保存脚本本身未删除、未被 V26 覆盖。
- [x] 对 Rust V26 最近 10 笔完整交易做行情层逐笔核对：52/52 个唯一生命周期时间均存在；
  10/10 信号 K 收盘相等、10/10 下一根开盘相等且差值为 0；8 笔止损与 2 笔目标均由
  TradingView 对应退出 K 的高低价确认。
- [x] 严格状态机保持 fail-closed：Rust 冻结加载起点为 `2025-06-21 17:15 UTC`，当前
  TradingView Basic 实际最早仅能加载到 `2026-07-11 23:00 UTC`；表格显示“冻结起点未命中 / 严格数据未就绪”，
  因此不生成近似信号，也不宣称 EMA、ATR、资格周期和完整交易账本已经全量一致。

## 2026-08-05：V12 严格视觉横盘突破合并到 TradingView

- [x] 用户在 V12 停止于 L2 后明确要求创建图表审计版；新增独立 Research-only Pine
  `docs/strategy_list/15min_strict_visual_consolidation_breakout_v12_strict_parity_research.pine`，
  仅改变可视化交付边界，不改变 V12 的停止结论或运行态资格。
- [x] Pine 复刻离散横盘窗口、P90/P10 视觉边界、弱离区观察、强突破棒、1～5 根首次接受、
  突破棒极值结构止损、最小 1ATR 风险和一个冻结区间高度目标；多空镜像，未创建提醒、
  Paper、Live、broker emulator 订单或真实下单入口。
- [x] 通过本地 TradingView 保存为独立脚本
  `15m Strict Visual Consolidation Breakout V12 Strict Parity Research`；Pine v6 编译 0 错误，
  本地 746 行源码与编辑器源码除 CRLF 外完全一致。
- [x] TradingView Basic 当前最多加载两个指标；当前 15m 图表保留 V20 并加载 V12，V26 只从
  当前布局移除，已保存脚本和源码均保留，未被覆盖或删除。
- [x] 当前可见历史的本地诊断得到 113 个确认横盘、77 个强突破、46 个确认/入场/退出；退出为
  30 止损、15 目标、1 反转，成本按每边 8bps 后为 `-20.97998R`。该统计明确标为
  `V12_DIAG / 本地诊断，非严格`，不替代 Rust L2 回测。
- [x] 严格回放起点 `2025-05-02` 超出当前 Basic 历史深度，因此未宣称完整 Pine/Rust parity；
  Rust 冻结结论仍为成本后 `EV -0.24568R / PF 0.65780`、Research-only、不得 promote。
- [x] 23 个 `strict_visual` Rust 定向测试通过；Pine 行数门禁、源码差异检查和注释审计通过。
